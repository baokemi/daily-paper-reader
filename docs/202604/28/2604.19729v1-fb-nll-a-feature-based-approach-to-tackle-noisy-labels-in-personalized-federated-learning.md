---
title: "FB-NLL: A Feature-Based Approach to Tackle Noisy Labels in Personalized Federated Learning"
title_zh: FB-NLL：一种基于特征的方法，用于解决个性化联邦学习中的噪声标签问题
authors: "Abdulmoneam Ali, Ahmed Arafa"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19729v1"
tags: ["query:flrlvldm"]
score: 9.0
evidence: 个性化联邦学习框架与优化
tldr: 本文提出FB-NLL框架，通过基于特征空间的用户表征与谱聚类方法，将用户聚类与训练动态解耦，并引入特征一致性标签校正机制，有效应对个性化联邦学习中的噪声标签问题，减少通信开销并提升性能稳定性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19729v1/fig-001.webp\", \"caption\": \"TABLE X: PFL performance under class-dependent noise across various label detection and correction methods.\", \"page\": 13, \"index\": 1, \"width\": 522, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.19729v1/fig-002.webp\", \"caption\": \"TABLE XI: PFL performance under uniform noise across various label detection and correction methods.\", \"page\": 13, \"index\": 2, \"width\": 522, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.19729v1/fig-003.webp\", \"caption\": \"TABLE IX: PFL performance under class-independent noise across various label detection and correction methods.\", \"page\": 13, \"index\": 3, \"width\": 523, \"height\": 325}]"
motivation: 现有基于学习动态的个性化联邦学习方法易受低质量数据和噪声标签影响，降低模型个性化效果。
method: 提出以特征空间协方差谱结构为基础的几何聚类和特征一致性标签校正策略，独立于模型和训练迭代。
result: FB-NLL在多数据集与噪声条件下均显著提升个性化联邦学习的准确率与稳定性。
conclusion: FB-NLL能高效构建抗噪声的个性化模型，比现有方法更稳定且准确。
---

## 摘要
个性化联邦学习（Personalized Federated Learning，PFL）旨在在异质数据分布下学习多个任务特定模型，而非单一的全局模型。现有的 PFL 方法通常依赖迭代优化过程——例如模型更新轨迹——来将需要完成相同任务的用户聚类。然而，这些基于学习动态的方法本质上易受到低质量数据和噪声标签的影响，因为受损的更新会扭曲聚类决策，降低个性化性能。为此，我们提出 FB-NLL，这是一种以特征为中心的框架，将用户聚类与迭代训练动态解耦。通过利用局部特征空间的内在异质性，FB-NLL 通过特征表示协方差的谱结构表征每个用户，并利用子空间相似性来识别任务一致的用户分组。这种基于几何的聚类方法不依赖标签，并在训练前以一次性方式执行，与迭代基线相比显著减少通讯开销和计算成本。与此同时，我们引入了一种基于特征一致性的检测与纠正策略，以处理聚类内的噪声标签。通过利用学习特征空间中的方向对齐，以及基于类别特定特征子空间进行标签分配，我们的方法减轻了受损监督的影响，而无需估计随机噪声转移矩阵。此外，FB-NLL 与模型无关，并可与现有的抗噪训练技术无缝集成。大量跨不同数据集和噪声设定的实验表明，我们的框架在平均准确率与性能稳定性方面均显著优于当前最先进的基线方法。

## Abstract
Personalized Federated Learning (PFL) aims to learn multiple task-specific models rather than a single global model across heterogeneous data distributions. Existing PFL approaches typically rely on iterative optimization-such as model update trajectories-to cluster users that need to accomplish the same tasks together. However, these learning-dynamics-based methods are inherently vulnerable to low-quality data and noisy labels, as corrupted updates distort clustering decisions and degrade personalization performance. To tackle this, we propose FB-NLL, a feature-centric framework that decouples user clustering from iterative training dynamics. By exploiting the intrinsic heterogeneity of local feature spaces, FB-NLL characterizes each user through the spectral structure of the covariances of their feature representations and leverages subspace similarity to identify task-consistent user groupings. This geometry-aware clustering is label-agnostic and is performed in a one-shot manner prior to training, significantly reducing communication overhead and computational costs compared to iterative baselines.   Complementing this, we introduce a feature-consistency-based detection and correction strategy to address noisy labels within clusters. By leveraging directional alignment in the learned feature space and assigning labels based on class-specific feature subspaces, our method mitigates corrupted supervision without requiring estimation of stochastic noise transition matrices. In addition, FB-NLL is model-independent and integrates seamlessly with existing noise-robust training techniques. Extensive experiments across diverse datasets and noise regimes demonstrate that our framework consistently outperforms state-of-the-art baselines in terms of average accuracy and performance stability.

---

## 论文详细总结（自动生成）

## 一、研究问题与背景

- **研究主题**：本文聚焦于个性化联邦学习（Personalized Federated Learning，PFL）中的**噪声标签问题**与**用户聚类挑战**。  
- **问题动机**：
  - 在PFL中，不同用户的数据分布差异较大，因此需要为不同任务或用户群体学习多个个性化模型，而非单一的全局模型。  
  - 现有方法通常依赖**训练动态信息**（如梯度、模型更新轨迹、损失函数等）来进行用户聚类，但这类方法容易被**标签噪声和低质量数据**干扰，从而削弱聚类准确性和个性化性能。  
  - 同时，在联邦环境中，服务器无法直接访问底层数据，数据质量监控困难，使得噪声标签问题更加突出。  
- **核心问题**：
  1. 在不依赖于迭代优化或训练动态的情况下，如何精确地对用户进行聚类？  
  2. 在聚类完成后，能否在训练前检测并纠正用户本地数据中的噪声标签？

---

## 二、方法论：FB-NLL 框架

FB-NLL（**Feature-Based Noisy Label Learning**）提出了一种完全基于特征的、训练前的一次性框架，包括两个主要组件：

### （1）特征空间谱聚类（Feature-based Clustering）

- **核心思想**：  
  通过每个用户本地数据的特征协方差矩阵的谱结构（eigen-structure），用其主要特征子空间（主成分方向）表征用户的数据分布特征。  
  然后计算用户间**谱子空间相似性**，从而在**无需标签、无需梯度**的条件下完成用户聚类。

- **关键技术步骤**：
  1. 每个用户 \( k \) 计算本地特征协方差矩阵：
     \[
     \Sigma_k = \frac{1}{n_k} \Phi(X_k)^\top \Phi(X_k)
     \]
     其中 \(\Phi(\cdot)\) 表示特征提取函数。
  2. 对 \(\Sigma_k\) 进行特征分解，提取前 \(q\) 个主特征向量 \(V_k\)。
  3. 仅交换主特征向量（不传模型参数或数据）以估算用户间的“投影能量相似度”：
     \[
     e_i^{(k,j)} = \left\| \Sigma_k v_i^{(j)} \right\|_2^2
     \]
  4. 定义归一化的相关性评分：
     \[
     s_i^{(k,j)} = \frac{\min(\lambda_i^{(k)}, e_i^{(k,j)})}{\max(\lambda_i^{(k)}, e_i^{(k,j)})}
     \]
     并聚合得到总体相似度 \( r(k,j) \in [0,1] \)。  
  5. 服务器基于该对称相似度矩阵 \(R\) 进行层次凝聚聚类（HAC），以获得用户聚类身份。

- **算法特性**：
  - 一次性（One-shot）完成聚类；
  - 通信量极低，只需交换少量主成分向量；
  - 完全不依赖于标签与模型结构，因此具有模型无关性。

---

### （2）噪声标签检测与纠正（Feature-consistency-based Correction）

- **核心思想**：  
  在完成用户聚类后，利用服务器端可获取的少量干净验证样本（每类200～300个特征，约占4%数据）建立**类特征子空间基准**，并以**特征空间对齐**为标准识别与修复标签错误。

- **主要步骤**：
  - **阶段1：类级修正**  
    比较客户端局部类别特征子空间 \( \tilde{V}_c \) 与服务器端类基准子空间 \( Q_c \) 的相似分数。  
    若某一局部类别与单一干净类别的平均相似度超过阈值 \( \tau_{sim} \)，则将该类整体重新标注。
  - **阶段2：样本级修正**  
    对未通过阈值的样本，执行逐样本投影：
    \[
    \tilde{c} = \arg\max_c \| Q_{c}^{(l)} (Q_{c}^{(l)})^\top z \|_2
    \]
    即将样本 \(z\) 分配给其在特征子空间中投影能量最大的类别。

- **隐私保护**：服务器只与用户交换主分量特征，不接触具体数据或真标签。  
- **可组合性**：该模块可与任意噪声鲁棒训练算法结合使用，形成插件式模块。

---

## 三、实验设计与对比

### 数据集与场景
- **CIFAR-10** 与 **SVHN**，采用ResNet18或ResNet10模型结构；
- 用户数 \( K = 20 \)，按不同任务划分；
- 提供干净验证集（每类200～300样本，占总数据的约4%）；
- 评估三种噪声模式：
  1. **类无关噪声（Class-independent）**
  2. **类依赖噪声（Class-dependent）**
  3. **任务无关均匀噪声（Uniform）**  
  均在多任务设定（2、3、5任务）下测试。

### 对比基线
- **聚类阶段对比**：
  - IFCA-PFL（迭代式损失最小化聚类）
  - Single global model（单模型）
- **标签修正阶段对比**：
  - FedCorr（利用高斯混合模型识别噪声用户）
  - ClipFL（依据验证集表现剪枝噪声客户端）
  - RHFL（带噪声鲁棒损失的联邦优化）
  - 以及三种混合式：`+FB-NLL` 插件集成版本。

### 评价指标
- 平均准确率 (%)  
- 性能稳定性（标准差）  
- 通信成本（特征维度与迭代次数）

---

## 四、资源与算力说明

- 论文中未明确指出GPU型号、数量、训练时长或具体硬件资源。  
- 提供了训练配置参数：学习率 \(5\times10^{-4}\)，batch size 64，每轮本地训练2 epoch，共80轮通信。  
- 可推测实验在标准深度学习GPU平台上完成，但具体算力未披露。

---

## 五、实验数量与充分性

- **覆盖广泛**：  
  - 2个数据集 × 3种噪声类型 × 3种任务划分（2、3、5任务设置）；  
  - 聚类模块与标签纠正模块均进行了独立评估与联合测试。  
- **消融分析**：比较FB-NLL(-)（无纠错模块）与完整版本；还进行了混合型（基线+FB-NLL）实验以验证模块可插性。  
- **公平性**：统一特征提取机制与清洁验证集大小，复现环境可比。
- **结论**：实验设计较为充分，覆盖噪音强度、任务复杂度与数据异质性等维度。

---

## 六、主要结论与发现

1. **特征子空间聚类效果优越**：  
   - FB-NLL(-) 在三种噪声环境下均显著优于IFCA-PFL与全局模型，差距最高可达+20个百分点。  
   - 聚类稳健性高，方差极低。
2. **噪声校正显著提升性能**：  
   - 完整FB-NLL在CIFAR-10与SVHN上的各设定均获得最高平均准确率；  
   - 对结构化噪声（class-dependent）尤为有效。
3. **可扩展性与兼容性**：  
   - 将FB-NLL加至现有噪声鲁棒算法（FedCorr、ClipFL、RHFL）中，均提升3–24个百分点。
4. **通信效率**：  
   - 用户仅需上传约10个主特征向量即可实现稳健聚类，通信量远低于权重或梯度交换方法。

---

## 七、优点与创新点

- **方法创新**：
  - 聚类与训练动态解耦，提出**基于谱结构的一次性任务聚类算法**；
  - 首次将**特征子空间一致性**用于**噪声标签自动修正**。
- **高效与隐私友好**：
  - 无需传播模型参数或梯度，通信量低且数据隐私更强。
- **通用性强**：
  - 完全模型无关、可与任何PFL或噪声鲁棒算法组合。
- **实验全面**：
  - 多任务多噪声的跨数据集验证，并包含独立与组合实验。

---

## 八、不足与局限

- **算力与资源未披露**：无法评估训练可复现性；
- **干净验证集依赖**：方法依赖服务器端存在小规模干净样本，在实际隐私保护场景可能受限；
- **仅验证分类任务**：未在回归或序列生成类任务中验证；
- **聚类为硬划分**：未考虑用户任务重叠或软聚类情形；
- **未评估系统级影响**：如通信延迟、边缘设备匹配等。

---

**（完）**
