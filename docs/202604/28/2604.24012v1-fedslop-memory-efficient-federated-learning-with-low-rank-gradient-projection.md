---
title: "FedSLoP: Memory-Efficient Federated Learning with Low-Rank Gradient Projection"
title_zh: FedSLoP：通过低秩梯度投影实现内存高效的联合学习
authors: "Yutong He, Zhengyang Huang, Jiahe Geng"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24012v1"
tags: ["query:flrlvldm"]
score: 10.0
evidence: 基于低秩梯度投影的内存高效联邦学习优化
tldr: 本文提出了FedSLoP算法，通过随机低秩子空间投影在联邦学习中降低梯度通信与存储成本，同时保持优化进展。理论上证明其在非凸条件下的收敛性；实验表明在异构数据环境中FedSLoP实现了显著的通信与内存节省，并保持良好性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24012v1/fig-001.webp\", \"caption\": \"Figure 1: Convergence and final precision on non-IID MNIST (α = 0.1). Left: evolution of test accuracy over T = 100 communication rounds for all methods. FedSLoP closely tracks the strong baseline FedAvg-M, while sparse and partial-update methods converge more slowly and saturate at lower accuracies. Right: final test accuracy (mean ± standard deviation over three seeds).\", \"page\": 21, \"index\": 1, \"width\": 958, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2604.24012v1/fig-002.webp\", \"caption\": \"Figure 2: Sensitivity to statistical heterogeneity (Dirichlet α). Final test accuracy after T = 100 communication rounds as a function of the Dirichlet concentration parameter α. Smaller α corresponds to stronger nonIIDness. FedSLoP exhibits a flatter degradation curve than FedAvg-M, maintaining ≈ 0.93 accuracy even at α = 0.05, whereas FedAvg-M drops to ≈ 0.90. Sparse and partial-update methods and the low-rank model baseline suffer much sharper degradation, especially at α = 0.05.\", \"page\": 22, \"index\": 2, \"width\": 554, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2604.24012v1/fig-003.webp\", \"caption\": \"Figure 3: Projection-rank ablation for FedSLoP. Test accuracy vs. communication rounds for different projection ranks r ∈ {16, 32, 64, 112, 192} on MNIST with α = 0.1. Larger r accelerates convergence and improves final precision, with curves for r ≥ 112 nearly overlapping, indicating diminishing returns beyond a certain rank.\", \"page\": 23, \"index\": 3, \"width\": 932, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2604.24012v1/fig-004.webp\", \"caption\": \"Figure 4: Client-number ablation for FedSLoP. Test accuracy vs. communication rounds for different numbers of clients p ∈ {10, 20, 50, 100} on MNIST with α = 0.1 and projection rank r = 112. Larger p accelerates convergence and reduces variance across rounds, reflecting improved gradient estimation, but exhibits diminishing returns beyond p ≈ 50.\", \"page\": 24, \"index\": 4, \"width\": 946, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2604.24012v1/fig-005.webp\", \"caption\": \"Table 1: Main federated configuration on MNIST. The step-size η is chosen from an auto-tuning procedure for FedSLoP and then fixed across all methods to isolate the effect of the update structure.\", \"page\": 20, \"index\": 5, \"width\": 686, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.24012v1/fig-006.webp\", \"caption\": \"Table 2: Final test accuracy on non-IID MNIST with Dirichlet parameter α = 0.1 after T = 100 communication rounds (mean ± standard deviation over three seeds). The full-parameter baseline FedAvg-M achieves the highest final precision, while FedSLoP remains close and clearly outperforms all sparse and low-rank model baselines. Best result in bold.\", \"page\": 20, \"index\": 6, \"width\": 690, \"height\": 200}]"
motivation: 针对联邦学习在异构和资源受限环境下通信与内存开销大的问题，需开发更高效的算法。
method: FedSLoP通过随机低秩子空间投影减少梯度维度，并结合动量机制实现高效联邦优化。
result: FedSLoP在联邦MNIST分类实验中有效减少通信和内存开销，并在精度上达到或优于FedAvg等基线方法。
conclusion: 随机子空间动量方法可为高效联邦学习提供有效解决方案。
---

## 摘要
联合学习使一组客户端能够在不交换原始数据的情况下协作训练机器学习模型，但诸如 FedAvg 等标准算法在异质且资源受限的环境中会遭遇收敛缓慢以及高通信和内存开销的问题。我们提出了 FedSLoP，这是一种联合优化算法，结合了梯度的随机低秩子空间投影，从而在保持优化进展的同时减少通信和存储更新的维度。在理论方面，我们在标准的光滑性和有界方差假设下进行了详细的非凸收敛性分析，证明 FedSLoP 保证以 $O(1/\sqrt{NT})$ 的速率收敛到一阶稳定点。在实验方面，我们在异质数据划分的联合 MNIST 分类任务上进行了大量实验，结果表明 FedSLoP 能显著减少通信量和客户端内存占用，同时较 FedAvg 及代表性的稀疏或低秩基线方法取得竞争性甚至更优的准确率。综合而言，我们的结果表明，像 FedSLoP 这样的随机子空间动量方法提供了一种具有理论依据且高效的通信与内存优化的联合学习途径。代码可在以下地址获取：https://github.com/pkumelon/FedSLoP.git。

## Abstract
Federated learning enables a population of clients to collaboratively train machine learning models without exchanging their raw data, but standard algorithms such as FedAvg suffer from slow convergence and high communication and memory costs in heterogeneous, resource-constrained environments. We introduce FedSLoP, a federated optimization algorithm that combines stochastic low-rank subspace projections of gradients, thereby reducing the dimension of communicated and stored updates while preserving optimization progress. On the theoretical side, we develop a detailed nonconvex convergence analysis under standard smoothness and bounded-variance assumptions, showing that FedSLoP is guaranteed to converge to a first-order stationary point at a rate of $O(1/\sqrt{NT})$. On the empirical side, we conduct extensive experiments on federated MNIST classification with heterogeneous data partitions, showing that FedSLoP substantially reduces communication volume and client-side memory while achieving competitive or better accuracy compared with FedAvg and representative sparse or low-rank baselines. Together, our results demonstrate that random subspace momentum methods such as FedSLoP provide a principled and effective approach to communication- and memory-efficient federated learning. Codes are available at: https://github.com/pkumelon/FedSLoP.git.

---

## 论文详细总结（自动生成）

# FedSLoP：通过低秩梯度投影实现内存高效的联合学习

---

## 一、研究背景与核心问题

- **研究动机**：  
  当前联邦学习（Federated Learning, FL）通过客户端协同训练全球模型而避免数据集中化，保障了隐私，但主流算法（如 FedAvg）在**设备资源受限、数据异质（非IID）**的环境下性能下降，面临：
  - 通信成本高：梯度或模型更新为全维度传输。
  - 内存瓶颈：客户端需存储高维动量与辅助优化状态。
  - 收敛性问题：非IID导致客户端模型漂移（client drift），收敛变慢。

- **总体目标**：  
  论文旨在提出一种**同时具备通信与内存高效性**的新型联邦优化算法，在保留 FedAvg 简洁 workflow 的同时提供理论收敛保证。

---

## 二、方法论：FedSLoP 算法设计与理论分析

### 1. 核心思想

FedSLoP（Federated Stochastic Low-rank Projection）通过**在每轮通信中随机采样一个低维子空间（低秩梯度投影）**，将客户端更新限制在该子空间内，从而减少梯度和动量的维度。

### 2. 关键技术机制

- **随机子空间选择**：
  - 每轮 t，服务器在 Stiefel 流形上采样一个随机正交矩阵 \(P_t \in \mathrm{St}(d, r)\)。
  - 构造投影算子： \(\Pi_t = P_t P_t^\top\)，其中投影维数 r < d，称为**子空间比率** \(\delta = r/d\)。

- **客户端更新步骤**（FedAvg 模板的改进版）：
  1. 服务端广播全局模型参数 \( \theta_t \) 与投影矩阵 \( P_t \)。
  2. 每个客户端以 \( \theta_t \) 为初始值进行 τ 次局部更新：
     - 计算随机梯度 \( g_{i,s} = \nabla F_i(\theta_{i,s}) \)。
     - 更新动量： \( v_{i,s+1} = \mu v_{i,s} + \Pi_t g_{i,s} \)。
     - 更新模型： \( \theta_{i,s+1} = \theta_{i,s} - \eta v_{i,s+1} \)。
  3. 客户端返回模型变化 \( \Delta_{ti} = \theta_{i,\tau} - \theta_t \)，服务器平均更新：
     \[
     \theta_{t+1} = \theta_t + \frac{1}{N}\sum_{i=1}^N \Delta_{ti}.
     \]

- **理论分析**：
  - 在**非凸条件**下进行完整收敛性推导，假设：
    - 全局目标与各本地目标均为 L-光滑函数；
    - 随机梯度满足有界方差假设。
  - 主定理给出收敛速率：
    \[
    \frac{1}{T}\sum_{t=0}^{T-1} \mathbb{E}\|\nabla f(\theta_t)\|^2 = O\left( \frac{1}{\sqrt{NT}} \right)
    \]
    表明在通信轮次 T 和客户端数量 N 增加时具有**线性加速特性**。
  - 收敛界中显式包含低秩比率 δ，刻画了维度约束对方差和收敛速度影响。

---

## 三、实验设计与比较

### 1. 数据集与场景
- **主要任务**：基于 MNIST 数据集的联邦分类任务。
- **模型结构**：两层多层感知机（SimpleMLP），输入维度 784 → 隐藏层 128 → 输出 10。
- **数据划分**：使用基于标签的 Dirichlet 分布分割方式（参数 α 控制异质性）：
  - 主实验设定 α = 0.1（强非IID场景）。
  - 敏感性测试范围 α ∈ {0.05, 0.1, 0.5, 1.0}。

### 2. 基线方法对比
| 类别 | 对应算法 | 特征 |
|------|-----------|------|
| 全维度基线 | **FedAvg-M** | 标准 FedAvg + 动量 |
| 稀疏与部分更新方法 | FedMef, NeuLite, FederatedSelect | 降维或块更新，通信显著减少 |
| 低秩模型方法 | FedLoRA-M | 对模型参数进行低秩化（类似 LoRA） |
| 本文方法 | **FedSLoP** | 梯度低秩投影 + 动量 |

- **统一训练配置**：  
  - 通信轮次 T = 100；  
  - 本地批量大小 b = 32；  
  - 步长 η = 0.018（通过贝叶斯优化选取）；  
  - 客户端数量 p = 50；  
  - 动量系数 μ = 0.8。

### 3. 算力说明
- 文中**未明确指出 GPU 型号或训练时长**；实验仅涉及 MNIST 和中小模型规模，推测无需大规模算力。

---

## 四、实验数量与充分性

- **实验类别**：
  1. **主实验**：非IID MNIST，α = 0.1，对比全部基线。
  2. **异质性敏感性实验**：不同 α ∈ {0.05, 0.1, 0.5, 1.0}。
  3. **消融实验**：
     - 投影秩 r ∈ {16, 32, 64, 112, 192}。
     - 客户数量 p ∈ {10, 20, 50, 100}。
- **充分性分析**：
  - 综合进行了统计异质性、子空间维度、客户端数量三方面的系统实验。
  - 每组实验均报告平均及标准差（3个随机种子），结果客观且可重复。
  - 虽仅使用 MNIST 单一数据集，但实验分维度全面且针对关键变量展开。

---

## 五、主要结果与发现

- **性能与精度**：
  - 在 α = 0.1 条件下，FedSLoP 准确率 0.9511±0.0028，仅略低于 FedAvg-M（0.9551±0.0015），但显著优于所有稀疏及低秩模型基线。
  - 通信压缩比约为 1/7，相对 FedAvg-M 降低约 85% 通信量。

- **鲁棒性**：
  - 在极端非IID情形（α=0.05）下，FedSLoP 精度仍保持 0.9269，高于 FedAvg-M 的 0.9035。
  - 对异质性更稳健。

- **低秩投影效果**：
  - 随投影秩 r 增加，精度提升但收敛趋饱和，表明中等秩（如 r=112）即可取得平衡。
  - 客户数量超过 50 后收益递减，符合理论线性加速但存在通信瓶颈。

---

## 六、方法优点与创新点

- **方法层面**：
  - 将**低秩随机子空间投影**与 FedAvg 动量机制结合，兼顾理论与工程简洁性。
  - 无需维护额外控制变量或辅助状态（如 SCAFFOLD），显著降低客户端内存占用。
  - 充分利用 Stiefel 流形的随机投影性质，理论上保持非凸收敛阶 O(1/√NT)。

- **设计亮点**：
  - 与标准 FedAvg 完全兼容，可直接部署于现有联邦系统。
  - 理论-实验闭环：明确子空间比率 δ 与收敛速度、方差项的定量关系。

---

## 七、不足与局限

- **实验范围有限**：
  - 仅测试在 MNIST 数据集与小型 MLP 模型，缺乏在更复杂模型（CNN、Transformer）和大规模数据上的验证。
- **未提供算力细节**：
  - 无 GPU 类型、训练时间或通信环境说明，限制实际资源评估。
- **理论假设理想化**：
  - 收敛分析基于全客户端参与（full participation），尚未涵盖更常见的部分参与或异步场景。
- **通信压缩与误差反馈结合未实验**：
  - 未探讨与量化、稀疏反馈机制等协同优化的潜力。

---

## 八、总体评价

FedSLoP 在结构简洁、内存节约和理论完备之间取得良好平衡，是一个**面向资源受限异构环境的联邦优化新方案**。它证实随机低秩投影可以在不损失收敛阶的情况下显著降低资源使用，为未来扩展至大型模型与异步参与提供了可能。

---

（完）
