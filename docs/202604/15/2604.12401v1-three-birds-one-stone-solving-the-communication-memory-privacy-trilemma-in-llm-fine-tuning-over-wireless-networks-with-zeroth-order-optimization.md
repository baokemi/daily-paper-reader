---
title: "Three Birds, One Stone: Solving the Communication-Memory-Privacy Trilemma in LLM Fine-tuning Over Wireless Networks with Zeroth-Order Optimization"
title_zh: 一石三鸟：利用零阶优化解决无线网络中LLM微调的通信-内存-隐私三难问题
authors: "Zhijie Cai, Yuhao Zheng, Haolong Chen, Dongzhu Liu, Bin Wang, Guangxu Zhu"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12401v1"
tags: ["query:flrlvldm"]
score: 9.0
evidence: 针对大语言模型微调的联邦学习框架，优化了通信和隐私
tldr: 本文针对联邦学习中LLM微调面临的通信、内存及隐私三重挑战，提出结合零阶优化与空中计算的pAirZero框架，实现低比特通信与低内存消耗的安全高效无线协同微调，并在OPT-125M上验证其显著性能与隐私优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12401v1/fig-001.webp\", \"caption\": \"TABLE I LEARNING RATE GRID SEARCH, SELECTED LEARNING RATES ARE BOLDED.\", \"page\": 7, \"index\": 1, \"width\": 504, \"height\": 639}, {\"url\": \"assets/figures/arxiv/2604.12401v1/fig-002.webp\", \"caption\": \"Fig. 5. Inherent sign-reversing probability vs. z⊤ s ∇F(w(t)).\", \"page\": 10, \"index\": 2, \"width\": 1020, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.12401v1/fig-003.webp\", \"caption\": \"Fig. 6. Distribution of z⊤ s ∇L(w,B). The red lines are estimated z⊤∇L(w).\", \"page\": 10, \"index\": 3, \"width\": 529, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2604.12401v1/fig-004.webp\", \"caption\": \"Fig. 1. The workflow of pAirZero.\", \"page\": 3, \"index\": 4, \"width\": 968, \"height\": 292}]"
motivation: 联邦学习在边缘微调大型语言模型时存在通信、存储与隐私三重瓶颈。
method: 提出pAirZero框架，将零阶优化与空中计算结合以高效安全地进行LLM微调。
result: "实验表明pAirZero能将内存成本降至25%，通信负载远低于传统方法，并保持隐私保护。"
conclusion: pAirZero成功解决了LLM在无线网络微调中的通信、内存和隐私三重难题。
---

## 摘要
联邦学习（Federated Learning，FL）为在边缘协作微调大型语言模型（LLMs）提供了一条有前景的途径；然而，这一范式面临着一个关键瓶颈：由于需要交换高维梯度，通信和内存开销极其高昂。此外，近期研究表明，用户的训练数据仍可能从这些本地梯度中被恢复，从而削弱了FL在隐私保护方面的核心优势。本文针对通信、内存与隐私三难问题，提出了pAirZero框架，这是一种将零阶（Zeroth-Order, ZO）优化与空中计算（Over-the-Air, OTA）相结合的新方法。独特的是，pAirZero使资源受限的设备能够以仅比特级的通信负载提交其本地梯度，同时以推理级内存成本参与LLMs的联邦微调。该方法不仅消除了LLM微调所需的高内存要求，还缓解了传统OTA方法中存在的严格同步限制。我们进一步建立了一个严谨的优化模型，以自适应地确定最优发射功率与噪声水平，从而确保在不同信道条件下仍能实现一致的隐私保护。数值实验表明，pAirZero能够在无线网络环境中实现安全高效的LLM联邦微调，在OPT-125M上仅需25%的峰值内存成本，且通信开销比传统方法低数个数量级。

## Abstract
Federated Learning (FL) offers a promising pathway for collaboratively fine-tuning Large Language Models (LLMs) at the edge; however, this paradigm faces a critical bottleneck: the prohibitive communication and memory overheads incurred by exchanging high-dimensional gradients. Furthermore, recent studies reveal that user training data can still be recovered from these local gradients, undermining the core privacy promise of FL. In this paper, we address this trilemma of communication, memory, and privacy by proposing pAirZero, a novel framework that synergizes Zeroth-Order (ZO) optimization with Over-the-Air (OTA) computation. Uniquely, pAirZero enables resource-constrained devices to submit their local gradient with only bit-level communication loads while participating in federated fine-tuning of LLMs with inference-level memory costs. This approach not only eliminates the high memory requirements needed for LLM fine-tuning but also alleviates the strict synchronization requirements that plague conventional OTA methods. We further formulate a rigorous optimization model to adaptively determine the optimal transmit power and noise levels, ensuring consistent privacy protection regardless of channel conditions. Numerical experiments demonstrate the superiority of pAirZero in enabling secure, efficient LLM fine-tuning over wireless networks, with only 25% peak memory cost on OPT-125M and communication load orders of magnitude lower than conventional methods.

---

## 论文详细总结（自动生成）

# 一石三鸟：利用零阶优化解决无线网络中LLM微调的通信-内存-隐私三难问题 — 论文总结

---

## 一、核心问题与整体含义

- **研究背景**：  
  大型语言模型（LLMs）在自然语言理解与生成方面展现出强大能力，但由于其巨大的参数规模，通常只能在云端数据中心部署；而最有价值的边缘数据（如手机、车辆、IoT设备上的私域数据）难以直接参与训练。  
  联邦学习（Federated Learning，FL）作为分布式协作学习的方案，使得边缘设备可以在不泄露隐私的情况下共同训练模型。

- **核心问题（三重瓶颈）**：  
  1. **通信瓶颈**：LLM梯度维度极大，上传消耗的无线带宽不可承受。  
  2. **内存壁垒**：传统反向传播需要存储中间激活图，内存需求超出边缘设备能力。  
  3. **隐私风险**：梯度反演攻击证明，即使不传原始数据，也能从梯度恢复用户样本。

- **研究动机与意义**：  
  针对上述通信、内存和隐私三难题，论文旨在提出一个统一框架，实现边缘设备低通信、低内存、强隐私保护条件下的安全高效LLM微调。

---

## 二、方法论：pAirZero 框架

### 1. 核心思想

- **方法名称**：pAirZero（privacy-preserving Air-based Zeroth-order optimization）。  
- **理论融合**：将**零阶优化（ZO）**与**空中计算（Over-the-Air, OTA）**结合，构成协同优化框架，使边缘设备通过“空中信号叠加”完成梯度聚合，同时利用无线通道噪声作为天然的隐私保护机制。  

### 2. 关键技术细节

- **零阶优化（ZO）原理**：
  - 不计算显式梯度，而是通过扰动损失函数值差异来近似梯度方向。  
  - 采用**SPSA（Simultaneous Perturbation Stochastic Approximation）**算法，通过随机向量 \( z \) 估计梯度投影：  
    \[
    p_k^{(t)} = \frac{F_k(w+\mu z^{(t)}) - F_k(w-\mu z^{(t)})}{2\mu}
    \]
    只需计算函数值，而非反向传播，极大降低内存消耗。

- **空中计算（OTA）聚合机制**：
  - 各客户端同时发送未经编码的信号，利用无线电波的叠加效果实现梯度平均。  
  - 聚合后信号：  
    \[
    y^{(t)} = \sum_{k=1}^K h_k^{(t)}(\alpha_k^{(t)}(p_k^{(t)} + n_k^{(t)})) + z^{(t)}
    \]
    其中 \( n_k^{(t)} \) 为人工噪声，用于差分隐私。

- **隐私保护与功率优化**：
  - 嵌入式 **差分隐私 (DP)**：在信道层面引入噪声，隐私约束由  
    \[
    P(y|D') \le e^{\epsilon} P(y|D'') + \delta
    \]
    保证。
  - 设计闭式解优化模型，以确定各客户端的最优发射功率 \( c(t) \) 和噪声水平 \( \sigma(t) \)，在保证DP约束下最小化最优性差距（optimality gap）。

- **框架变体**：
  - **pAirZero（模拟通信）**：以信号强度形式发送连续值。  
  - **Sign-pAirZero（数字通信）**：进行一位压缩，只传输梯度投影符号，实现比特级通信。

### 3. 算法流程简述（Algorithm 1）

1. 初始化模型权重 \( w^{(0)} \)。  
2. 每轮 \( t \)：  
   - 客户端根据伪随机种子生成扰动向量，计算投影 \( p_k^{(t)} \)。  
   - 依通信模式（模拟或数字）选择信号形式并发送。  
   - 服务器接收叠加信号 \( y^{(t)} \)，估计平均值并广播。  
   - 客户端更新模型： \( w^{(t+1)} = w^{(t)} - \eta \hat{p}^{(t)} z^{(t)} \)。  
3. 重复至收敛。

---

## 三、实验设计

- **模型与任务**：
  - 使用 **OPT-125M**（一种轻量级公开LLM）。  
  - 两项语言任务：
    1. **SST-2**：情感分类（正/负）。  
    2. **SQuAD**：问答任务，评估上下文理解能力。

- **对比方法**：
  - **Perfect**：理想无噪声聚合。  
  - **Static**：固定功率分配，无自适应优化。  
  - **Reversed**：反向功率变化趋势的对照版本。  
  - 同时与传统 FO-Adam、FO-SGD 等一阶优化做内存与通信对比。

- **优化与隐私参数**：
  - 隐私预算：\( \epsilon = 5, \delta = 0.01 \)。  
  - 总迭代次数：\( T = 8000 \)。  
  - 扰动尺度：\( \mu = 0.001 \)。  
  - 客户端数量：\( K = 5 \)。

- **信道模型**：
  - 无线信道含随机噪声，最大信噪比 \( \text{SNR}_{\max} = P / (dN_0) \)。

---

## 四、资源与算力

- 实验在单台工作站上进行：
  - CPU：AMD EPYC 7742 64核处理器；  
  - GPU：4 × NVIDIA A100-SXM4-80GB。  
- 文中未列出训练时长细节，仅说明每任务均运行了多次独立试验（四次随机种子平均）。

---

## 五、实验数量与充分性

- **主实验**：两项任务（SST-2、SQuAD），分别测试模拟与数字通信模型在不同SNR下性能。  
- **消融实验**：
  - 对比三种功率分配策略（Solution、Static、Reversed）。  
  - 内存与通信开销对比（FO vs ZO）。  
- **参数研究**：
  - 评估了隐含符号反转概率 \( e_0 \)、梯度上界 \( \gamma \)、收缩比 \( A, \tilde{A} \)。  
- **充分性判断**：  
  实验覆盖了核心模块（通信、隐私、功率分配、内存效率）及理论参数验证，实验数量合理且结果一致；但数据集范围仅限两项任务，通用性验证相对有限。

---

## 六、主要结论与发现

- **性能**：pAirZero 与 Sign-pAirZero 在保隐私条件下实现与非隐私基线相近的准确率。  
- **效率**：  
  - 内存成本下降至推理级，仅约传统BP微调的 25%；  
  - 通信负载降至比特级，较传统梯度上传小数个数量级。  
- **隐私保障**：差分隐私嵌入无线传输环节，隐私损失与信道条件解耦，在不同噪声强度下持续保持保护。  
- **收敛性**：在理论上证明线性收敛至最优邻域，实验验证稳定。

---

## 七、优点与亮点

- **统一解决三难问题**：同时优化通信、内存与隐私，而非分步处理。  
- **理论与实践结合紧密**：提供收敛与隐私保证的严格分析（带闭式最优解）。  
- **极高通信压缩**：数字版仅需 1-bit 上传，显著适配带宽受限的无线设备。  
- **隐私设计创新**：将无线信道噪声本身当作DP噪声来源，实现“隐私即物理层”。  
- **设备可行性高**：无需反向传播，边缘设备仅需推理级运算与存储。  

---

## 八、不足与局限

- **实验范围有限**：  
  仅验证于单一轻量级模型和两个标准数据集，未涵盖更大LLM或多领域任务。  
- **通信环境简化**：  
  假设同步性和噪声模型较理想，尚未在真实无线系统中实测通信时延与干扰。  
- **隐私度量依赖理论推导**：  
  差分隐私验证主要基于概率界推导，缺乏实际攻击模拟。  
- **算力消耗描述不足**：  
  未报告训练时间与能耗开销，难以评估资源效率的全貌。  
- **适用性限制**：  
  方法依赖空中叠加信道，难在非无线或低协作度场景应用。

---

（完）
