---
title: "Seeing is Believing: Robust Vision-Guided Cross-Modal Prompt Learning under Label Noise"
title_zh: 眼见为实：在标签噪声下的鲁棒视觉引导跨模态提示学习
authors: "Zibin Geng, Xuefeng Jiang, Jia Li, Zheng Li, Tian Wen, Lvhua Wu, Sheng Sun, Yuwei Wang, Min Liu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09532v1"
tags: ["query:vlm-clip"]
score: 9.0
evidence: 提高了视觉语言模型跨模态提示学习的鲁棒性
tldr: 本文提出VisPrompt框架，以跨模态注意力将视觉语义反向注入文本提示，从而在标签噪声环境下提高视觉语言模型的鲁棒性。通过条件调制机制自适应控制视觉信息注入强度，VisPrompt在保持主干冻结的情况下实现高效稳健的提示学习，并在多数据集上取得领先性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09532v1/fig-001.webp\", \"caption\": \"Figure 1: Prior label-driven promptmethods can bemisled by noisy supervision and produce incorrect predictions, while our method injects reliable image-grounded information to guide prompt learning toward the correct class.\", \"page\": 2, \"index\": 1, \"width\": 494, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2604.09532v1/fig-002.webp\", \"caption\": \"Figure 4: Ablation performance comparison.\", \"page\": 8, \"index\": 2, \"width\": 496, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2604.09532v1/fig-003.webp\", \"caption\": \"Figure 2: The overall architecture of VisPrompt which consists of four steps. Step 2 and Step 3 denote the robust update process.\", \"page\": 4, \"index\": 3, \"width\": 1022, \"height\": 350}]"
motivation: 现有的视觉语言模型在标签噪声下的提示学习鲁棒性较弱，提示容易受到错误标签的干扰。
method: 提出利用跨模态注意力和条件调制机制的视觉引导提示学习框架VisPrompt，在噪声标签条件下强化提示稳定性。
result: VisPrompt在七个基准数据集上的实验表明其在合成和真实标签噪声环境下均显著优于现有方法。
conclusion: VisPrompt有效提升提示学习在噪声标签场景下的鲁棒性，同时保持参数效率。
---

## 摘要
Prompt learning 是一种针对视觉-语言模型的参数高效方法，但其在标签噪声下的鲁棒性研究较少。视觉内容包含更丰富且更可靠的语义信息，在标签噪声下表现出更强的稳健性。然而，prompt 本身对标签噪声高度敏感。基于这一直觉，我们提出 VisPrompt，一个轻量且鲁棒的视觉引导 prompt 学习框架，用于带有噪声标签的环境。具体而言，我们利用跨模态注意力机制，反向将视觉语义注入到 prompt 表示中。这使得 prompt token 能够选择性聚合与当前样本相关的视觉信息，从而通过将 prompt 学习锚定于稳定的实例级视觉证据上来增强鲁棒性，并减少噪声监督的影响。为了解决由于对所有样本采用相同的视觉信息注入方式而导致的不稳定性问题（尽管样本视觉线索质量不同），我们进一步引入了一种轻量的条件调制机制，以自适应控制视觉信息注入的强度，从而在文本语义先验与图像实例证据之间实现更稳健的平衡。所提出的框架能够有效抑制由噪声引起的扰动，减少 prompt 更新过程中的不稳定性，并缓解对错误标注样本的记忆。VisPrompt 在保持预训练 VLM 主干冻结的同时，仅引入少量可训练参数，即可显著提升鲁棒性。大量合成与真实标签噪声条件下的实验表明，VisPrompt 在七个基准数据集上普遍优于现有基线，并取得更强的鲁棒性。我们的代码已公开于：https://github.com/gezbww/Vis_Prompt。

## Abstract
Prompt learning is a parameter-efficient approach for vision-language models, yet its robustness under label noise is less investigated. Visual content contains richer and more reliable semantic information, which remains more robust under label noise. However, the prompt itself is highly susceptible to label noise. Motivated by this intuition, we propose VisPrompt, a lightweight and robust vision-guided prompt learning framework for noisy-label settings. Specifically, we exploit a cross-modal attention mechanism to reversely inject visual semantics into prompt representations. This enables the prompt tokens to selectively aggregate visual information relevant to the current sample, thereby improving robustness by anchoring prompt learning to stable instance-level visual evidence and reducing the influence of noisy supervision. To address the instability caused by using the same way of injecting visual information for all samples, despite differences in the quality of their visual cues, we further introduce a lightweight conditional modulation mechanism to adaptively control the strength of visual information injection, which strikes a more robust balance between text-side semantic priors and image-side instance evidence. The proposed framework effectively suppresses the noise-induced disturbances, reduce instability in prompt updates, and alleviate memorization of mislabeled samples. VisPrompt significantly improves robustness while keeping the pretrained VLM backbone frozen and introducing only a small amount of additional trainable parameters. Extensive experiments under synthetic and real-world label noise demonstrate that VisPrompt generally outperforms existing baselines on seven benchmark datasets and achieves stronger robustness. Our code is publicly available at https://github.com/gezbww/Vis_Prompt.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义  

- **研究背景**：视觉-语言模型（VLMs，如 CLIP）在零样本任务中表现优异，但在下游任务微调中，如果训练数据存在**标签噪声（label noise）**，提示学习（prompt learning）的鲁棒性显著下降。  
- **问题动机**：  
  - Prompt learning 参数高效（冻结骨干，只优化少量 prompt token），但**对错误标签敏感**，噪声梯度易导致提示语义漂移。  
  - 现有鲁棒方法多依赖**标签侧修正**（如置信度估计、样本筛选）而忽视了**图像本身的语义稳定性**。  
- **核心问题**：如何在标签噪声环境下，利用视觉信息作为稳定锚点，引导提示学习过程，减少噪声对表示优化的干扰。

---

## 2. 方法论  

- **核心思想**：  
  以“**实例级图像语义引导提示更新**”为原则，将局部视觉特征通过**跨模态注意力**注入文本提示，再通过**FiLM（Feature-wise Linear Modulation）条件调制机制**自适应控制注入强度，平衡图像证据与文本先验。  
- **关键模块**：  
  1. **Visual Feature Projection**：  
     - 从冻结的视觉编码器提取局部 token 特征，线性投影对齐到文本空间。  
  2. **Cross-modal Visual Prompt Conditioning**：  
     - 以 prompt token 作为查询，对齐的视觉 token 作为键值，经多头注意力检索语义相关的视觉证据，生成跨模态条件特征 \( A_i \)。  
  3. **FiLM-based Robust Modulation**：  
     - 从 \( A_i \) 生成比例因子（scale）、偏置（bias）和 gating（sigmoid 门控），对上下文 token 进行逐特征调制，并通过残差更新控制注入强度。  
  4. **Robust Loss via Optimal Transport (OT)**：  
     - 用 OT 生成样本的伪标签和置信度，区分“可靠子集”与“不可靠子集”，分别用交叉熵（CE）和广义交叉熵（GCE）优化，缓解过拟合噪声样本。  
- **特点**：  
  - 冻结 VLM 主干，额外参数 <1%  
  - 对输入样本自适应调整视觉注入强度，减少统一策略带来的不稳定。

---

## 3. 实验设计  

- **数据集**：  
  - **合成噪声**（对干净数据注入对称/非对称噪声，噪声率 ∈ [12.5%, 75%]）：  
    - EuroSAT、Flowers102、OxfordPets、DTD、UCF101、Caltech101  
  - **真实噪声**：  
    - Food101N（来自网络，弱标注）  
- **对比方法**：  
  - CoOp（基线，文本侧提示）  
  - GCE（基于损失的鲁棒优化）  
  - JoAPR（损失分布建模与样本筛选）  
  - NLPrompt（最优传输划分样本子集）  
- **任务场景**：  
  - 小样本（few-shot）提示学习，其中单类样本量从 1 到 16 不等。  
  - 同时测试不同上下文长度（4/8/16/32 tokens）对鲁棒性的影响。

---

## 4. 资源与算力  

- 明确说明：  
  - 硬件：NVIDIA RTX 4090 GPU  
  - 框架：PyTorch  
- 其它参数：  
  - 训练 200 epoch，使用 SGD + 余弦退火，batch size 16，prompt 长度默认 16，8 头注意力。  
- 未明确：  
  - GPU 数量（可能为单卡）  
  - 精确训练时长

---

## 5. 实验数量与充分性  

- 实验覆盖：  
  - 七个数据集（6 合成 + 1 真实）  
  - 六档噪声率（0.125~0.75）  
  - 两种噪声模式（对称/非对称）  
  - 四个上下文长度测试  
  - 五组 shot 数测试  
  - 主干网络可扩展性实验（6 种 CLIP backbone）  
  - 消融实验（移除视觉引导/FiLM gating）  
- 公平性：  
  - 与对比方法统一骨干、训练设置和超参数  
  - 重复三组随机种子取均值  
- 充分性评价：实验维度全面，含主干泛化、消融分析、敏感性测试，可客观反映结果。

---

## 6. 主要结论与发现  

- VisPrompt 在七个数据集、各种噪声模式和噪声强度下，**普遍优于所有基线**，且在极高噪声（75%）下退化最慢。  
- 在真实噪声 Food101N 上，相比最强基线（NLPrompt）提升 2.74%。  
- 模型对上下文长度和 shot 数敏感性较小，可跨多种视觉主干保持性能。  
- FiLM gating 在高噪声下显著提升鲁棒性，相比纯图像引导（无 gating）在 EuroSAT 上有明显增益。

---

## 7. 优点  

- **创新性**：提出“实例级图像语义锚定”原则，将跨模态注意力与 FiLM 门控结合到提示学习中。  
- **鲁棒性强**：在从中低到极高噪声率的广泛场景中保持领先精度。  
- **参数高效**：仅增加 <1% 可训练参数；FiLM 模块仅占 0.3%。  
- **实验全面**：包括合成噪声、真实噪声、多骨干、多超参、消融和敏感性分析。  
- **可扩展性好**：支持多种 CLIP 架构（ViT、ResNet）。

---

## 8. 不足与局限  

- **任务范围有限**：实验集中在图像分类任务，尚未验证于检测、分割或视频等更复杂任务。  
- **噪声模式覆盖不足**：合成噪声主要为对称/非对称，缺乏实例相关（instance-dependent）或开放集标签噪声的系统验证。  
- **计算开销**：相比单纯文本侧提示，跨模态 FiLM 模块增加了少量计算量，在极度算力受限环境中需权衡。  
- **数据分布广度**：真实噪声测试仅限于 Food101N，现实应用中的多类型、多来源噪声情况未被完全覆盖。

---

（完）
