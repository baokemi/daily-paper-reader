---
title: Probabilistic Feature Imputation and Uncertainty-Aware Multimodal Federated Aggregation
title_zh: 概率特征插补与不确定性感知的多模态联邦聚合
authors: "Nafis Fuad Shahid, Maroof Ahmed, Md Akib Haider, Saidur Rahman Sagor, Aashnan Rahman, Md Azam Hossain"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12970v1"
tags: ["query:visfed"]
score: 9.0
evidence: 具有模态异构性和资源约束的多模态联邦学习
tldr: 本研究针对多模态联邦学习中不同机构因模态缺失导致的性能下降问题，提出P-FIN模型以概率方式插补缺失特征并输出不确定性估计，并结合Fed-UQ-Avg机制在全局聚合时更重视可靠客户端更新，在多个医学影像数据集上显著提升性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12970v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of Stage 2: P-FIN Inference. The gating mechanism g attenuates unreliable dimensions of the imputed feature vector µ based on uncertainty σ2 before attention-guided fusion and classification.\", \"page\": 6, \"index\": 1, \"width\": 904, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.12970v1/fig-002.webp\", \"caption\": \"Figure 5: Qualitative Examples: Top row shows low-uncertainty cases with clear imaging; bottom row shows high-uncertainty cases with complex or ambiguous presentations.\", \"page\": 11, \"index\": 2, \"width\": 867, \"height\": 627}, {\"url\": \"assets/figures/arxiv/2604.12970v1/fig-003.webp\", \"caption\": \"Figure 4: Uncertainty calibration analysis. (a) Reliability diagram showing dependable calibration with ECE = 0.0422. (b) Binned analysis demonstrating strong correlation between predicted uncertainty and reconstruction error.\", \"page\": 10, \"index\": 3, \"width\": 868, \"height\": 451}, {\"url\": \"assets/figures/arxiv/2604.12970v1/fig-004.webp\", \"caption\": \"Table 1: Test AUC (%) across federated configurations with varying unimodal (I) to multimodal (M) client ratios. Best results in bold.\", \"page\": 9, \"index\": 4, \"width\": 798, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2604.12970v1/fig-005.webp\", \"caption\": \"Figure 3: (Left) Evolution of uncertainty estimates for all clients (0–9). Unimodal clients (0–7) are shown in blue, while multimodal clients (8–9) are in orange. (Right) AUC progression per round.\", \"page\": 9, \"index\": 5, \"width\": 896, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2604.12970v1/fig-006.webp\", \"caption\": \"Figure 1: Overview of Stage 1: P-FIN Training. The architecture leverages a Transformer encoder to map image features to text embedding distributions, trained via βNLL loss for calibrated uncertainty.\", \"page\": 5, \"index\": 6, \"width\": 904, \"height\": 429}]"
motivation: 现有方法无法为缺失模态的插补特征提供可靠性评估，导致在医学场景中存在潜在安全风险。
method: 提出概率特征插补网络（P-FIN）和不确定性感知的联邦聚合策略Fed-UQ-Avg，用于处理多模态联邦学习中的模态缺失问题。
result: "在多个胸部X光数据集上实验表明，该方法相比确定性基线实现了最高5.36%的AUC提升。"
conclusion: 该方法提升了联邦多模态学习中缺失模态处理的可靠性和整体模型性能。
---

## 摘要
多模态联邦学习能够在医疗机构间实现隐私保护的协作式模型训练。然而，一个根本性的挑战来自模态异质性：许多临床机构由于资源限制或工作流程差异，仅拥有部分模态。现有方法通常通过特征插补网络来合成缺失模态的表征，但这些方法仅生成点估计而不提供可靠性度量，迫使下游分类器将所有插补特征视为同等可信。在安全关键的医疗应用中，这一限制带来显著风险。我们提出了一种概率特征插补网络（Probabilistic Feature Imputation Network，P-FIN），能够在生成插补特征的同时输出经过校准的不确定性估计。该不确定性在两个层面上被利用：（1）局部层面，通过 sigmoid 门控在分类前抑制不可靠的特征维度；（2）全局层面，通过 Fed-UQ-Avg 聚合策略优先整合来自插补可靠客户端的更新。在基于 CheXpert、NIH Open-I 和 PadChest 的联邦胸部 X 光分类实验中，我们的方法在各项配置下均优于确定性基线方法，在最具挑战的配置中实现了 5.36% 的 AUC 提升。

## Abstract
Multimodal federated learning enables privacy-preserving collaborative model training across healthcare institutions. However, a fundamental challenge arises from modality heterogeneity: many clinical sites possess only a subset of modalities due to resource constraints or workflow variations. Existing approaches address this through feature imputation networks that synthesize missing modality representations, yet these methods produce point estimates without reliability measures, forcing downstream classifiers to treat all imputed features as equally trustworthy. In safety-critical medical applications, this limitation poses significant risks. We propose the Probabilistic Feature Imputation Network (P-FIN), which outputs calibrated uncertainty estimates alongside imputed features. This uncertainty is leveraged at two levels: (1) locally, through sigmoid gating that attenuates unreliable feature dimensions before classification, and (2) globally, through Fed-UQ-Avg, an aggregation strategy that prioritizes updates from clients with reliable imputation. Experiments on federated chest X-ray classification using CheXpert, NIH Open-I, and PadChest demonstrate consistent improvements over deterministic baselines, with +5.36% AUC gain in the most challenging configuration.

---

## 论文详细总结（自动生成）

# 概率特征插补与不确定性感知的多模态联邦聚合（P-FIN + Fed-UQ-Avg）论文总结

---

## 1. 研究问题与背景

- **研究动机**：  
  医疗领域的多模态学习（如 X 光图像与放射报告结合）已被证明能显著提高诊断准确性。但现实场景中，不同医疗机构可获取的数据模态往往不全（例如小型诊所仅有影像而无文本报告），形成 **模态异质性（modality heterogeneity）** 。  
  传统特征插补网络虽可合成缺失模态，但多为**确定性方法**，输出点估计而无可靠性度量。这导致下游模型无法区分“可信的插补”与“可能错误的猜测”，在安全敏感的医疗场景中潜藏风险。

- **研究目标**：  
  该研究旨在解决 **联邦多模态学习中模态缺失且缺乏不确定性量化** 的问题，使模型既能生成缺失模态的合理估计，又能识别自身的不确定性，从而提高医疗 AI 的可解释性与安全性。

---

## 2. 方法论：核心思想与关键技术

### 2.1 总体框架

提出两大核心创新：

1. **P-FIN（Probabilistic Feature Imputation Network）**：  
   以概率建模方式插补缺失模态特征，并输出对应的不确定性估计（均值 μ 与方差 σ²）。
2. **Fed-UQ-Avg（Uncertainty-Aware Federated Averaging）**：  
   在联邦聚合阶段，基于每个客户端的平均不确定性动态调整其更新权重，使高质量、低不确定性的客户端在全局更新中占更大权重。

### 2.2 模型细节

- **特征编码器**：  
  - 图像编码器：ResNet-50（预训练于 ImageNet），投射为 256 维、经 L2 归一化。  
  - 文本编码器：BERT-base 提取 [CLS] 向量，同样投射至 256 维空间。

- **概率特征插补模块（P-FIN）**：  
  - 从图像特征 \( z_I \) 预测缺失文本特征分布 \( p(z_T|z_I) = \mathcal{N}(\mu, \sigma^2) \)。  
  - 架构使用 2 层 Transformer 编码器（含可学习 query token），最后通过双输出头分别预测 μ 和 σ²。  
  - 训练损失为 **β-NLL**，通过 stop-gradient 技术防止方差无意义增大，确保不确定性校准。

- **局部不确定性门控与融合**：  
  - **Sigmoid 门控**：\( g = \text{sigmoid}(-\log \sigma^2) \)，用于抑制高不确定特征维度。
  - **跨模态注意力融合**：采用双向多头注意力在视觉与插补特征间交互，使可靠特征在融合时被动态放大。
  - 最终通过线性层生成融合特征 \( z_{\text{fused}} \) 用于分类。

- **全局聚合策略（Fed-UQ-Avg）**：  
  聚合权重设为数据量权重与置信度权重的加权和：  
  \[
  W_k = (1 - \alpha) w_{\text{data}} + \alpha w_{\text{conf}}
  \]
  其中：
  \[
  w_{\text{conf}} \propto e^{-\bar{\sigma}^2 / T}
  \]
  客户端平均不确定性越低，其权重越高（α = 0.6, T = 0.2）。

---

## 3. 实验设计

- **数据集**：
  1. **CheXpert**：22 万张胸部 X 光，用于主要训练（单模态客户端）。  
  2. **NIH Open-I**：7,470 张图像与配对报告（多模态客户端）。  
  3. **PadChest**：16 万张图像，用于外部泛化验证。

- **联邦设定**：
  - 客户端数量：10 个。  
  - 模态分布比：分别测试 8:2、6:4、4:6（单模态：多模态）。  
  - 数据异质性由 Dirichlet(α=0.5) 模拟。

- **对比基线**：
  1. Zero-filling（填零）  
  2. Uniform-filling（填均值）  
  3. FIN + FedAvg（确定性插补 + 标准聚合）  
  4. P-FIN + FedAvg（仅使用概率插补，无UQ聚合）  
  5. Ours: P-FIN + Fed-UQ-Avg  

- **训练配置**：  
  20 轮通讯，每轮 4 个本地 epoch，batch size 32，Adam 优化器，学习率 1e−4。

---

## 4. 资源与算力说明

- 文中**未明确提及硬件或算力资源**（如 GPU 型号、数量或训练耗时）。  
  推测实验在单机或多卡 GPU 上完成，但具体配置未说明。

---

## 5. 实验数量与充分性

- **主要实验**：  
  - 3 种模态比例设置（8:2、6:4、4:6）。  
  - 5 种方法对比（含两个消融）。  
  - 所有实验重复并报告平均 AUC ± 标准差。
- **补充实验**：  
  - 不确定性校准分析（ECE = 0.0422）。  
  - 不确定性与误差正相关性验证。  
  - 收敛性曲线与不确定性演化图（Figure 3）。
- **充分性评估**：  
  覆盖了数据异质性、方法消融、可靠性校准与外部验证，实验总体较为全面且指标一致，具有较高客观性。

---

## 6. 主要结论与发现

- **性能提升显著**：  
  在最困难的 8:2 配置中（80% 客户端无文本），AUC 较确定性插补基线提升 **+5.36%**。  
- **不确定性量化有效**：  
  预测方差与重构误差高度相关，校准误差低（ECE=0.0422）。  
- **全局聚合更稳健**：  
  Fed-UQ-Avg 能动态抑制高不确定客户端的干扰，提升整体模型收敛与泛化。

---

## 7. 方法优点与创新亮点

- **首次将不确定性建模引入联邦多模态插补场景**，提升医学 AI 可解释性。  
- **双层机制**（局部 gating + 全局聚合）相辅相成，兼顾个体与系统层面的可靠性控制。  
- **β-NLL 训练策略** 有效防止方差塌陷或爆发，使预测分布更可解释。  
- 设计实验广泛，包括模态比例变化、消融、校准与外部测试，证明方法稳健。  
- 与 FDA 等监管要求对齐，有助于临床可信部署。

---

## 8. 不足与局限

- **未报告算力与运行资源**，可重复性受限。  
- **仅实现单向（图像→文本）插补**，未探索双向或多模态间更复杂关系。  
- **仅估计观测不确定性（aleatoric）**，未包含 epistemic 不确定性（模型层）。  
- **医学专属任务场景**，可扩展性至通用多模态联邦学习仍需验证。  
- **聚合参数α、T 固定**，未自适应调节，灵敏度分析有限。

---

**（完）**
