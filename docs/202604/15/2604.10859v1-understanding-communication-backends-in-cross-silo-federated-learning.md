---
title: Understanding Communication Backends in Cross-Silo Federated Learning
title_zh: 跨机构联邦学习中通信后端的理解
authors: "Amir Ziashahabi, Chaoyang He, Salman Avestimehr"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10859v1"
tags: ["query:flrlvldm"]
score: 9.0
evidence: 跨孤岛联邦学习中通信后端的基准测试
tldr: 本研究针对跨机构联邦学习中的通信后端进行系统评估，比较了MPI、gRPC和PyTorch RPC等技术，并提出混合方案gRPC+S3，以应对远程分布和大型模型的传输瓶颈，实现最高3.8倍的性能提升，为通信后端选择提供实用指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10859v1/fig-001.webp\", \"caption\": \"Fig. 1: Three common deployment environments for federated learning: (1) LAN: participants in a single building; (2) GeoProximal: participants in close geographical proximity; (3) Geo-Distributed: participants in distant locations. Contextaware selection of communication backends is crucial for optimizing performance in each setting.\", \"page\": 1, \"index\": 1, \"width\": 527, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.10859v1/fig-002.webp\", \"caption\": \"Fig. 3: Architecture of the proposed gRPC+S3 backend.\", \"page\": 3, \"index\": 2, \"width\": 529, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.10859v1/fig-003.webp\", \"caption\": \"Fig. 5: Per-state duration in end-to-end LAN experiments (communication, CPU-GPU migration, serialization, waiting; plus training on clients and aggregation on server).\", \"page\": 5, \"index\": 3, \"width\": 959, \"height\": 312}]"
motivation: 联邦学习应用在网络配置和模型规模上的多样性使得通信后端的选择对性能至关重要。
method: 通过对多种通信后端进行详细基准测试并设计混合方案gRPC+S3开展实验。
result: 提出的gRPC+S3方案在跨地域大模型传输中实现最高3.8倍端到端速度提升。
conclusion: 研究为不同网络环境下联邦学习通信后端的选择和配置提供了实用参考。
---

## 摘要
联邦学习（Federated Learning，FL）已成为一种实现隐私保护分布式机器学习的实用方法。FL 的灵活设计使其适用于多种训练环境，从跨设备 FL 中的物联网边缘设备到跨机构 FL 中的高性能服务器。这种灵活性带来的一个重要结果是 FL 应用在网络配置上的高度多样性。再加上对大型模型（如大语言模型）日益增长的需求，合理选择和配置通信后端对于确保 FL 系统的最佳性能变得至关重要。本研究聚焦于跨机构联邦学习（cross-silo federated learning），对包括 MPI、gRPC 和 PyTorch RPC 在内的多种通信后端进行了深入的基准测试。此外，我们提出了一种混合后端 gRPC+S3，旨在克服现有方法的局限，特别是在地理分布式部署中传输大型模型的场景下，实现了相较于 gRPC 高达 3.8 倍的端到端加速。我们的基准测试评估了在现实网络条件下、不同模型规模下的点对点和端到端性能。研究结果为根据特定的联邦学习任务和网络配置选择和调优合适的通信后端提供了实用见解。

## Abstract
Federated learning (FL) has emerged as a practical means for privacy-preserving distributed machine learning. FL's versatile design makes it suitable for various training settings, from IoT edge devices in cross-device FL to powerful servers in cross-silo FL. A key consequence of this versatility is the high level of diversity found in the networking configuration of FL applications. Coupled with the rising demand for large-scale models such as large language models, well-informed selection and configuration of communication backends become crucial for ensuring optimal performance in FL systems. This work focuses on cross-silo federated learning, presenting in-depth benchmarks of various communication backends, including MPI, gRPC, and PyTorch RPC. In addition, we introduce gRPC+S3, a hybrid backend designed to overcome the limitations of existing approaches, particularly for transmitting large models across geo-distributed deployments, achieving up to $3.8\times$ end-to-end speedup over gRPC. Our benchmarks examine point-to-point and end-to-end performance for a broad range of model sizes running under realistic network conditions. Our findings provide practical insights for selecting and configuring suitable communication backends tailored to the specific federated learning tasks and network configurations.

---

## 论文详细总结（自动生成）

# 跨机构联邦学习中通信后端的理解 — 论文详细总结

---

## 一、研究核心问题与背景

- **研究动机：**  
  随着联邦学习（Federated Learning, FL）的广泛应用，不同应用场景（如跨设备与跨机构，cross-device vs. cross-silo）对通信系统的需求差异巨大。跨机构 FL 通常涉及多个组织（如医院、银行或数据中心）之间的协作训练，在确保隐私的前提下同步模型更新。  
  然而，**通信后端（communication backend）** 的性能是影响整体训练效率的关键因素，尤其在参与方地理分散、模型规模庞大的情境下，更容易受到网络延迟、带宽限制等因素制约。

- **问题陈述：**  
  现有 FL 系统多依赖通用通信框架（如 gRPC），但缺乏系统化的性能评估与优化指南。论文旨在回答：  
  > “在不同网络环境与模型规模下，哪类通信后端最适合跨机构联邦学习？”

- **目标与意义：**  
  1. 系统化评估主流通信后端在多网络情境下的性能。  
  2. 提出改进设计以提升跨地域通信效率。  
  3. 为构建高效、可扩展、安全的跨机构 FL 系统提供实证依据。

---

## 二、方法论概述

### 1. 总体思路

论文建立了一个**系统化基准测试框架**，比较 MPI、gRPC、PyTorch RPC 三类主流通信后端的性能特征，并提出一种新型混合方案——**gRPC+S3**，以应对 gRPC 在大模型分布式传输中的性能瓶颈。

### 2. gRPC+S3 的核心架构

- **核心思想：**  
  将通信负载拆分为两种路径：
  - 小规模元数据（metadata）通过 gRPC 进行 RPC 通信；
  - 大规模模型参数（payload）通过 Amazon S3 进行对象存储传输。

- **关键机制：**
  - **发送端：**  
    1. 分离元数据与模型参数。  
    2. 模型参数序列化后上传至 S3，生成对象 key。  
    3. 通过 gRPC 发送元数据及该 key 给接收端。  
  - **接收端：**  
    1. 解析收到的 key。  
    2. 向 S3 异步下载模型参数。  
    3. 重新组合元数据与参数以恢复完整消息。

- **优势结构：**
  - RPC 通道保持轻量，降低 CPU 占用与拷贝开销。
  - 使用对象存储实现“一次上传、多端并行下载”，提升带宽利用率。
  - 独立的连接与缓存机制显著提升容错性与并发性能。

- **安全性与可靠性：**
  - gRPC 与 S3 均支持加密传输（TLS/HTTPS）。
  - 访问控制可通过限时凭据或预签名 URL 管理。

---

## 三、实验设计

### 1. 实验环境与网络场景

论文定义了三类典型跨机构部署：

| 场景类型 | 说明 | 实验平台 |
|-----------|------|----------|
| LAN（局域网） | 单数据中心内低延迟高带宽 | 本地 GPU 服务器 |
| Geo-Proximal（地理邻近） | 同一区域或城市内多个站点 | AWS EC2 北加州多可用区 |
| Geo-Distributed（跨洲分布） | 各大洲协作，高延迟广域网 | AWS EC2 跨七区域（北加州、弗吉尼亚、香港等） |

### 2. 模型与数据集

四个模型规模层级：
| 层级 | 模型 | 参数数量 | 模型大小 |
|------|------|----------|----------|
| Small | ResNet56 | 59 万 | 2.39 MB |
| Medium | MobileNetV3 | 515 万 | 19.85 MB |
| Big | DistilBERT | 6636 万 | 253 MB |
| Large | ViT-Large | 3.07 亿 | 1.24 GB |

对应数据集：
- **GLD-23K**（图像任务）
- **20 Newsgroups**（文本任务）

### 3. 对比方法与基准

- **主要通信后端：** MPI (两种实现)、gRPC、PyTorch RPC、gRPC+S3。  
- **实验种类：**
  - **点对点传输**（peer-to-peer）：单消息延迟、并发传输、内存使用。  
  - **端到端训练**（end-to-end）：集成至 FedML 框架，测量完整联邦训练过程中的时间占比（通信、训练、同步等）。

### 4. 评估指标

- CPU-to-CPU 传输延迟  
- 并发发送带来的加速比  
- 峰值内存占用  
- 整体训练轮（round）执行时间分布（通信、同步、等待时间）

---

## 四、计算资源与算力

- **硬件环境：**
  - LAN 实验：使用两台各含 8 × NVIDIA Quadro RTX 5000 GPU 的服务器。  
  - 云端实验：AWS EC2 *g4dn.2xlarge* 实例。  

- **说明：**
  - 文中未提供总训练时长。  
  - 未提及 GPU 使用率或能耗分析。  
  - 实验聚焦通信部分，计算资源配置主要用于复现真实联邦训练环境。

---

## 五、实验数量与充分性

- **实验维度丰富：**
  1. 3 种网络环境 × 4 种模型规模 × 5 种通信后端。  
  2. 包括单点与端到端评测两个层级。  
  3. 并行度（并发消息数）影响的量化分析。
- **公平性：**
  - 各后端均在最佳配置下运行；
  - gRPC+S3 在 LAN 情境下不参与对比（因会受外部存储延迟影响，避免不公平）。
- **结论判断：**
  - 实验覆盖充分，测试维度全面，结论具有较高客观性与实用性。

---

## 六、主要结论与发现

1. **网络环境决定最优后端选择：**
   - **低延迟环境（LAN/Geo-Proximal）**：MPI_MEM_BUFF 与 PyTorch RPC 最快，因其内存缓冲机制避免序列化开销。  
   - **高延迟环境（Geo-Distributed）**：多连接吞吐效率主导性能，PyTorch RPC 与 MPI 表现优异。

2. **gRPC 的局限被量化：**
   - 单连接 HTTP/2 通道使大模型传输性能下降明显；
   - 并发连接虽能提升带宽利用率，但会线性增加内存开销。

3. **gRPC+S3 的性能突破：**
   - 在跨洲高延迟环境下，大模型（>10MB）端到端性能改进达 **3.5–3.8×** 相对 gRPC；
   - 内存占用在多客户端广播中保持常量级增长，具显著可扩展性。

4. **现实部署指导：**
   - 在可信 LAN 内建议使用 MPI 或 PyTorch RPC；  
   - 在不可信、高延迟环境（如不同机构间）可使用 gRPC+S3，并为小模型自动回退到 gRPC。

---

## 七、方法与实验亮点

- **系统性评测框架：** 首次系统探讨不同通信后端在跨机构 FL 情境下的表现。
- **混合通信设计创新：**  
  gRPC+S3 结合对象存储与 RPC 的优点，在大模型分发中兼顾安全性与吞吐效率。
- **现实化实验设计：**  
  模拟多地 AWS 实例网络特征，结果具有较强工程指导意义。
- **内存与带宽分析细化：**  
  显示并发广播对各后端内存管理的影响机制，为部署优化提供依据。

---

## 八、局限与不足

- **实验环境限制：**
  - 使用 AWS 模拟跨地理网络，未验证在其他云和广域企业网下的泛化。  
- **算法层未融合：**
  - 研究仅关注通信系统层，不涉及算法级通信压缩（如量化、稀疏化）方法。
- **算力与能耗未分析：**
  - 未提供详细 GPU 使用与功耗开销。  
- **安全与隐私机制仅做简述：**
  - gRPC+S3 虽支持加密传输，但未评价在数据合规、访问控制上的风险。  

---

**（完）**
