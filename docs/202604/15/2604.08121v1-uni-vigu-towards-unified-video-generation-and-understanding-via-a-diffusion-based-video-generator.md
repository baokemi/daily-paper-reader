---
title: "Uni-ViGU: Towards Unified Video Generation and Understanding via A Diffusion-Based Video Generator"
title_zh: Uni-ViGU：通过基于扩散的视频生成器实现统一的视频生成与理解
authors: "Luozheng Qin, Jia Gong, Qian Qiao, Tianjiao Li, Li Xu, Haoyu Pan, Chao Qu, Zhiyu Tan, Hao Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08121v1"
tags: ["query:flrlvldm"]
score: 9.0
evidence: 通过扩散模型统一视频生成与理解
tldr: Uni-ViGU提出一种以视频生成为核心的统一框架，通过扩展视频生成器来同时实现视频理解与生成。它利用统一流匹配方法和模态驱动的MoE结构，实现文本与视频的协同生成和理解，并通过双向训练机制提升跨模态表现，在多项实验中取得优异性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08121v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of Uni-ViGU framework. We formulate unified multimodal generation via a uniflow process, where video generation is modeled with continuous flow matching in the latent space, while text generation is modeled with discrete flow matching over token embeddings. Both modalities are jointly learned within a single Transformer backbone: self- and cross-attention layers are shared to enable cross-modal interaction and alignment, whereas modality-specific FFN branches are employed to capture domain-specific knowledge for video and text, respectively. This design allows the model to unify generation and understanding while preserving modality-specialized capacity.\", \"page\": 4, \"index\": 1, \"width\": 820, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.08121v1/fig-002.webp\", \"caption\": \"Figure 3: Qualitative Results on Video-Text Joint Generation\", \"page\": 8, \"index\": 2, \"width\": 824, \"height\": 197}]"
motivation: 现有多模态模型在视频生成上计算开销高，限制了统一理解与生成的融合。
method: 采用统一流匹配和模态驱动MoE结构，并设计双阶段双向训练机制，实现视频与文本的统一生成和理解。
result: Uni-ViGU在视频生成和理解任务上均取得了竞争性表现。
conclusion: 研究表明，以生成为中心的架构是实现多模态统一智能的可扩展方向。
---

## 摘要
统一的多模态模型在整合视觉理解与生成方面面临一个根本挑战：视觉生成的计算成本远高于理解，尤其是在视频领域。这种不平衡促使我们颠倒传统范式：我们提出 Uni-ViGU，这一框架通过扩展视频生成器作为基础，将视频生成与理解统一起来，而不是将以理解为中心的多模态大型语言模型（MLLMs）扩展至支持生成。我们引入了一种统一的流方法，在单一过程内对视频执行连续流匹配、对文本执行离散流匹配，从而实现一致的多模态生成。我们进一步提出了一种基于模态驱动的 MoE 框架，为 Transformer 模块添加轻量层以支持文本生成，同时保留生成先验。为将生成知识重新用于理解任务，我们设计了双向训练机制，分为两个阶段：‘知识召回’阶段通过重构输入提示以利用学习到的文本-视频对应关系，而‘能力优化’阶段则在详细描述上进行微调，以建立具备区分性的共享表征。实验表明，Uni-ViGU 在视频生成与理解任务上均取得了有竞争力的性能，验证了以生成为中心的架构是实现统一多模态智能的可扩展路径。项目主页与代码：https://fr0zencrane.github.io/uni-vigu-page/。

## Abstract
Unified multimodal models integrating visual understanding and generation face a fundamental challenge: visual generation incurs substantially higher computational costs than understanding, particularly for video. This imbalance motivates us to invert the conventional paradigm: rather than extending understanding-centric MLLMs to support generation, we propose Uni-ViGU, a framework that unifies video generation and understanding by extending a video generator as the foundation. We introduce a unified flow method that performs continuous flow matching for video and discrete flow matching for text within a single process, enabling coherent multimodal generation. We further propose a modality-driven MoE-based framework that augments Transformer blocks with lightweight layers for text generation while preserving generative priors. To repurpose generation knowledge for understanding, we design a bidirectional training mechanism with two stages: Knowledge Recall reconstructs input prompts to leverage learned text-video correspondences, while Capability Refinement fine-tunes on detailed captions to establish discriminative shared representations. Experiments demonstrate that Uni-ViGU achieves competitive performance on both video generation and understanding, validating generation-centric architectures as a scalable path toward unified multimodal intelligence. Project Page and Code: https://fr0zencrane.github.io/uni-vigu-page/.

---

## 论文详细总结（自动生成）

# Uni-ViGU：通过基于扩散的视频生成器实现统一的视频生成与理解  

---

## 一、研究背景与问题动机  

- **核心问题**：现有多模态模型（尤其是视觉语言模型，MLLMs）尝试将视觉理解与生成统一，但在视频任务上存在显著计算不平衡。  
  - 视频生成需要数百万级的迭代去噪和大量 token 处理，远高于视觉理解的代价。  
  - 现有方法多以「理解为中心」的架构扩展至生成任务，导致生成质量有限、训练效率低。  

- **研究动机**：作者反其道而行之，提出以「视频生成为核心」的统一框架。  
  - 模拟人类认知过程——感知（生成）先于语言表达（理解）；  
  - 生成模型天生具有丰富的视觉先验，可成为统一多模态智能的基础。  

---

## 二、方法论概述  

### 1. 总体思路  
- **核心思想**：以现有高效视频生成器（Wan2.1）为基础，将其扩展为同时支持视频生成与理解的统一模型。  
- 主张“**以生成为中心的统一智能**”，利用扩散模型的强视觉表征能力反向迁移至理解任务。  

### 2. 关键组成模块  

#### （1）统一流匹配（Unified Flow Matching, Uni-Flow）  
- 同时在单一过程内建模视频与文本：  
  - 视频：**连续流匹配（Continuous Flow Matching）**——在 VAE 潜空间中线性插值噪声与真实视频潜向量；  
  - 文本：**离散流匹配（Discrete Flow Matching）**——在 token 嵌入空间内拟合噪声到词向量的变换。  
- 两个过程独立采样时间步 τv, τt，实现视频与文本在不同噪声水平下的耦合学习。  
- **统一优化目标公式**：  
  \[
  L_{UFM} = E[\lambda_v ||\hat{v}_v - u_v||^2 + \lambda_t ||\hat{v}_t - u_t||^2]
  \]
  其中 λv, λt 根据 token 数进行归一化，确保视频与文本训练权重均衡。  

#### （2）模态驱动的混合专家结构（Modality-driven MoE Architecture）  
- 在每个 Transformer 块中：  
  - **自注意力（Self-Attn）与交叉注意力（Cross-Attn）**层共享，用于跨模态特征对齐；  
  - **前馈层（FFN）**分为视频专家与文本专家两支，保存生成先验同时容纳文本生成能力。  
- 不使用动态门控，而是显式按模态路由，减少计算复杂度并保持特征专化。

#### （3）双向训练机制（Bidirectional Training）  
分两个阶段：  
1. **知识召回阶段（Knowledge Recall）**  
   - 目标文字与输入提示相同，通过“条件丢弃”防止模型抄答案。  
   - 强化视频→文字的反向映射能力。  
2. **能力优化阶段（Capability Refinement）**  
   - 使用详细视频描述进行微调，促使模型发展更细粒度的理解能力。  
   - 模型需自发关注视频细节完成文字生成。

---

## 三、实验设计  

### 1. 数据集与场景  
- 自建视频-文本对齐数据，分两阶段：  
  - 阶段一：约 **10K 视频与提示语对**；  
  - 阶段二：新增 **10K 视频 + 简要提示 + 详细描述三元组**。  
- 视频由先进生成模型合成，详细描述由大型语言模型生成，确保内容准确与丰富。  

### 2. 对比与基线  
- 与近期多模态统一模型对比，如 Janus [1]、Show-o [2]、Omni-Diffusion [45] 等。  
- 关注**视频生成质量**与**视频理解（描述）准确性**两方面性能。  

---

## 四、训练资源与算力  

- **硬件**：使用 **16 枚 H800 GPU**；  
- **训练时间**：约一周；  
- **优化器**：Adam，学习率分阶段（2×10⁻⁴ → 5×10⁻⁵）；  
- **总训练步数**：Stage1 约 40K steps，Stage2 约 60K steps。  
- 作者明确报告了资源规模，具备高可重复性。  

---

## 五、实验数量与充分性  

- 包含两阶段联合训练、联合生成和视频理解实验。  
- 总计约 20K 视频样本；  
- 展示多组 **定性与定量结果**（图 3 展示联合生成样例）。  
- 对比多个现有框架，且讨论了参数效率与性能，实验广度合理。  
- 未详述消融实验细节，但推理过程与架构设计逻辑自洽。  
→ 整体实验充分度中等偏高，结果具客观性。  

---

## 六、主要结论与发现  

- **核心结论**：以视频生成器为统一框架的“生成中心”模型，能有效整合视频理解与生成。  
- **重要发现**：  
  - 连续与离散流匹配的统一过程可同时在视觉和文本空间中兼顾。  
  - 共享注意力 + 分离 FFN 的结构提升跨模态泛化。  
  - 双向训练实现了从“生成知识”到“理解能力”的迁移。  

---

## 七、方法优点  

- **创新范式**：颠转传统理解驱动的设计，以生成为中心；  
- **技术新颖**：首次在视频与文本间实现统一流匹配；  
- **结构高效**：共享注意力、轻量 FFN 分支保持低参数增长；  
- **训练机制合理**：逐步迁移生成知识至理解任务，具备认知启发意义；  
- **资源性价比高**：仅一周训练即可达主流性能。  

---

## 八、不足与局限  

- **数据来源有限**：主要基于合成视频及LLM生成文字，可能存在分布偏差；  
- **实验范围**：未涉及多样真实数据集（如自然视频或跨语言场景）；  
- **消融分析不足**：缺乏对流匹配设计与MoE分支效果的独立验证；  
- **推理复杂度**：双模态流匹配在长视频推理中仍存在算力瓶颈。  
- **泛化性验证待加强**：未对下游视频问答或开放场景理解进行评估。  

---

**（完）**
