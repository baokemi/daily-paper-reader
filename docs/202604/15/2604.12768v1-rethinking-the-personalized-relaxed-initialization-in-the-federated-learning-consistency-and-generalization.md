---
title: "Rethinking the Personalized Relaxed Initialization in the Federated Learning: Consistency and Generalization"
title_zh: 重新思考联邦学习中的个性化松弛初始化：一致性与泛化
authors: "Li Shen, Yan Sun, Dacheng Tao"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12768v1"
tags: ["query:flrlvldm"]
score: 9.0
evidence: 解决客户端漂移和初始化问题的联邦学习算法
tldr: 本文针对联邦学习中由客户端最优不一致引起的漂移问题，提出FedInit算法，通过阶段性个性化放松初始化策略缓解客户端漂移。理论分析揭示其主要影响泛化误差而非优化误差，实验验证了该方法在无额外开销下具有优良的泛化性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12768v1/fig-001.webp\", \"caption\": \"Table 4: Test accuracy on the CIFAR-10 dataset. We test two active ratios on each dataset. On each setup, we test two Dirichlet splittings, and each result tests 3 times. The table reports results on ResNet-18-GN (upper) and VGG-11 (lower).\", \"page\": 15, \"index\": 1, \"width\": 636, \"height\": 539}, {\"url\": \"assets/figures/arxiv/2604.12768v1/fig-002.webp\", \"caption\": \"Table 10: Some abbreviations of the used terms in the proof of the optimization process.\", \"page\": 21, \"index\": 2, \"width\": 946, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2604.12768v1/fig-003.webp\", \"caption\": \"Table 6: We incorporate the relaxed initialization (RI) into the different benchmarks to test their improvements on the ResNet-18-GN model on the CIFAR-10 dataset under the same settings and hyperparameters selections.\", \"page\": 18, \"index\": 3, \"width\": 772, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2604.12768v1/fig-004.webp\", \"caption\": \"Table 2: Summary of the convergence bound of our proposed FedInit method.\", \"page\": 8, \"index\": 4, \"width\": 906, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.12768v1/fig-005.webp\", \"caption\": \"Table 1: Notation tables.\", \"page\": 4, \"index\": 5, \"width\": 664, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2604.12768v1/fig-006.webp\", \"caption\": \"Table 8: Acceleration ratio of the communication rounds and wall-clock time of the ResNet18-GN model on CIFAR-10 dataset.\", \"page\": 19, \"index\": 6, \"width\": 564, \"height\": 545}, {\"url\": \"assets/figures/arxiv/2604.12768v1/fig-007.webp\", \"caption\": \"Figure 2: The accuracy and divergence of different β. (a) and (b) are tested with FedAvg + RI (FedInit), while (c) and (d) for SCAFFOLD + RI. (a) and (c) are tested on the Dir-0.6 10%-100 setups, while (b) and (d) for Dir-0.1 5%-200 setups.\", \"page\": 17, \"index\": 7, \"width\": 881, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2604.12768v1/fig-008.webp\", \"caption\": \"Table 9: Communication and storage costs.\", \"page\": 20, \"index\": 8, \"width\": 631, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2604.12768v1/fig-009.webp\", \"caption\": \"Table 5: Test accuracy on the CIFAR-10/100 dataset. We test two active ratios on each dataset. On each setup, we test two Dirichlet splittings, and each result tests 3 times. The table reports results on ResNet-18-GN (upper) and VGG-11 (lower).\", \"page\": 16, \"index\": 9, \"width\": 636, \"height\": 539}, {\"url\": \"assets/figures/arxiv/2604.12768v1/fig-010.webp\", \"caption\": \"Figure 1: Sensitivity studies of local intervals K and relaxed coefficient β of the FedInit method on CIFAR-10. To fairly compare their efficiency.\", \"page\": 16, \"index\": 10, \"width\": 796, \"height\": 334}]"
motivation: 现有联邦学习因各客户端最优点不一致导致客户端漂移问题，缺乏理论解释和有效缓解方法。
method: 提出FedInit算法，通过个性化放松初始化使每轮本地模型在全局和上次本地状态间调整初始值。
result: 实验表明FedInit在不增加训练或通信成本下，性能与多种先进方法相当或更优。
conclusion: FedInit有效缓解了客户端漂移问题，提升了联邦学习的泛化性能且无需额外开销。
---

## 摘要
联邦学习（Federated Learning，FL）是一种分布式范式，通过对分布异质的数据集进行分阶段的本地训练，协调大量本地客户端协作训练一个全局模型。以往的研究隐含地指出，FL 存在“客户端漂移（client-drift）”问题，即由于不同客户端的最优解不一致而引发的不稳定性。然而，迄今为止，还缺乏扎实的理论分析来解释这种局部不一致性的影响。为缓解“客户端漂移”的负面效应并探究其在 FL 中的本质，本文首先提出了一种高效的 FL 算法——FedInit，它允许在每个本地训练阶段开始时采用个性化的松弛初始化状态。具体而言，FedInit 通过从当前全局状态出发，沿最新本地状态的反方向移动来初始化本地状态。此外，为进一步理解不一致性如何在 FL 中破坏性能，本文引入了超额风险分析，并研究了发散项以探究 FL 中的测试误差。我们的研究表明，优化误差对这种局部不一致性并不敏感，而它主要影响泛化误差界。大量实验验证了该方法的效率。所提出的 FedInit 方法在无额外训练或通信成本的情况下，可获得与多种先进基准算法相当的结果。同时，分阶段的个性化松弛初始化也可结合到多种现有先进算法中，以在 FL 范式下实现更高的泛化性能。

## Abstract
Federated learning (FL) is a distributed paradigm that coordinates massive local clients to collaboratively train a global model via stage-wise local training processes on the heterogeneous dataset. Previous works have implicitly studied that FL suffers from the ``client-drift'' problem, which is caused by the inconsistent optimum across local clients. However, till now it still lacks solid theoretical analysis to explain the impact of this local inconsistency. To alleviate the negative impact of ``client drift'' and explore its substance in FL, in this paper, we first propose an efficient FL algorithm FedInit, which allows employing the personalized relaxed initialization state at the beginning of each local training stage. Specifically, FedInit initializes the local state by moving away from the current global state towards the reverse direction of the latest local state. Moreover, to further understand how inconsistency disrupts performance in FL, we introduce the excess risk analysis and study the divergence term to investigate the test error in FL. Our studies show that optimization error is not sensitive to this local inconsistency, while it mainly affects the generalization error bound. Extensive experiments are conducted to validate its efficiency. The proposed FedInit method could achieve comparable results compared to several advanced benchmarks without any additional training or communication costs. Meanwhile, the stage-wise personalized relaxed initialization could also be incorporated into several current advanced algorithms to achieve higher generalization performance in the FL paradigm.

---

## 论文详细总结（自动生成）

# 论文总结：重新思考联邦学习中的个性化松弛初始化——一致性与泛化

---

## 一、核心问题与整体含义

- **研究背景与动机**
  - 联邦学习（Federated Learning, FL）通过协调大量分布式客户端在本地数据上训练模型，从而实现隐私保护的分布式协作学习。
  - 由于各客户端的数据分布通常高度异质，其最优解（local optimum）并不一致，导致一种被称为 **“客户端漂移（client drift）”** 的问题，即全局模型偏离最优点。
  - 现有方法多聚焦于通过正则化、动量或校正向量等方式缓解漂移，但缺乏对 **“一致性（consistency）”与“泛化误差（generalization error）”之间关系** 的系统理论研究。
  - 本文旨在 **重新审视一致性在联邦学习中的作用**，通过理论与实证双重视角分析本地不一致性如何影响全局泛化表现。

---

## 二、方法论：FedInit算法

### 核心思想

- 提出 **FedInit（Federated Initialization）** 算法，通过**个性化松弛初始化（Personalized Relaxed Initialization, RI）** 在每轮本地训练开始时调节模型初始状态，以缓解客户端漂移、提高泛化一致性。
- 核心思想：在每轮开始前，为每个客户端设置初始参数
  \[
  w_{t,i,0} = w_t + \beta (w_t - w_{t-1,i,K})
  \]
  其中：
  - \( w_t \)：当前全局模型；
  - \( w_{t-1,i,K} \)：客户端上一次本地训练后的状态；
  - \( \beta \)：松弛系数，决定个性化调整强度。
- 当 \( \beta > 0 \) 时，初始化“远离”上一次本地模型，从而避免过度偏向局部最优，维持全局一致性。

### 算法流程（文字描述）
1. **客户端选择**：在每轮通信中，随机选择部分客户端参与训练。
2. **个性化初始化**：选中客户端用上述公式更新初始状态（即“放松初始化”）。
3. **本地训练**：按标准本地SGD进行K步迭代。
4. **聚合更新**：服务端收集并平均各客户端模型，形成新的全局参数。
5. **循环迭代**：重复执行上述步骤至收敛。
6. **可插拔设计**：FedInit可无缝融合到其他算法（如SCAFFOLD、FedDyn、FedSAM）中作为预初始化模块。

### 理论分析重点
- **提出“超额风险分析（Excess Risk Analysis）”框架**，从联合视角拆解泛化误差 \( E_G \) 与优化误差 \( E_O \)。
- 证明：
  - 局部不一致对优化误差影响较小；
  - 对泛化误差上界的影响显著；
  - 通过引入松弛初始化可有效减弱该上界的增长率，从而改善测试精度与稳健性。
- 提供收敛性分析与一致性项（divergence term \(\Delta_t\)）的理论上界。

---

## 三、实验设计

### 数据集与模型
- **数据集**：
  - CIFAR-10 与 CIFAR-100。
  - 利用 Dirichlet 分布采样（系数 \( D_r = 0.1, 0.6 \)）构造不同程度的非IID异质性。
  - 额外引入 **客户端级噪声**（随机调整 RGB 通道）与 **类别级噪声**（亮度扰动）以加剧数据异质性。
- **模型架构**：
  - ResNet-18-GN
  - VGG-11

### 对比方法（benchmarks）
- FedAvg（基准）
- FedAdam（自适应优化）
- SCAFFOLD（方差修正）
- FedDyn（动态正则）
- FedCM（客户端动量）
- FedSAM（Sharpness-Aware 优化）

### 实验场景
- 客户端总数：100 或 200；
- 参与比例：10% 与 5%；
- 训练轮数：CIFAR-10 (500) / CIFAR-100 (800)；
- 学习率与衰减：局部 0.1，全局 1.0（或适配FedAdam、FedDyn等算法的设定）。

---

## 四、资源与算力

- **论文中未明确说明使用的具体硬件或GPU配置**。
- 提供了每轮平均训练时间（wall-clock time）对比，FedInit 在相同实现下与FedAvg成本几乎相同。
  - 例如：在 CIFAR-10 上，FedInit 约 20.4 秒/轮，与 FedAvg 的 19.4 秒基本一致。
- 可推测实验在常规单节点 / 多GPU环境完成，但无明确算力详情。

---

## 五、实验数量与充分性

- **实验覆盖范围广**：
  - 不同数据集（CIFAR-10/100）；
  - 不同模型（ResNet-18-GN 与 VGG-11）；
  - 不同非IID强度（Dir-0.1 / 0.6）；
  - 不同参与比例（10% / 5%）。
- **消融与敏感性分析**：
  - 研究局部步数 K 与松弛系数 β 的影响；
  - 插件式测试（在其他算法中加入 +RI）。
- **充分性与公平性**：
  - 所有对比方法共享相同超参数与通信设置；
  - 报告三次重复并给出标准差；
  - 未显示跨任务或隐私约束测试，但整体实验设计完备且可重复。

---

## 六、主要结论与发现

- **理论方面**：
  - 个性化放松初始化能降低泛化误差上限，而对优化误差无显著影响；
  - 证明收敛速率在有无RI的情形下可保持最优级别。
- **实验方面**：
  - FedInit 在 CIFAR-10 的高异质条件下：
    - 精度比 FedAvg 提升约 3.4%；
    - 低参与率下性能几乎无衰减；
  - 插入至其他算法（如 SCAFFOLD、FedSAM）时仍带来 2–3% 提升。
- **性能代价**：
  - 不增加额外通信或存储；
  - 训练时间与FedAvg近似；
  - 壁钟时间和收敛轮数均实现加速（约2倍通信效率）。

---

## 七、优点

- **轻量、通用、无通信负担**：仅修改初始化策略，无需额外变量或通信项。
- **理论与实验结合紧密**：证明了RI对泛化的明确优化作用。
- **高度兼容性**：可作为模块集成进各主流FL算法。
- **清晰的敏感性分析**：验证了参数 β 与局部步长 K 对性能的平衡点。
- **显著的稳健性与收敛性改进**：在强异质环境下保持性能稳定。

---

## 八、不足与局限

- **资源信息缺失**：论文未提供GPU型号、训练时长等，难以评估实验可复现性。
- **数据单一**：仅在图像分类任务上验证，缺少自然语言或时序数据实验。
- **β的选择依赖经验**：虽说明取值范围，但缺乏自适应策略。
- **理论分析局限**：部分假设（如PL条件、插值性质）较强，实际网络未必严格满足。
- **未涵盖隐私/安全策略耦合**：对加密、差分隐私下的适用性尚未讨论。

---

**（完）**
