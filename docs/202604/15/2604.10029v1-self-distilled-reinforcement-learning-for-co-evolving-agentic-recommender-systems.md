---
title: Self-Distilled Reinforcement Learning for Co-Evolving Agentic Recommender Systems
title_zh: 用于协同进化智能推荐系统的自蒸馏强化学习
authors: "Zongwei Wang, Min Gao, Hongzhi Yin, Junliang Yu, Tong Chen, Shazia Sadiq, Tianrui Li"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10029v1"
tags: ["query:flrlvldm"]
score: 9.0
evidence: 用于代理推荐系统的自蒸馏强化学习
tldr: 本文提出自蒸馏强化学习框架CoARS，用于协同演化的智能推荐系统。该方法将推荐视作推荐代理与用户代理的多轮交互过程，通过互动奖励和自蒸馏信用分配机制，将交互经验内化为模型参数，从而提高推荐性能和用户匹配度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10029v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of evolution paradigms in agentic recommender systems. The left part shows the standard ARS pipeline with the Reflexion paradigm. The right part compares three RL paradigms.\", \"page\": 2, \"index\": 1, \"width\": 1022, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.10029v1/fig-002.webp\", \"caption\": \"TABLE III: Recommendation performance comparison across datasets. The best result in each dataset column is highlighted in bold, and the second-best result is underlined.\", \"page\": 7, \"index\": 2, \"width\": 496, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.10029v1/fig-003.webp\", \"caption\": \"TABLE IV: User simulation performance comparison across datasets. The best result in each dataset column is highlighted in bold, and the second-best result is underlined.\", \"page\": 7, \"index\": 3, \"width\": 496, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2604.10029v1/fig-004.webp\", \"caption\": \"Fig. 2: The framework of our CoARS.\", \"page\": 4, \"index\": 4, \"width\": 1022, \"height\": 481}]"
motivation: 现有推荐代理未能内化交互经验且忽视交互过程中的密集监督信号。
method: 提出自蒸馏强化学习框架CoARS，结合互动奖励与自蒸馏信用分配进行学习。
result: 实验结果显示CoARS在多个数据集上优于主流智能推荐系统基线。
conclusion: CoARS实现了推荐与用户代理的协同演化，显著提升了推荐效果与用户对齐度。
---

## 摘要
由大型语言模型驱动的智能推荐系统（Agentic Recommender Systems, ARS）将推荐问题重新表述为推荐代理与用户代理之间的多轮交互过程，使得偏好表达与优化可以在传统的一次性预测之外反复迭代。然而，现有的 ARS 主要在类似 Reflexion 的范式下优化，其中过去的交互轨迹被存储为文本记忆，并在后续推理中作为提示上下文被检索。虽然这种设计使代理能够回忆先前的反馈和观察，但累积的经验仍然是外部的，没有融入模型参数，使代理依赖于通用推理，而无法通过学习逐步获得面向推荐的决策能力。因此，强化学习（Reinforcement Learning, RL）为将此类交互经验内化到参数中提供了自然途径。然而，现有的 ARS 强化学习方法仍受到两个关键限制：第一，它们未能捕捉 ARS 的交互特性——推荐代理和用户代理持续相互影响，并可通过交互反馈自然产生内生监督；第二，它们将丰富的多轮交互过程简化为最终结果，忽视了整个轨迹中密集的监督信号。为此，我们提出 CoARS，这是一种用于协同进化的智能推荐系统的自蒸馏强化学习框架。CoARS 引入了两种互补的学习机制：交互奖励（interaction reward），从同一交互轨迹中为推荐代理和用户代理导出耦合的任务级监督；以及自蒸馏信用分配（self-distilled credit assignment），在师生条件下将历史轨迹转换为令牌级信用信号。在多个数据集上的实验表明，CoARS 在推荐性能和用户契合度方面均优于代表性 ARS 基线模型。

## Abstract
Large language model-empowered agentic recommender systems (ARS) reformulate recommendation as a multi-turn interaction between a recommender agent and a user agent, enabling iterative preference elicitation and refinement beyond conventional one-shot prediction. However, existing ARS are mainly optimized in a Reflexion-style paradigm, where past interaction trajectories are stored as textual memory and retrieved as prompt context for later reasoning. Although this design allows agents to recall prior feedback and observations, the accumulated experience remains external to model parameters, leaving agents reliant on generic reasoning rather than progressively acquiring recommendation-specific decision-making ability through learning. Reinforcement learning (RL) therefore provides a natural way to internalize such interaction experience into parameters. Yet existing RL methods for ARS still suffer from two key limitations. First, they fail to capture the interactive nature of ARS, in which the recommender agent and the user agent continuously influence each other and can naturally generate endogenous supervision through interaction feedback. Second, they reduce a rich multi-turn interaction process to final outcomes, overlooking the dense supervision embedded throughout the trajectory. To this end, we propose CoARS, a self-distilled reinforcement learning framework for co-evolving agentic recommender systems. CoARS introduces two complementary learning schemes: interaction reward, which derives coupled task-level supervision for the recommender agent and the user agent from the same interaction trajectory, and self-distilled credit assignment, which converts historical trajectories into token-level credit signals under teacher-student conditioning. Experiments on multiple datasets show that CoARS outperforms representative ARS baselines in recommendation performance and user alignment.

---

## 论文详细总结（自动生成）

## 一、核心问题与研究背景

- **研究动机**：传统推荐系统通常基于一次性预测（one-shot prediction），仅根据用户历史行为来预测下一步兴趣。然而这种方式：
  - 无法捕获用户的动态偏好变化；
  - 不能处理多轮反馈中的丰富监督信号；
  - 无法让模型参数“记住”过往交互经验。

- **智能推荐系统（Agentic Recommender Systems, ARS）**：
  - 借助大型语言模型（LLM）实现双代理交互：推荐代理（RecAgent）与用户代理（UserAgent）通过多轮对话进行推荐与反馈；
  - 现有方法仍依赖**Reflexion范式**，即将历史对话作为文本记忆存储并注入提示（prompt），不做参数层更新；
  - 这种方式只能在上下文中复用记忆，而不能真正让模型从经验中“学习”。

- **研究问题**：  
  作者指出，现有基于强化学习（RL）的 ARS 存在两大问题：
  1. 奖励信号依赖人类规则或外部判官（judge），无法体现系统内生的交互性；
  2. 奖励多为结果级（outcome-level）稀疏信号，忽略轨迹中每一步的密集监督信息。

---

## 二、方法论：自蒸馏强化学习框架 CoARS

CoARS（**C**o-evolving **A**gentic **R**ecommender **S**ystems）旨在通过**互动奖励机制（interaction reward）**与**自蒸馏信用分配（self-distilled credit assignment）**实现推荐代理与用户代理的协同进化。

### 2.1 核心思想
- 将推荐流程建模为**双智能体的多轮博弈与学习过程**；
- 交互中的每一轮包含推荐推理、用户反馈、记忆更新；
- 借助强化学习的参数优化，将交互经验内化至模型权重；
- 进一步通过自蒸馏机制提供细粒度的token级信号，解决传统RL奖励稀疏的问题。

### 2.2 关键组件

#### （1）互动奖励 Interaction Reward
- **设计原则**：每轮交互为双方生成**相互耦合的奖励信号**。
- **RecAgent 奖励公式：**
  \[
  R^{rec}_t = (2hit_t - 1)(0.5 + 0.5s_t)D_t
  \]
  - \(hit_t\)：推荐是否命中目标；
  - \(s_t\)：用户接受强度（来自UserAgent的评分）；
  - \(D_t\)：交互阶段权重（过程越晚，错误惩罚越大）。
- **UserAgent 奖励公式：**
  \[
  R^{user}_t = (2hit_t - 1)(2s_t - 1)(1 - \alpha q_t(2s_t - 1))
  \]
  - 考虑推荐准确性、用户反应方向与强度、以及同类用户相似度 \(q_t\)；
  - 同类用户反馈越一致，奖励调节越强；
  - 参数 α 控制相似度权重（设定为0.1）。

#### （2）自蒸馏信用分配 Self-Distilled Credit Assignment
- **思路**：每个历史交互轨迹都可作为“经验教师”；
- 将同一代理分为**教师模式**与**学生模式**：
  - 教师模式：可访问诊断后的**参考轨迹**（包含原推荐、实际反馈与修正说明）；
  - 学生模式：仅根据原始上下文决策；
- **比较二者在token层概率分布的差异**，形成诊断优势（advantage）信号：
  \[
  A_{t,n} = clip(\log\pi_T - \log\pi_S, -1, 1)
  \]
  用剪裁防止梯度不稳定。
- 通过这种方式，将温和的参考轨迹转化为fine-grained的学习信号。

#### （3）总体优化目标
- 推荐侧与用户侧各自优化：
  \[
  J = \sum_t \frac{1}{|y_t|}\sum_n (R_t + \lambda_{SD}A_{t,n}) \cdot \log \pi_S(y_{t,n} | x_t, y_{t,<n})
  \]
  - 奖励与token诊断优势共同构建复合信号；
  - 通过LoRA微调实现轻量级训练。

---

## 三、实验设计与基准设置

### 3.1 数据集
共使用三个公开序列推荐数据集：
| 数据集 | 用户数 | 项目数 | 交互数 |
|---------|---------|---------|---------|
| LastFM | 2,100 | 18,745 | 92,835 |
| MovieLens | 943 | 1,682 | 100,001 |
| Instruments | 903,330 | 112,136 | 1,512,531 |

### 3.2 对比方法 Baselines
- **Reflexion**（基于记忆检索的单次预测）
- **AFL**（双代理交互但无参数更新）
- **iAgent**（指令增强型用户代理）
- **RecoWorld**（基于强化学习的单向优化）

### 3.3 模型与评测设置
- **LLM骨干**：
  - Qwen3-8B / Qwen3-4B（可训练）
  - GPT-5.4-mini（固定评测）
- **底层推荐器**：SASRec 用于生成20个候选项；
- **评估指标**：
  - 推荐性能：**Hit@1**
  - 用户反馈模拟：**F1 分数**

---

## 四、资源与算力信息

- 文中未明确说明使用的GPU型号、数量或训练时长；
- 仅提及“采用 LoRA 微调、冻结教师模型”；
- 暗示所需算力有限，适用于常规模型适配。

---

## 五、实验数量与充分性

- 主要实验覆盖：
  1. 三个数据集 × 多种LLM（共约6组主实验）；
  2. 用户仿真测试；
  3. **六组消融实验**（分别去除用户训练、互动奖励、自蒸馏）；
  4. 案例分析（teacher-student构造过程）。
- **充分性评价**：
  - 实验覆盖多数据集、多模型规模；
  - 对照基线包含多种范式，较为公平；
  - 消融与案例分析验证机制合理，实验设计客观完整。

---

## 六、主要结论与发现

- CoARS 在所有数据集上均显著优于对照方法；
- **推荐准确性（Hit@1）**与**用户模拟一致性（F1）**均大幅提升；
- 双代理协同优化明显优于单侧优化；
- 自蒸馏机制有效缓解了稀疏奖励问题；
- 案例研究表明，诊断式自蒸馏能明确指出代理错误来源，并据此纠正。

---

## 七、优点与创新点

- 🎯 **从Prompt到Parameter的跃迁**：将交互经验从外部记忆移入内部参数更新；
- 🔁 **双向演化机制**：RecAgent与UserAgent同时学习，形成真正的共进化；
- 🧩 **组合式奖励设计**：联合考虑推荐准确性、用户接受度、交互阶段；
- 🧠 **自监督式蒸馏机制**：不依赖外部教师或人工标注，利用历史轨迹生成teacher信号；
- 🧮 **广泛实验验证**：多数据集、多规模模型、消融与案例相结合；
- ⚖️ **可解释性**：诊断标签可指明错误来源，提升反馈透明度。

---

## 八、不足与局限

- ⚙️ **算力与资源信息缺失**：未报告GPU配置与训练成本；
- 🧾 **外部应用验证缺乏**：仅在离线数据集上测试，缺少真实交互场景；
- 🧪 **模型规模偏有限**：主要基于中型LLM（4B/8B），未验证大规模可扩展性；
- 📊 **奖励函数超参数未系统探讨**（如 α、λ 的敏感性）；
- 👥 **UserAgent普适性**：只有单一共享用户代理，个性化用户建模能力尚有限。

---

**（完）**
