---
title: "FP4 Explore, BF16 Train: Diffusion Reinforcement Learning via Efficient Rollout Scaling"
title_zh: FP4 探索，BF16 训练：通过高效 Rollout 扩展实现的扩散强化学习
authors: "Yitong Li, Junsong Chen, Shuchen Xue, Pengcuo Zeren, Siyuan Fu, Dinghao Yang, Yangyang Tang, Junjie Bai, Ping Luo, Song Han, Enze Xie"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06916v1"
tags: ["query:flrlvldm"]
score: 10.0
evidence: 用于文生图对齐的扩散强化学习
tldr: 本文提出了Sol-RL框架，用于在大规模文本到图像扩散模型中实现高效的强化学习后训练。通过结合FP4量化用于快速探索和BF16用于高精度优化，这一两阶段策略有效解决了扩展rollout的计算瓶颈，在保持训练精度的同时加速模型对齐过程。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06916v1/fig-001.webp\", \"caption\": \"Table 1 | Quantitative comparison of alignment performance. Evaluated on FLUX.1 under an identical GPU-hour budget. Δ indicates the performance improvement over the Base (w/o CFG) model. Bold and darker green background indicate the best results, while lighter green background indicates the second best.\", \"page\": 6, \"index\": 1, \"width\": 983, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.06916v1/fig-002.webp\", \"caption\": \"Figure 4 | Comparison across diverse foundation models and alignment metrics. Evaluated under identical wallclock budgets (GPU Hours), Sol-RL (green) consistently outperforms the DiffusionNFT baseline (grey). Across all tested combinations of models and reward functions, our decoupled scaling strategy accelerates convergence to the baseline’s equivalent performance by up to 4.64×, ultimately converging to a remarkably higher final alignment ceiling.\", \"page\": 7, \"index\": 2, \"width\": 917, \"height\": 671}, {\"url\": \"assets/figures/arxiv/2604.06916v1/fig-003.webp\", \"caption\": \"Figure 7 | Qualitative comparison on PickScore-optimized models. We compare images generated by Flux.1-dev base models against their Sol-RL, DiffusionNFT and FlowGRPO fine-tuned variants. Sol-RL produces images with stronger semantic alignment to the prompt, richer fine-grained details, and more coherent artistic style.\", \"page\": 14, \"index\": 3, \"width\": 983, \"height\": 1350}, {\"url\": \"assets/figures/arxiv/2604.06916v1/fig-004.webp\", \"caption\": \"Figure 5 | Visual comparison before and after Sol-RL. Compared to the SANA base model without fine-tuning (top row), the counterpart optimized across multiple rewards (HPSv2, PickScore, CLIPScore and OCR) via Sol-RL (bottom row) exhibits substantial improvements in complex detail rendering and semantic alignment across various prompts.\", \"page\": 8, \"index\": 4, \"width\": 983, \"height\": 439}, {\"url\": \"assets/figures/arxiv/2604.06916v1/fig-005.webp\", \"caption\": \"Figure 6 | Visualization of NVFP4 and BF16 rollouts. Despite minor localized deviations, the NVFP4 quantized rollouts maintain the overall semantic layout and structure.\", \"page\": 8, \"index\": 5, \"width\": 465, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2604.06916v1/fig-006.webp\", \"caption\": \"Figure 2 | Decoupled two-stage reinforcement learning pipeline of Sol-RL. We separate the high-throughput FP4 exploration from the selective BF16 high-contrastive rollout. This framework achieves up to 2.4× acceleration compared to naive scaling while avoiding quantization-induced corruption, introducing merely a 2% computational overhead.\", \"page\": 4, \"index\": 6, \"width\": 975, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2604.06916v1/fig-007.webp\", \"caption\": \"Table 7 | Training hyperparameters. Rows with a single value in the rightmost column are shared across all models.\", \"page\": 12, \"index\": 7, \"width\": 983, \"height\": 829}, {\"url\": \"assets/figures/arxiv/2604.06916v1/fig-008.webp\", \"caption\": \"Figure 3 | Pitfalls and Potential of NVFP4 rollouts. (a) Time breakdown of high-precision rollout scaling and direct quantized rollout. The x-axis labels follow the format 𝐾-in-𝑁 (𝑃 ), denoting that 𝐾 samples are selected for training from 𝑁 generated rollouts under 𝑃 precision. (b) Directly integrating FP4 rollout in RL pipeline leads to severe instability and performance degradation compared to the BF16 baseline. (c) Conversely, the dense diagonal distribution of intra-group relative reward rankings validates NVFP4 quantized rollouts as a reliable proxy for reward sorting.\", \"page\": 5, \"index\": 8, \"width\": 1069, \"height\": 395}, {\"url\": \"assets/figures/arxiv/2604.06916v1/fig-009.webp\", \"caption\": \"Figure 9 | Qualitative comparison on ImageReward-optimized models. We compare images generated by Flux.1-dev base models against their Sol-RL, DiffusionNFT and FlowGRPO fine-tuned variants. Sol-RL produces images with stronger semantic alignment to the prompt, richer fine-grained details, and more coherent artistic style.\", \"page\": 16, \"index\": 9, \"width\": 983, \"height\": 1371}]"
motivation: 为解决大规模扩散模型RL后训练中rollout计算成本过高的问题，提高对齐效率与性能。
method: 提出基于FP4探索和BF16训练的两阶段强化学习框架Sol-RL以高效扩展Diffusion RL。
result: 该方法在多个扩散模型上显著提升了对齐性能，并使训练收敛速度提高了4.64倍。
conclusion: Sol-RL在保持BF16训练完整性的前提下充分利用FP4算力，成功实现高效率与高性能的统一。
---

## 摘要
基于强化学习的后训练近期成为一种将文本到图像扩散模型与人类偏好对齐的有前景的范式。最新研究中发现，增加 rollout 组的规模会显著提升性能，表明进一步对齐仍有相当大的提升空间。然而，将 rollout 扩展到大规模基础扩散模型（如 FLUX.1-12B）会带来巨大的计算负担。为缓解这一瓶颈，我们探索了在 Diffusion RL rollout 中引入 FP4 量化。然而，我们发现天真地采用量化管线会带来显著的性能退化风险。为解决效率与训练完整性之间的两难，我们提出了 Sol-RL（Speed-of-light RL），一种基于 FP4 的两阶段强化学习框架。首先，我们利用高吞吐的 NVFP4 rollout 生成大规模候选池，并提取出高度对比性的子集。其次，我们在 BF16 精度下重新生成这些选定样本，并仅在这些样本上优化策略。通过将候选探索与策略优化解耦，Sol-RL 将 rollout 扩展的算法机制与 NVFP4 的系统级吞吐增益相结合。这种算法与硬件的协同设计有效加速了 rollout 阶段，同时保留用于优化的高保真样本。我们的实验证明，该框架在充分利用 FP4 算术带来的吞吐增益的同时，保持了 BF16 精度管线的训练完整性。在 SANA、FLUX.1 和 SD3.5-L 上的大量实验表明，我们的方法在多个指标上实现了更优的对齐性能，并将训练收敛速度最高提升至 4.64 倍，以极低成本释放了大规模 rollout 扩展的潜力。

## Abstract
Reinforcement-Learning-based post-training has recently emerged as a promising paradigm for aligning text-to-image diffusion models with human preferences. In recent studies, increasing the rollout group size yields pronounced performance improvements, indicating substantial room for further alignment gains. However, scaling rollouts on large-scale foundational diffusion models (e.g., FLUX.1-12B) imposes a heavy computational burden. To alleviate this bottleneck, we explore the integration of FP4 quantization into Diffusion RL rollouts. Yet, we identify that naive quantized pipelines inherently introduce risks of performance degradation. To overcome this dilemma between efficiency and training integrity, we propose Sol-RL (Speed-of-light RL), a novel FP4-empowered Two-stage Reinforcement Learning framework. First, we utilize high-throughput NVFP4 rollouts to generate a massive candidate pool and extract a highly contrastive subset. Second, we regenerate these selected samples in BF16 precision and optimize the policy exclusively on them. By decoupling candidate exploration from policy optimization, Sol-RL integrates the algorithmic mechanisms of rollout scaling with the system-level throughput gains of NVFP4. This synergistic algorithm-hardware design effectively accelerates the rollout phase while reserving high-fidelity samples for optimization. We empirically demonstrate that our framework maintains the training integrity of BF16 precision pipeline while fully exploiting the throughput gains enabled by FP4 arithmetic. Extensive experiments across SANA, FLUX.1, and SD3.5-L substantiate that our approach delivers superior alignment performance across multiple metrics while accelerating training convergence by up to $4.64\times$, unlocking the power of massive rollout scaling at a fraction of the cost.

---

## 论文详细总结（自动生成）

# FP4 探索，BF16 训练：通过高效 Rollout 扩展实现的扩散强化学习 — 深度总结

---

## 一、研究问题与背景

- **核心问题**  
  本文针对 **文本到图像扩散模型** 在强化学习后训练（RL-based post-training）中的效率与稳定性矛盾展开研究：  
  - 扩展 rollout 规模（即每个提示生成更多候选样本）可以显著提升人类偏好对齐效果。  
  - 但在大模型（如 FLUX.1-12B）上进行 rollout 扩展计算代价极高，其瓶颈在于大量候选样本生成阶段。  
  - 传统加速方案（如低比特量化）虽然节约算力，却引入性能退化与训练不稳定性。

- **研究动机**  
  作者希望在不牺牲模型对齐性能的前提下，**充分利用硬件的量化算力（FP4）** 实现大规模 Rollout 的高效扩展，从而提升训练速度与可扩展性。

- **总体目标**  
  解决“**效率与训练完整性两难问题**”，通过结合低比特探索和高精度优化，实现扩散强化学习的稳健加速。

---

## 二、方法论：Sol-RL 框架

### 1. 核心思想
提出 **Sol-RL (Speed-of-Light Reinforcement Learning)** 两阶段扩散强化学习框架：
- 利用 **FP4 量化模型进行高速探索（Exploration）**，生成大规模候选样本池。
- 再将筛选出的最具对比性的样本（最高奖励与最低奖励）在 **BF16 精度下重新生成并用于策略优化**。
- 通过结构性解耦，使探索阶段与优化阶段分别在不同精度下执行，兼顾算力吞吐与训练保真。

### 2. 技术细节
- **FP4 量化实现**：采用 NVIDIA 的 NVFP4 格式（1符号位 + 2指数位 + 1尾数位），支持每16元素共享缩放因子，实现约 **4×算力提升**。  
- **Group Relative Policy Optimization (GRPO)**：基于相对奖励的优势计算公式：
  \[
  A_i = \frac{R_i - \mu_R}{\sigma_R}
  \]
  用于对同一提示的候选样本进行相对评分，提升训练信号稳定性。
- **两阶段流程**
  1. **FP4 Exploration**  
     - 使用 NVFP4 模型，以 6 步推理生成 96 个候选样本，计算近似奖励值。  
     - 保留 Top-K 与 Bottom-K 样本的初始随机种子。
  2. **BF16 Regeneration & Training**  
     - 使用高精度（BF16）模型根据筛选的种子重新生成 24 张高保真样本。  
     - 仅在该高精度集合上执行策略优化（DiffusionNFT 目标函数）。  
     - 更新后的权重再量化为 NVFP4 用于下一轮探索。

### 3. 理论分析
附录中利用 **Lipschitz 连续性与极值理论（EVT）** 证明了低精度 rollouts 保留相对奖励排名的稳定性：  
- 误差上界为静态常数Δ，与 rollout 数量无关。  
- 当 N 扩展时，极值对比信号的增长 \( 2σ√(2logN) \) 会掩盖低精度误差，从而保持训练信号有效。

---

## 三、实验设计

### 1. 模型与数据集
实验覆盖三种当前主流扩散模型：
- **SANA**：高分辨率线性扩散 Transformer。
- **FLUX.1**：基础视觉生成模型（512×512）。  
- **Stable Diffusion 3.5-Large（SD3.5-L）**：高分辨率生成模型。

训练与评估使用公开的文本到图像偏好数据集：
- **PickScore**（用于训练与验证提示采样）。
- 奖励模型包括：**ImageReward、CLIPScore、PickScore、HPSv2**（衡量人类偏好与语义一致性）。

### 2. Benchmark 与对比方法
对比基线：
- FlowGRPO [2025]
- DanceGRPO [2025]
- AWM (Advantage Weighted Matching) [2025]
- DiffusionNFT [2026]（直接后训练方法）

### 3. 评估指标
- 四项偏好对齐指标（ImageReward、CLIPScore、PickScore、HPSv2）。
- 定量性能提升 Δ（相对于 Base 模型）和收敛速度加速倍数。

---

## 四、资源与算力

- **硬件配置**：8× NVIDIA **B200 GPU**，FP4 部分使用 **Transformer Engine** 加速。  
- **精度设置**：NVFP4 用于探索阶段；BF16 用于训练阶段。  
- **计算性能**：FP4 算术可达 BF16 的 4× FLOPs，整体训练阶段加速最高达 **4.64×**。  
- 文中未提供训练总时长或样本数，仅报告每阶段推理耗时（如 FLUX.1 rollout：184s → 79s）。

---

## 五、实验数量与充分性

- **主要实验**：
  1. 三个模型 × 四个奖励函数的综合评估（共12组完整训练比较）。
  2. **消融实验**：
     - 不同 FP4 探索步数（2,4,6,8），验证排名可靠性。
     - 不同探索池大小（N=24,48,72,96）。
     - 对比量化误差（NVFP4 vs BF16）与 Inception/CLIPScore。
  3. **代理排名一致性验证**：
     - Kendall τ 和 Spearman ρ 达到平均 0.798 / 0.927，精确匹配率超 96%。

- **充分性与公平性**：  
  - 覆盖多模型、多奖励、多参数设定，实验丰富。  
  - 对比方法齐全且共享相同超参数与预算（GPU-hour相同），具有较高公平性。  

---

## 六、主要结论与发现

- Sol-RL 框架解决了扩散强化学习中 **计算瓶颈与训练退化并存的问题**。  
- 关键结果：
  - **对齐性能提升**：在 FLUX.1 上四项指标均超越全部基线。
  - **收敛加速**：训练达到同等奖励水平的时间缩短至 **1.91×到4.64×**。
  - **对齐保真度**：与纯 BF16 rollout 相比性能差距<1%，甚至在 SANA 上略有提升。
  - **代理稳定性**：FP4 探索阶段保持可靠排名，验证量化误差不会扰乱梯度更新。

---

## 七、方法优势

- **算法与硬件协同设计**：将 FP4 算力与 RL 训练结构结合，充分利用最新 GPU 架构吞吐。  
- **解耦设计**：探索（生成）与优化（训练）分离，保证效率与稳定性并存。  
- **高扩展性**：可在超大基础扩散模型上实现大规模 Rollout。  
- **理论可解释性**：通过误差上界和极值推导验证代理稳健性。  
- **系统性加速**：在硬件层和算法层同时获得收益（2.4× rollout加速，1.6×端到端加速）。

---

## 八、不足与局限

- **训练资源披露不完整**：未给出训练时长与样本总数。  
- **适用范围限制**：目前实验仅针对文本到图像生成模型，未验证在语音或视频扩散上的通用性。  
- **探索阶段误差依赖假设**：理论保证基于 Lipschitz 条件与子高斯奖励分布，实际分布可能更复杂。  
- **奖励模型依赖性**：使用的 ImageReward / CLIPScore 等模型本身存在偏好偏差。  
- **通用部署可行性**：FP4 要求支持 NVFP4 的特定硬件体系，通用GPU上可用性受限。

---

**（完）**
