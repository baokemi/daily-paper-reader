---
title: Efficient Hierarchical Implicit Flow Q-learning for Offline Goal-conditioned Reinforcement Learning
title_zh: 高效层次隐式流 Q 学习用于离线目标条件强化学习
authors: "Zhiqiang Dong, Teng Pang, Rongjian Xu, Guoqiang Wu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08960v1"
tags: ["query:flrlvldm"]
score: 9.0
evidence: 离线目标条件强化学习算法的进展
tldr: 本文针对离线目标条件强化学习中长时间序列控制和策略表达能力不足的问题，提出了一种结合平均速度场的层次隐式流Q学习方法，并通过LeJEPA损失增强目标区分与泛化，在OGBench中取得明显性能提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08960v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of HIFQL. During training (left), a goal-conditioned value function and a shared goal representation encoder are jointly optimized, while high-level and low-level mean flow policies are trained via advantage-weighted regression to generate subgoals and actions, respectively. During inference (right), both policies perform one-step generation from Gaussian noise, enabling efficient hierarchical decision-making without iterative sampling.\", \"page\": 4, \"index\": 1, \"width\": 638, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.08960v1/fig-002.webp\", \"caption\": \"Table 2: Results on pixel-based tasks. We evaluate all methods on four sparse-reward visual AntMaze tasks, where observations are high-dimensional pixel inputs and long-horizon planning is required to reach the specified goal. All results report the average success rate and standard deviation over 5 random seeds.\", \"page\": 12, \"index\": 2, \"width\": 715, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2604.08960v1/fig-003.webp\", \"caption\": \"Table 1: Results on state-based tasks. We evaluate all methods on three Maze environment families—PointMaze, AntMaze, and HumanoidMaze—covering navigation, stitching, and teleport variants with increasing state dimensionality and horizon length. A total of 13 sparse-reward tasks are included. We report the average success rate and standard deviation over 5 random seeds for each algorithm. The best result for each task is shown in bold.\", \"page\": 12, \"index\": 3, \"width\": 714, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.08960v1/fig-004.webp\", \"caption\": \"Figure 2: Ablation study on the representation regularization coefficient λ in visual AntMaze tasks.\", \"page\": 13, \"index\": 4, \"width\": 699, \"height\": 697}]"
motivation: 现有层次方法在离线目标条件强化学习中面临高层子目标生成效果差及高斯策略表达受限的问题。
method: 提出了引入平均速度场的层次隐式流策略和LeJEPA损失，用于增强目标表示和策略表达能力。
result: 在OGBench中的状态和像素任务上均表现出优异性能，超过现有方法。
conclusion: 该方法显著提升了离线目标条件强化学习的长时序控制能力与泛化性能。
---

## 摘要
离线目标条件强化学习（GCRL）是一种实用的强化学习范式，旨在从无奖励的离线数据中学习目标条件策略。尽管最近在层次结构如 HIQL 等方面取得了进展，但由于高斯策略的表达能力有限以及高层策略无法生成有效的子目标，离线 GCRL 在长时序控制上仍面临挑战。为了解决这些限制，我们提出了目标条件平均流策略（goal-conditioned mean flow policy），它在离线 GCRL 的层次策略建模中引入平均速度场。具体而言，平均流策略通过学习的平均速度场捕获高层和低层策略的复杂目标分布，从而实现通过一步采样的高效动作生成。此外，鉴于目标表示的不足，我们引入一种 LeJEPA 损失，在训练过程中排斥目标表示的嵌入，从而鼓励更具区分性的表示并提升泛化能力。实验结果表明，我们的方法在 OGBench 基准的基于状态和基于像素的任务中都取得了优异的性能。

## Abstract
Offline goal-conditioned reinforcement learning (GCRL) is a practical reinforcement learning paradigm that aims to learn goal-conditioned policies from reward-free offline data. Despite recent advances in hierarchical architectures such as HIQL, long-horizon control in offline GCRL remains challenging due to the limited expressiveness of Gaussian policies and the inability of high-level policies to generate effective subgoals. To address these limitations, we propose the goal-conditioned mean flow policy, which introduces an average velocity field into hierarchical policy modeling for offline GCRL. Specifically, the mean flow policy captures complex target distributions for both high-level and low-level policies through a learned average velocity field, enabling efficient action generation via one-step sampling. Furthermore, considering the insufficiency of goal representation, we introduce a LeJEPA loss that repels goal representation embeddings during training, thereby encouraging more discriminative representations and improving generalization. Experimental results show that our method achieves strong performance across both state-based and pixel-based tasks in the OGBench benchmark.

---

## 论文详细总结（自动生成）

# 高效层次隐式流 Q 学习用于离线目标条件强化学习 — 论文总结

---

## 一、研究背景与核心问题

- **研究领域**：离线目标条件强化学习（Offline Goal-conditioned Reinforcement Learning, GCRL），旨在只借助无奖励的离线数据学习可以达到任意目标状态的策略。
- **问题背景**：  
  当前主流的层次式离线 GCRL 方法（如 HIQL）虽能分层决策（高层生成子目标，低层执行动作），但仍存在两大瓶颈：  
  1. **策略表达能力弱**：HIQL 使用的高斯策略是单峰分布，难以拟合复杂行为数据的多模态分布。  
  2. **高层子目标生成能力不足**：尤其在长时序控制任务中，高层策略难以有效输出子目标，导致整个决策链不稳定。  
- **研究动机**：为解决上述问题，作者引入更具表达力的生成建模（flow matching 与 mean flow）思想，并通过改进目标表示学习增强高层决策的泛化与鲁棒性。

---

## 二、方法论与技术框架

### 1. 整体框架：HIFQL（Hierarchical Implicit Flow Q-Learning）

- 对标 HIQL，提出更高效的 **层次隐式流 Q 学习结构**。
- 高层和低层策略均采用 **Mean Flow Policy** 进行建模，用平均速度场替代高斯分布，实现更精准且可一键生成（one-step）的动作或子目标。
- 核心思想：通过平均流（mean flow）学习高层/低层策略分布的“平均速度场”，在推理阶段可直接从噪声生成行为，节省多步采样计算量。

### 2. 关键技术组件

1. **Hierarchical Goal-conditioned Mean Flow Policy**：  
   - 高层策略：根据状态与目标生成子目标表示。  
   - 低层策略：根据状态与子目标生成动作。  
   - 训练时使用平均速度场的目标函数，基于公式：
     \[
     u_{g}(x_t,r,t,s_h,y) = v_g(x_t,t,s_h,y) - (t-r)\frac{d u_g}{dt}
     \]
     通过 Jacobian-Vector Product（JVP）高效计算梯度。
   - 推理时仅需一步采样：
     \[
     x_{0h} = x_{1h} - u_{\theta_h}(x_{1h},0,1,s,g)
     \]
     \[
     a = x_{1l} - u_{\theta_l}(x_{1l},0,1,s,x_{0h})
     \]
     实现无迭代、单步生成的层次决策。

2. **LeJEPA 目标表示编码器**：  
   - 基于联合嵌入预测架构（JEPA）理念，增强高层目标表示的语义一致性与分布规范性。  
   - 引入 **Sketch Isotropic Gaussian Regularization（SIGReg）**，使嵌入趋近于理想的各向同性高斯分布。  
   - 可同时约束语义一致性（预测损失）与全局分布（正则项）。

3. **训练框架**：
   - 使用 Expectile Regression 训练价值函数。
   - 高低层策略则采用 **Advantage-Weighted Regression (AWR)** 加权行为克隆方式进行优化。
   - 训练算法流程见伪代码（Algorithm 1），包含价值网络、编码器及两级平均流策略的联合优化；推理阶段则为单步生成（Algorithm 2）。

---

## 三、实验设计

- **Benchmark 数据集**：OGBench（2024）离线目标条件强化学习基准。  
  包含：
  - **状态任务**：PointMaze（2 DoF）、AntMaze（8 DoF）、HumanoidMaze（21 DoF）等共 13 个稀疏奖励任务；
  - **像素任务**：4 个视觉 AntMaze 任务，测试高维观测下的长时序规划能力。
- **对比基线** 六类主流算法：
  1. GCBC — 直接行为克隆；
  2. GCIVL / GCIQL — 基于隐式价值函数的目标条件学习；
  3. QRL — 基于拟度量学习的时序距离估计；
  4. CRL — 基于对比学习的目标条件强化学习；
  5. HIQL — 使用分层高斯策略的隐式 Q 学习。
- **评估指标**：各任务成功率（Success Rate）及标准差，五个不同随机种子平均。

---

## 四、资源与算力说明

- 文中 **未给出具体硬件或算力使用说明**（如 GPU 型号、数量、训练时长及批量大小）。
- 整体训练过程描述为“标准离线 GCRL 环境下端到端优化”，未涉及额外算力消耗分析。

---

## 五、实验规模与充分性

- **主要实验**：
  - 三类状态任务 + 四类像素任务，共 **17 个长期决策任务**；
  - 每个任务均以 5 个随机种子重复，统计平均与标准差。
- **消融实验**：
  - 调整目标表示正则系数 λ 的对比实验（Figure 2）；
  - 探讨 LeJEPA 正则项对高维表示学习的稳定性影响。
- **充分性与公平性**：
  - 基线覆盖主流方向：模仿、值函数、层次、对比学习。
  - 算法均按 OGBench 标准协议训练，因此比较较为公平。
  - 但缺少复杂机器人任务或跨任务泛化测试，评估范围有限。

---

## 六、主要结论与发现

1. **性能提升显著**：  
   - 在 **PointMaze** 大规模和 stitching 类任务中，成功率明显超越 HIQL 与其他所有基线。
   - 在 **Visual AntMaze** 中（特别是中、大任务），HIFQL 达到最高成功率，证明平均流 + LeJEPA 表示对高维目标学习的促进作用。
2. **推理效率优越**：  
   - 一步采样机制减少了多步生成带来的延迟，推理极高效。
3. **目标表示正则效果显著**：  
   - 适当的 LeJEPA 权重能稳定训练并提升最终收敛成功率。

---

## 七、优点与亮点

- **策略表达性强**：平均流建模突破高斯限制，能刻画多模态行为分布。  
- **推理高度效率化**：单步采样替代多步 ODE 积分，提升推理速度。  
- **表现鲁棒**：通过表征正则（LeJEPA）改善视觉任务中的抽象目标生成。  
- **层次结构合理**：高层–低层分工明确，结合价值信号构成完整的离线决策体系。  
- **实验覆盖全面**：状态任务与像素任务均体现方法的通用性。

---

## 八、不足与局限

- **算力与实现细节缺失**：论文未公布训练资源与复杂度指标，复现成本不明。  
- **应用局限**：当前仅在 Maze 类环境验证，对实际机器人或连续视觉控制尚无结果。  
- **性能不均衡**：在高维如 HumanoidMaze 与部分 teleport 任务中不如 HIQL，说明高层表达改进不能完全解决低层控制困难。  
- **理论分析不足**：缺乏对平均流策略收敛性或稳定性的理论刻画。  
- **表示方法复杂**：LeJEPA 的正则过程计算昂贵、超参数较多，可能影响普适性。

---

**（完）**
