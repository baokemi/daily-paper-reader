---
title: "Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation"
title_zh: Firebolt-VL：通过跨模态调制实现高效的视觉-语言理解
authors: "Quoc-Huy Trinh, Mustapha Abdullahi, Bo Zhao, Debesh Jha"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04579v2"
tags: ["query:vlm-clip"]
score: 9.0
evidence: 替换Transformer交叉注意力的效率型视觉语言模型
tldr: 该研究提出Firebolt-VL模型，旨在提升视觉语言理解的效率与精度。其通过将Transformer解码器替换为液态基础模型解码器，并引入Token-Grid相关模块以轻量方式关联文本与图像特征，从而实现高效且准确的细粒度多模态推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04579v2/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison of the proposed Firebolt-VL model with existing MLLMs across seven benchmarks. The superscript p denotes the perception score on the MME benchmark, while SQAI refers the ScienceQA-IMG [30]. The best results are shown in bold, and the second-best results are underlined. “–” indicates results not reported in the original papers.\", \"page\": 6, \"index\": 1, \"width\": 976, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2604.04579v2/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison of responses from Firebolt-VL with recent efficient vision–language models, including MobileVLM V2 [10] and SmolVLM2 [31], on detail-dependent question-answering tasks. Firebolt-VL demonstrates stronger fine-grained grounding and more accurate, instruction-aligned responses.\", \"page\": 6, \"index\": 2, \"width\": 891, \"height\": 588}, {\"url\": \"assets/figures/arxiv/2604.04579v2/fig-003.webp\", \"caption\": \"Figure 2. Overview of the Firebolt-VL architecture. The Cross-Modal Modulator (CMM) fuses textual instructions with the visual representations of the query image to produce conditioned tokens, which are then processed by the Liquid Foundation Model (LFM). The model is trained in two stages: (1) CMM pre-training to initialize modulation parameters, and (2) end-to-end training of the full framework.\", \"page\": 4, \"index\": 3, \"width\": 912, \"height\": 430}]"
motivation: 多模态大模型在视觉语言理解上虽有突破，但计算成本高且小模型难以精确对齐图像细节。
method: 采用液态基础模型解码器并设计Token-Grid关联模块，通过FiLM条件调制提升跨模态信息对齐。
result: 实验表明，Firebolt-VL在多个基准上实现了高精度细粒度理解，同时大幅提升了推理效率。
conclusion: Firebolt-VL证明了基于状态空间和关联调制的结构可在保持线性推理效率的同时实现精细视觉语言理解。
---

## 摘要
近年来，多模态大型语言模型（MLLM）的进展显著推动了视觉-语言理解的发展，但其高计算成本限制了在资源受限场景下的应用，如个人助手、文档理解和智能摄像头等。现有多数方法依赖于基于 Transformer 的跨模态注意力机制，其平方复杂度阻碍了效率提升。此外，小型视觉-语言模型往往难以精准捕捉任务相关的细粒度视觉区域，导致在细粒度推理任务中的性能下降，从而限制了其实际应用效果。为了解决这些问题，我们提出了 Firebolt-VL，一种高效的视觉-语言模型，它以 Liquid Foundation Model（LFM）解码器替代基于 Transformer 的解码器。为进一步增强视觉定位能力，我们提出了 Token-Grid 相关模块，该模块计算文本 token 与图像 patch 之间的轻量相关性，并通过带有 FiLM 条件的状态空间模型进行调制。该设计使模型能够在保持线性时间推理的同时，选择性地强化与文本提示相关的视觉区域。在多个基准测试上的实验结果表明，Firebolt-VL 实现了精确、细粒度的理解，并显著提升了效率。我们的模型与代码可在：https://fireboltvl.github.io 获取。

## Abstract
Recent advances in multimodal large language models (MLLMs) have enabled impressive progress in vision-language understanding, yet their high computational cost limits deployment in resource-constrained scenarios such as personal assistants, document understanding, and smart cameras. Most existing methods rely on Transformer-based cross-attention, whose quadratic complexity hinders efficiency. Moreover, small vision-language models often struggle to precisely capture fine-grained, task-relevant visual regions, leading to degraded performance on fine-grained reasoning tasks that limit their effectiveness in the real world. To address these issues, we introduce Firebolt-VL, an efficient vision-language model that replaces the Transformer-based decoder with a Liquid Foundation Model (LFM) decoder. To further enhance visual grounding, we propose a Token-Grid Correlation Module, which computes lightweight correlations between text tokens and image patches and modulates via the state-space model with FiLM conditioning. This enables the model to selectively emphasize visual regions relevant to the textual prompt while maintaining linear-time inference. Experimental results across multiple benchmarks demonstrate that Firebolt-VL achieves accurate, fine-grained understanding with significantly improved efficiency. Our model and code are available at: https://fireboltvl.github.io

---

## 论文详细总结（自动生成）

# Firebolt-VL：通过跨模态调制实现高效视觉-语言理解 — 论文总结

---

## 一、研究背景与核心问题

- **研究动机**  
  多模态大型语言模型（Multimodal LLM, MLLM）在视觉语言任务（如图像描述、VQA、OCR 等）上表现优异，但存在两大痛点：  
  1. **计算与内存代价高昂** —— 基于 Transformer 的跨注意力结构具有平方复杂度，难以在资源受限设备上部署。  
  2. **小模型细粒度理解不足** —— 小型模型难以聚焦于任务相关区域，导致细节性问题理解不准确。

- **研究目标**  
  提出一种高效、轻量、能捕捉细粒度区域的视觉语言模型，实现**线性复杂度推理**与**精确定向视觉理解**。

---

## 二、方法论与技术创新

### 2.1 总体结构

Firebolt-VL 由三个核心组件组成：

1. **视觉编码器（Vision Encoder）**：采用 SigLIP 模型，使用视觉 Transformer 将图像划分为 patch/grid，提取多尺度视觉嵌入。
2. **跨模态调制模块（Cross-Modal Modulator, CMM）**：承担视觉与文本特征融合，替代传统的跨注意力。
3. **语言解码器（Language Decoder）**：基于 Liquid Foundation Model (LFM) 的状态空间架构，实现线性时间的自回归解码。

### 2.2 核心思想与算法流程

- **轻量跨模态对齐（Token-Grid Correlation）**：
  - 将文本 token 与图像 grid embedding 投射到共享空间。
  - 计算 **token–grid 相关矩阵** \( S_{mm} = \sigma(X_t^H X_v^{H⊤} / \sqrt{D_H}) \)，并选取前 k 个最相关的 grid。
  - 使用加权求和得到每个 token 的视觉上下文向量。

- **特征线性调制（FiLM Conditioning）**：
  - 使用 FiLM（Feature-wise Linear Modulation）结构对文本表示执行按通道缩放与偏移：
    - \( \text{FiLM}(x, c) = \gamma(c) \odot x + \beta(c) \)
  - 通过视觉上下文控制文本特征，以实现语义层级的跨模态融合。

- **状态空间融合与线性推理（State-Space Model, SSM）**：
  - 使用结构化状态空间模型（S4/S4D/Mamba）传递时间依赖信息，复杂度近似 O(T)。
  - 结合 FiLM-in 与 FiLM-out 双重调制，使文本在序列建模的每一阶段保持视觉条件关联。

- **复杂度优势**：  
  相较 Transformer 跨注意力的 O(TG) 复杂度，CMM+CMM 的组合在 top-k 稀疏选择下几乎实现 **线性时间与线性内存**。

---

## 三、实验设计

### 3.1 训练流程

模型分两阶段训练：

1. **阶段一**：冻结视觉与语言模块，初始化 CMM（在 CC3M 数据集上）。  
2. **阶段二**：端到端训练，使用 LLaVA-CoT 与自构建的 MMPR-v1.2 数据集进行推理增强（包含 Chain-of-Thought 格式）。

### 3.2 基准测试与对比方法

评估任务覆盖七个主流数据集：
- **VQAv2**, **POPE**, **AI2D**, **MMMU-val**, **MME**, **SQA-IMG**, **MMB-dev**

对比模型包括：
- 大模型 (>7B)：IDEFICS、OpenFlamingo v2、MiniGPT-4 (v1/v2)、Chameleon、FastV  
- 小模型 (<3B)：Kosmos-2、MobileVLM、MobileVLM V2、MoE-LLaVA、LLaVA-Phi、SmolVLM2  

---

## 四、资源与算力使用

- **硬件环境**：使用 2 张 NVIDIA H100 (80GB) GPU 进行 CMM 预训练，端到端阶段使用 8 张 H100。
- **训练参数**：
  - Stage 1 批量大小 128，学习率 5×10⁻⁴；
  - Stage 2 批量大小 8，学习率 1×10⁻⁴；
  - 各阶段训练 5 个 epoch，以验证集困惑度（perplexity）选优。
- **未说明细节**：未明确给出训练时长与能耗估计。

---

## 五、实验充分性与客观性

- **主实验**：在 7 个基准任务上与 12 个代表性模型进行定量比较（包括大/小模型对照）。
- **消融实验**：包含三类分析：
  1. **SSM 选择**（Mamba/S4D/S4）
  2. **跨模态融合方式对比**（Prepend、Cross-Attend、CMM）
  3. **Top-k grid 敏感性分析**（k=1~5）  
- **实验充分性评价**：  
  - 消融与主任务覆盖全面，特别关注结构部件对性能的贡献。  
  - 多基准验证结果一致性良好，具备客观性。  
  - 未报告显著方差/显著性分析，但趋势清晰。

---

## 六、主要结论与发现

- **效果方面**：
  - 参数量仅 **0.8B**，在 VQAv2、MME、AI2D 等任务上超越同类轻量模型；
  - 相比 MobileVLM V2 (1.7B)，在 MME 感知评分上提升至 **1376.2**；
  - 线性时间解码的 **推理速度提升 20–100%**，吞吐达 46.67 tok/s。
- **结构性结论**：
  - 状态空间模型结合 FiLM 调制能有效取代注意力结构；
  - Token-Grid 机制提升模型对细粒度细节的感知与定位；
  - Firebolt-VL 在准确率—延迟权衡上最优。

---

## 七、优点与创新亮点

- ⚙️ **计算效率显著提升**：LFM 解码器和 CMM 模块实现近线性推理复杂度。  
- 🔍 **细粒度视觉理解**：Token-Grid 关联显著增强区域感知能力。  
- 🧩 **结构设计新颖**：融合 SSM 与 FiLM，为跨模态建模提供新范式。  
- 📈 **综合性能出色**：在轻量模型中实现 SOTA 表现，兼顾速度与精度。  
- 🧪 **实验设计周全**：系统性消融验证了每个模块的有效性。

---

## 八、不足与局限

- 🚫 **应用范围受限**：当前模型仅支持单图像输入，未能处理视频或多图交互。  
- 🧮 **轻量模型偏差**：隐式状态传播机制有时欠抑制幻觉（如在 POPE 基准上略低）。  
- ⏱️ **训练细节不完整**：未报告训练时间与能效对比，难以全面评估实际部署成本。  
- 📊 **缺少多语言与多模态拓展实验**：主要聚焦于英文任务与静态图像。  

---

**（完）**
