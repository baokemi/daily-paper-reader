---
title: "Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation"
title_zh: Firebolt-VL：通过跨模态调制实现高效的视觉-语言理解
authors: "Quoc-Huy Trinh, Mustapha Abdullahi, Bo Zhao, Debesh Jha"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04579v1"
tags: ["query:visfed"]
score: 7.0
evidence: 适用于资源受限场景的高效视觉语言模型
tldr: Firebolt-VL是一种高效的视觉语言模型，针对传统基于Transformer的交叉注意力计算复杂度高和细粒度推理能力弱的问题，采用Liquid Foundation Model解码器和Token-Grid相关模块，在保证精度的同时实现线性时间推理并增强视觉定位能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04579v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison of the proposed Firebolt-VL model with existing MLLMs across seven benchmarks. The superscript p denotes the perception score on the MME benchmark, while SQAI refers the ScienceQA-IMG [30]. The best results are shown in bold, and the second-best results are underlined. “–” indicates results not reported in the original papers.\", \"page\": 6, \"index\": 1, \"width\": 1038, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.04579v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison of responses from Firebolt-VL with recent efficient vision–language models, including MobileVLM V2 [10] and SmolVLM2 [31], on detail-dependent question-answering tasks. Firebolt-VL demonstrates stronger fine-grained grounding and more accurate, instruction-aligned responses.\", \"page\": 6, \"index\": 2, \"width\": 891, \"height\": 589}, {\"url\": \"assets/figures/arxiv/2604.04579v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of the Firebolt-VL architecture. The Cross-Modal Modulator (CMM) fuses textual instructions with the visual representations of the query image to produce conditioned tokens, which are then processed by the Liquid Foundation Model (LFM). The model is trained in two stages: (1) CMM pre-training to initialize modulation parameters, and (2) end-to-end training of the full framework.\", \"page\": 4, \"index\": 3, \"width\": 949, \"height\": 448}]"
motivation: 现有多模态大模型计算开销高且难以在资源受限场景中高效执行，特别是在细粒度视觉推理上表现不足。
method: 模型以LFM解码器替代Transformer并引入Token-Grid相关模块，通过轻量相关和FiLM调制增强视觉对齐与线性推理能力。
result: 实验结果表明，Firebolt-VL在多个基准上实现了高精度的细粒度理解，并显著提升推理效率。
conclusion: Firebolt-VL有效平衡了性能与效率，为资源受限场景下的视觉语言理解提供了可行方案。
---

## 摘要
多模态大型语言模型（MLLM）的最新进展推动了视觉-语言理解方面的显著提升，然而，其高昂的计算成本限制了在诸如个人助手、文档理解和智能摄像头等资源受限场景中的部署。现有的大多数方法依赖于基于 Transformer 的跨模态注意力机制，而其平方级复杂度限制了模型的效率。此外，小型视觉-语言模型通常难以精准捕捉与任务相关的细粒度视觉区域，从而导致在细粒度推理任务上的性能下降，削弱了在现实场景中的实用性。为了解决这些问题，我们提出了 Firebolt-VL，这是一种高效的视觉-语言模型，它用 Liquid Foundation Model（LFM）解码器替代了基于 Transformer 的解码器。为了进一步增强视觉锚定能力，我们设计了一个 Token-Grid Correlation Module，该模块通过轻量级地计算文本 token 与图像 patch 之间的相关性，并通过带有 FiLM 条件的状态空间模型进行调制，从而使模型能够在保持线性时间推理的同时，有选择地强调与文本提示相关的视觉区域。多项基准测试的实验结果表明，Firebolt-VL 在显著提升效率的同时，实现了准确的细粒度理解。我们的模型与代码可在 https://fireboltvl.github.io 获取。

## 速览
**TLDR**：Firebolt-VL是一种高效的视觉语言理解模型，针对现有多模态大模型计算复杂度高和细粒度视觉理解能力不足的问题，采用Liquid Foundation Model解码器并引入Token-Grid相关模块，通过FiLM调制实现轻量级跨模态关联，从而在保持高效线性推理的同时提升视觉对齐与理解精度。 \
**Motivation**：现有视觉语言模型计算昂贵且难以捕捉细粒度视觉信息，限制其在实际场景中的应用。 \
**Method**：采用基于Liquid Foundation Model的解码器和Token-Grid相关模块实现文本与图像间的轻量级跨模态调制。 \
**Result**：实验表明Firebolt-VL在多项基准上表现出更高的精度与推理效率。 \
**Conclusion**：Firebolt-VL显著提升了视觉语言理解的效率与精度，为资源受限场景中的多模态应用提供了有效解决方案。

---

## Abstract
Recent advances in multimodal large language models (MLLMs) have enabled impressive progress in vision-language understanding, yet their high computational cost limits deployment in resource-constrained scenarios such as personal assistants, document understanding, and smart cameras. Most existing methods rely on Transformer-based cross-attention, whose quadratic complexity hinders efficiency. Moreover, small vision-language models often struggle to precisely capture fine-grained, task-relevant visual regions, leading to degraded performance on fine-grained reasoning tasks that limit their effectiveness in the real world. To address these issues, we introduce Firebolt-VL, an efficient vision-language model that replaces the Transformer-based decoder with a Liquid Foundation Model (LFM) decoder. To further enhance visual grounding, we propose a Token-Grid Correlation Module, which computes lightweight correlations between text tokens and image patches and modulates via the state-space model with FiLM conditioning. This enables the model to selectively emphasize visual regions relevant to the textual prompt while maintaining linear-time inference. Experimental results across multiple benchmarks demonstrate that Firebolt-VL achieves accurate, fine-grained understanding with significantly improved efficiency. Our model and code are available at: https://fireboltvl.github.io

---

## 论文详细总结（自动生成）

# Firebolt-VL：通过跨模态调制实现高效的视觉-语言理解 — 中文总结

---

## 一、核心问题与研究背景

- **研究动机**：  
  当前的多模态大型语言模型（Multimodal Large Language Models, MLLMs）在视觉-语言理解任务上取得了显著进步，但其 **计算成本极高**，导致难以在资源受限的设备（如智能助手、嵌入式系统、智能摄像机等）中部署。  
  同时，小型视觉-语言模型在 **细粒度视觉特征捕获** 与 **跨模态推理** 方面表现不足，无法准确定位任务相关的视觉区域，降低在视觉问答、图表理解等任务中的有效性。

- **核心问题**：
  1. 传统 Transformer 的注意力机制计算复杂度为平方级（O(n²))，在长序列和多模态输入下效率极低。  
  2. 小型模型难以实现 **精准视觉锚定（visual grounding）**。  
  3. 需要新的机制在保持高性能的同时实现 **线性时间推理**。

- **研究目标**：  
  提出既能实现精确视觉对齐、又具有高推理效率的轻量级视觉语言模型架构。

---

## 二、方法论与技术路线

### 1. 整体架构
Firebolt-VL 模型包括三大模块：
1. **Vision Encoder**：基于 SigLIP 多语言视觉编码器，提取图像的网格级嵌入（grid-level embeddings）。  
2. **Cross-Modal Modulator（CMM）**：核心创新模块，通过状态空间模型（SSM）和 FiLM 调制实现高效的跨模态融合。  
3. **Liquid Foundation Model (LFM) 解码器**：以液态状态空间模型代替传统 Transformer，实现线性时间推理。

### 2. 核心思想
- 用 **状态空间模型（Structured State Space Model, SSM）** 替代自注意力机制，保持线性复杂度。
- 引入 **Feature-wise Linear Modulation (FiLM)**：对文本 token 特征进行逐元素加性和乘性调制，使视觉上下文信号可直接影响语言通道。
- 建立 **Token-Grid 相关机制**：计算文本 token 与视觉 patch 间的轻量相关矩阵，集中于前 k 个最相关区域（top-k 视觉网格）。

### 3. 关键步骤（文字化算法流程）
1. 图像经 SigLIP 编码器处理，生成视觉网格特征 \( X_v \)。  
2. 文本经语言嵌入层编码为 \( X_t \)。  
3. 双模态投影到同一潜空间，计算相关矩阵 \( S_{mm} = \sigma({X_t' X_v'^T / \sqrt{D}}) \)。  
4. 选择 top-k 最相关视觉网格，得到上下文向量 \( c \)。  
5. 使用 FiLM 进行两次线性调制（FiLM-in / FiLM-out），在进入状态空间模型前后对文本特征进行缩放与偏移。  
6. 状态空间模型以线性复杂度进行序列建模，输出视觉调制后的文本序列表示 \( X_{out} \)。  
7. 最后经均值池化得到全局多模态向量，并送入 LFM 解码器生成语言响应。

---

## 三、实验设计

### 1. 训练流程
- **两阶段训练**：
  1. **阶段一**：预训练 CMM 模块（冻结视觉编码器和语言模型），使用 **CC3M** 图像字幕数据集。
  2. **阶段二**：端到端训练，使用 **LLaVA-CoT** 数据集及自构建 **MMPR-v1.2** 数据集，采用链式推理格式。

### 2. 对比基准与任务
- **任务：**
  - 图像描述（Image Captioning）
  - 视觉问答（VQA）
  - 图表与图解理解（AI2D）
  - 文本识别（OCR）与科学问题解释（ScienceQA-IMG）

- **Benchmark：**
  - VQAv2、POPE、AI2D、MMMU-val、MME、SQA-Image、MMB-dev。

- **对比模型：**
  - 大型模型：IDEFICS、OpenFlamingo v2、MiniGPT-4 系列、Chameleon、FastV。  
  - 轻量模型：Kosmos-2、MobileVLM / MobileVLM V2、MoE-LLaVA、LLaVA-Phi、SmolVLM2。

---

## 四、资源与算力

- **训练硬件**：
  - 阶段一：2 × NVIDIA H100 80GB GPU。  
  - 阶段二：8 × NVIDIA H100 80GB GPU。  
- **训练规格**：
  - 阶段一批量大小 128，阶段二批量大小 8。  
  - 学习率分别为 \(5 × 10^{-4}\) 和 \(1 × 10^{-4}\)。  
  - 每阶段训练 5 epoch（根据验证集 perplexity 指标选取最优模型）。  

明确说明了训练环境，算力规模中等偏高，但与模型规模（0.8B参数）相匹配。

---

## 五、实验数量与充分性

- **定量评测：**
  - 共七个标准数据集的综合测试，覆盖感知、推理、问答、图表理解等任务。  
  - 含多组 SSM 变体实验（Mamba, S4D, S4）。  
  - 不同跨模态融合策略对比（Prepend、Cross-Attention、CMM）。  
  - top-k 网格选择的超参数敏感性分析（k = 1~5）。  

- **实验充分性**：  
  设计较为全面，涵盖性能、效率、结构与融合机制对比，数据与模型规模匹配合理，结果客观且公平。

---

## 六、主要结论与发现

- **性能方面**：
  - 在参数量不到 1B 的情况下，Firebolt-VL 在多数任务上与 7B+ 参数的大模型表现接近甚至更优。  
  - 在 VQAv2 和 MME 基准上达到最高分，表现出优异的视觉感知与推理能力。

- **效率方面**：
  - 通过 LFM 解码器和 CMM 模块，推理速度提升显著（最高吞吐量达 46.67 token/s），优于所有轻量基线模型（如 MobileVLM 系列）。

- **机制验证**：
  - 结构化状态空间模型（S4/S4D）优于 Mamba。  
  - CMM 模块显著提升跨模态感知与精度。  
  - 最优 top-k 值为 4。

---

## 七、优点与创新亮点

- **技术创新**：
  - 将 **液态状态空间模型（LFM）** 引入多模态领域，实现线性时间复杂度。  
  - 设计 **跨模态调制模块 CMM**，结合 Token-Grid 相关性与 FiLM 调制实现高效视觉对齐。  
  - 无需显式注意力矩阵，大幅减少计算与内存占用。

- **设计优势**：
  - 可在单 GPU 上高效推理，适用于实时或边缘设备场景。  
  - 模块化架构易扩展至视频或多图任务。  
  - 提供代码与模型公开（促进复现和后续研究）。

---

## 八、不足与局限性

- **功能限制**：
  - 当前模型仅支持单图输入，尚未扩展到视频或多图序列。  
  - 缺乏针对多时序视觉线索的优化版本。

- **潜在问题**：
  - 状态空间模型隐状态传播可能导致 **幻觉内容抑制能力弱**（在 POPE benchmark 上略有下降）。  
  - top-k 选择敏感，过多视觉网格会引入噪声。

- **未来方向**：
  - 设计适用于多视频帧与长序列视觉输入的扩展版 CMM。  
  - 进一步提升稳定性与跨任务泛化。

---

**（完）**
