---
title: "FedSpy-LLM: Towards Scalable and Generalizable Data Reconstruction Attacks from Gradients on LLMs"
title_zh: FedSpy-LLM：面向LLM的可扩展与可泛化梯度数据重建攻击研究
authors: "Syed Irfan Ali Meerza, Feiyi Wang, Jian Liu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06297v1"
tags: ["query:visfed"]
score: 9.0
evidence: 联邦大语言模型中的隐私挑战与数据重构攻击
tldr: 本文提出FedSpy-LLM，一种针对联邦学习中大语言模型的可扩展、通用数据重构攻击方法，通过梯度分解策略实现从PEFT训练的模型梯度中高效恢复长文本和多样架构的训练数据，揭示联邦LLM的隐私漏洞。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06297v1/fig-001.webp\", \"caption\": \"TABLE IV COMPARISON OF SEQUENCE RECONSTRUCTION FROM GRADIENTS BETWEEN FEDSPY-LLM AND OTHER BASELINE ALGORITHMS UNDER TWO DIFFERENT PEFT METHODS WITH b = 4 ON COLA DATASET.\", \"page\": 11, \"index\": 1, \"width\": 516, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2604.06297v1/fig-002.webp\", \"caption\": \"TABLE V TEXT RECONSTRUCTION COMPARISON WITH b = 4. YELLOW MARKS TOKENS RECOVERED CORRECTLY, GREEN SHOWS CORRECT N-GRAMS.\", \"page\": 11, \"index\": 2, \"width\": 520, \"height\": 623}, {\"url\": \"assets/figures/arxiv/2604.06297v1/fig-003.webp\", \"caption\": \"TABLE I COMPARISON OF STATE-OF-THE-ART DATA RECONSTRUCTION ATTACKS ON LLMS.\", \"page\": 2, \"index\": 3, \"width\": 1076, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2604.06297v1/fig-004.webp\", \"caption\": \"TABLE III COMPARISON OF SEQUENCE RECONSTRUCTION FROM GRADIENTS BETWEEN FEDSPY-LLM AND DAGER ON LARGER BATCH SIZES WITH DIFFERENT DATASETS AND VARIOUS MODEL ARCHITECTURES. R-1, R-2, AND R-L DENOTE THE ROUGE-1, ROUGE-2, ROUGE-L SCORES RESPECTIVELY.\", \"page\": 10, \"index\": 4, \"width\": 1064, \"height\": 391}, {\"url\": \"assets/figures/arxiv/2604.06297v1/fig-005.webp\", \"caption\": \"TABLE II COMPARISON OF SEQUENCE RECONSTRUCTION FROM GRADIENTS BETWEEN FEDSPY-LLM AND OTHER BASELINE ALGORITHMS ON VARIOUS BATCH SIZES AND DATASETS WITH BERTBase TARGET MODEL. R-1, R-2, AND R-L DENOTE THE ROUGE-1, ROUGE-2, AND ROUGE-L SCORES, RESPECTIVELY. ALL RESULTS ARE REPORTED AS MEAN ± STANDARD DEVIATION OVER 10 INDEPENDENT RUNS.\", \"page\": 10, \"index\": 5, \"width\": 1064, \"height\": 391}, {\"url\": \"assets/figures/arxiv/2604.06297v1/fig-006.webp\", \"caption\": \"Fig. 1. Overview of FEDSPY-LLM. FEDSPY-LLM enables an adversary to reconstruct client training data by initializing a dummy input and iteratively updating it to match the client’s gradient. To reduce the search space, the server projects candidate tokens onto the gradient’s column space and recovers the correct sequence by comparing individual token gradients iteratively.\", \"page\": 5, \"index\": 6, \"width\": 709, \"height\": 441}]"
motivation: 联邦学习结合LLM的隐私需求增加，但现有梯度攻击方法在规模、序列长度和架构通用性上受限。
method: 提出基于梯度分解的攻击策略，利用梯度的秩缺陷和子空间结构高效提取和排序标记。
result: FedSpy-LLM能重建更长序列和更大批次的数据，并在各种LLM架构下保持高恢复精度。
conclusion: FedSpy-LLM证明了在使用PEFT和不同架构的联邦LLM中，仍可有效重构训练数据，揭示了潜在隐私风险。
---

## 摘要
随着在大型语言模型（LLM）训练中对私有数据依赖的不断增加，联邦学习（Federated Learning，FL）结合参数高效微调（Parameter-Efficient Fine-Tuning，PEFT）因其在隐私与效率方面的优势而受到广泛关注。尽管FL能够在一定程度上保护隐私，先前研究表明仍可通过共享梯度提取私有数据。然而，这些研究主要针对全参数模型训练，且局限于重建小批量数据、短输入序列以及特定模型架构（如仅编码器或仅解码器模型）。当处理来自PEFT方法的梯度时，重建质量进一步下降。为全面理解联邦LLM的实际攻击面，本文提出了FedSpy-LLM，一种可扩展且具备泛化能力的数据重建攻击方法，旨在在更大的批量规模和更长的序列下重建训练数据，并能在不同模型架构之间泛化，即使在使用PEFT方法训练的情况下也同样适用。FedSpy-LLM的核心是一种新颖的梯度分解策略，它利用梯度的秩亏性质与子空间结构，实现高效的token提取，同时在大规模条件下保留关键信号成分。该方法进一步缓解了PEFT引入的大规模零空间带来的重建挑战，确保在仅编码器、仅解码器以及编码器—解码器等多种模型架构下的鲁棒性。此外，通过迭代地将每个token的部分序列梯度与完整序列梯度对齐，FedSpy-LLM可在重建序列中保持准确的token顺序。

## Abstract
Given the growing reliance on private data in training Large Language Models (LLMs), Federated Learning (FL) combined with Parameter-Efficient Fine-Tuning (PEFT) has garnered significant attention for enhancing privacy and efficiency. Despite FL's privacy benefits, prior studies have shown that private data can still be extracted from shared gradients. However, these studies, mainly on full-parameter model training, are limited to reconstructing small batches, short input sequences, and specific model architectures, such as encoder-based or decoder-based models. The reconstruction quality becomes even worse when dealing with gradients from PEFT methods. To fully understand the practical attack surface of federated LLMs, this paper proposes FedSpy-LLM, a scalable and generalizable data reconstruction attack designed to reconstruct training data with larger batch sizes and longer sequences while generalizing across diverse model architectures, even when PEFT methods are deployed for training. At the core of FedSpy-LLM is a novel gradient decomposition strategy that exploits the rank deficiency and subspace structure of gradients, enabling efficient token extraction while preserving key signal components at scale. This approach further mitigates the reconstruction challenges introduced by PEFT's substantial null space, ensuring robustness across encoder-based, decoder-based, and encoder-decoder model architectures. Additionally, by iteratively aligning each token's partial-sequence gradient with the full-sequence gradient, FedSpy-LLM ensures accurate token ordering in reconstructed sequences.

---

## 论文详细总结（自动生成）

## 一、研究背景与核心问题

- **研究动机**：  
  随着用于训练大型语言模型（LLMs）的数据量激增，其中常包含高度敏感的个人或机密信息。联邦学习（Federated Learning, FL）通过仅共享梯度而非原始数据，被认为能有效提升隐私性，同时结合参数高效微调（Parameter-Efficient Fine-Tuning, PEFT）在资源受限场景下实现高效训练。  
  然而，先前研究表明梯度信息仍可能泄露原始数据，即便在FL框架下也无法完全避免隐私暴露。已有梯度数据重建攻击在图像或语音等模态取得成功，但在文本、尤其是LLM场景中存在 **三大难题**：  
  1. 批量规模和序列长度增长导致搜索空间指数扩大；  
  2. PEFT方法产生稀疏、低秩梯度，使信息难以辨识；  
  3. 模型架构复杂（编码器 / 解码器 / 编码器-解码器）造成梯度信号交织。  
  本文针对这一瓶颈提出 **FedSpy-LLM** ——一种可扩展、具泛化能力的LLM梯度数据重建攻击方法，以揭示联邦LLM的隐私风险边界。

---

## 二、方法论：FedSpy-LLM 的设计与原理

### 1. 核心思想  
FedSpy-LLM 基于**梯度分解与低秩子空间分析**思想，认为LLM的梯度矩阵在批量维度和隐藏维度上本质为低秩结构，输入嵌入向量受限于该梯度的列空间。利用此特性，可从共享梯度中推断输入 token embeddings 并恢复训练文本。

### 2. 关键机制  
- **梯度子空间引导（Gradient Subspace Guidance）**：  
  通过将候选嵌入向量投影到梯度矩阵的列空间，并最小化投影距离，指导优化搜索，比传统“全梯度匹配”更高效、更稳定。  

- **空域正则化（Null Space Regularization）**：  
  处理PEFT梯度稀疏问题。计算梯度的零空间（null space），并在优化目标中加入惩罚项，限制嵌入向量的偏移方向，减少信息噪声和歧义。  

- **序列顺序校准（Sequence Order Calibration）**：  
  利用部分序列梯度与完整序列梯度的余弦相似度，迭代确定 token 的正确位置。证明了“正确顺序的部分梯度与全序列梯度更对齐”这一理论性质（定理3）。  

### 3. 优化目标函数  
综合梯度匹配损失与子空间正则项，最终目标为：  

\[
L_\text{FedSpy-LLM} = L_{rec} + \gamma L_{dis} + \eta \| P_N x^* \|^2
\]
其中：
- \(L_{rec}\)：层级加权余弦相似度损失（结合梯度方向与幅度）；  
- \(L_{dis}\)：投影距离约束；  
- \(P_N\)：投影到 null 空间的矩阵；  
- \(\gamma, \eta\)：权重超参数。

流程分两阶段：  
1. **Token Recovery**：通过梯度匹配与子空间投影生成候选嵌入。  
2. **Sequence Recovery**：利用梯度对齐度逐位确定 token 顺序。

---

## 三、实验设计

### 1. 数据集与任务  
- **文本任务类型**：  
  - 二分类（情感分析）：CoLA、Rotten Tomatoes  
  - 语言建模（下一词预测）：MIMIC-III（医疗记录文本）  
- **任务场景**：FL 环境下的 LLM 微调，针对客户端梯度逆向推断输入数据。

### 2. 模型与架构  
选取三类主流 Transformer 架构：  
- **Encoder-based**：BERT Base  
- **Decoder-based**：GPT-2  
- **Encoder-decoder**：T5 Base  
并扩展至大型模型 Llama-2 (7B, 13B) 与 Llama-3.1 (8B, 70B)。

### 3. 对比基线  
共对比七种主流攻击方法：DLG、TAG、LAMP、APRIL、FILM、BGP、DAGER，覆盖优化搜索类与解析类攻击。

### 4. 评价指标  
- ROUGE-1/2/L（单词、双词、最长公共子序列匹配）  
- 实体级F1（Named Entity Recovery）  
- 时间复杂度与运行时分析。

---

## 四、资源与算力

- **实现环境**：PyTorch  
- **硬件配置**：NVIDIA A100 Tensor Core GPU（40GB）  
  - Llama-2 与 GPT-2 试验使用双 GPU  
  - 其他模型单卡运行，内存需求 16GB–250GB  
- **时间成本**：  
  - BERT Base + MIMIC-III：约 39小时  
  - GPT-2：约 9小时  
  - Llama-2 (7B)：约 14小时（b=4），提高批次至 b=8 时为 19小时。  
  整体性能与计算效率在与搜索类方法（如 DAGER）对比中保持较优。

---

## 五、实验数量与充分性

- 涉及 **3大任务类型 × 多模型架构 × 多批次规模（b=1–128） × 不同PEFT方法（SLoRA, FedAdapter）**。  
- **消融实验**：  
  - 比较不同损失函数（L1/L2、余弦相似度）  
  - 评估序列顺序校准前后性能提升  
  - 测试隐私防御（加噪、梯度裁剪、差分隐私DP）下攻击效果  
  - 模型大小差异实验（从7B至70B）。  
- 实验覆盖面广，对比基线全面，性能评估客观，结果有标准偏差报告（±σ），具统计稳健性。

---

## 六、主要结论与发现

- FedSpy-LLM **大幅提升梯度数据重建的可扩展性与稳定性**：  
  - 批量可扩展至128；  
  - 可跨架构（Encoder/Decoder/Encoder-Decoder）重建文本；  
  - PEFT下仍保持高精度。  
- 在多数任务中较现有最优攻击（DAGER）**提升40–250%重建质量**（ROUGE指标）。  
- 序列顺序校准显著提高句法保持度（ROUGE-L可提升>30点）。  
- 对抗隐私防御（DP-SGD、加噪、裁剪）仍能恢复有效成分，显示隐私风险更严重。

---

## 七、方法与实验的优点

- **理论创新**：首次系统分析LLM梯度的低秩特性与列空间结构，为攻击建模提供数学支撑。  
- **算法设计**：组合梯度投影与空域正则化，使优化免受PEFT稀疏梯度干扰。  
- **架构泛化**：跨Encoder/Decoder体系均可使用，无需访问特定层结构。  
- **序列恢复机制**：基于梯度对齐度的顺序推断，有效解决早期攻击无法恢复句序的问题。  
- **实验覆盖**：数据集多样，结果统计充分，对比公平。

---

## 八、不足与局限性

- **算力成本较高**：优化与顺序校准阶段计算复杂度高，仍需十数小时至数十小时计算。  
- **仍依赖白盒模型访问**：需完全获取每轮客户端梯度，未考虑安全聚合或加密情境。  
- **未评估跨语言与多模态场景**：仅限英文文本，医学与情感场景。  
- **防御措施的研究仍表浅**：仅测试固定噪声等级(σ=10⁻⁵)，缺乏系统防御优化分析。  
- **隐私威胁范围未量化**：虽证明可恢复文本，但未对真实风险（如个人可识别信息泄露概率）进行量化研究。

---

（完）
