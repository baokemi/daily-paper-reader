---
title: "BlazeFL: Fast and Deterministic Federated Learning Simulation"
title_zh: BlazeFL：快速且确定性的联邦学习模拟
authors: "Kitsuya Azuma, Takayuki Nishio"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03606v1"
tags: ["query:flrlvldm"]
score: 7.0
evidence: 用于快速且确定性联邦学习模拟的轻量级框架
tldr: 本文提出BlazeFL，一个轻量级单节点联邦学习模拟框架，通过线程级并行和独立随机数流管理解决并行计算中的非确定性问题，既提升运行效率又保证结果可重复，实验表明其在图像分类任务中显著快于现有方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03606v1/fig-001.webp\", \"caption\": \"Figure 3. Wall-clock time for five communication rounds on the workstation-class server (32 CPU cores, NVIDIA Quadro RTX 6000) as a function of client parallelism P . Timings include client training, server aggregation, and global evaluation, but exclude dataset download and partition generation. Comparison of BlazeFL (free-threaded), BlazeFL (process-based shared memory), and Flower (Ray backend). Lower is better.\", \"page\": 6, \"index\": 1, \"width\": 1036, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2604.03606v1/fig-002.webp\", \"caption\": \"Figure 2. Wall-clock time for five communication rounds on the high-performance server (48 CPU cores, NVIDIA H100) as a function of client parallelism P . Timings include client training, server aggregation, and global evaluation, but exclude dataset download and partition generation. Comparison of BlazeFL (free-threaded), BlazeFL (process-based shared memory), and Flower (Ray backend). Lower is better. Missing points for BlazeFL (process-based) indicate execution failure due to CUDA out-of-memory errors.\", \"page\": 5, \"index\": 2, \"width\": 1036, \"height\": 720}, {\"url\": \"assets/figures/arxiv/2604.03606v1/fig-003.webp\", \"caption\": \"Figure 4. Accumulation of non-deterministic errors in Flower across 10 runs with manual global seeding. The y-axis represents the L2 distance between each run’s client logits and the mean logits of all runs at the start of each communication round. The trajectories fan out as floating-point rounding differences from nondeterministic aggregation order compound over time.\", \"page\": 7, \"index\": 3, \"width\": 496, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2604.03606v1/fig-004.webp\", \"caption\": \"Figure 1. Architecture overview of BlazeFL. A main thread coordinates client scheduling, while worker threads execute within a shared address space, enabling server-to-client parameter broadcast and client-to-server uploads without cross-process serialization or IPC. Each client is associated with an isolated RNG stream to support deterministic repeated execution under controlled settings.\", \"page\": 1, \"index\": 4, \"width\": 496, \"height\": 413}]"
motivation: 现有单节点FL模拟在并行效率和结果可复现性之间存在难以兼顾的矛盾。
method: 通过自由线程的共享内存执行和独立随机数流管理实现确定性并行的联邦学习模拟。
result: 在CIFAR-10实验中，BlazeFL相对主流开源基线实现了最高3.1倍加速且结果可重现。
conclusion: BlazeFL在固定软硬件条件下可实现高并发下的完全可重复FL模拟，为研究提供高效稳定的实验平台。
---

## 摘要
随着联邦学习（FL）研究越来越依赖在单节点上模拟数百或数千个虚拟客户端，高效性和可复现性变得至关重要。然而，并行的客户端训练常常因共享随机状态和调度差异导致非确定性，从而迫使研究者在吞吐量与可复现性之间做出权衡，或在复杂的框架中自定义控制逻辑。我们提出 BlazeFL，这一轻量级的单节点 FL 模拟框架，通过自由线程的共享内存执行和确定性的随机性管理，消除了这种权衡。BlazeFL 采用基于线程的并行机制，并在服务器与客户端之间进行内存中的参数交换，从而避免了序列化和进程间通信的开销。为支持确定性执行，BlazeFL 为每个客户端分配独立的随机数生成器（RNG）流。在固定的软件/硬件栈下，当随机算子使用由 BlazeFL 管理的生成器时，该设计能够在高并发情况下的线程模式和进程模式下均产生逐位相同的结果。在 CIFAR-10 图像分类实验中，BlazeFL 相较于广泛使用的开源基线，显著减少了执行时间，在以通信为主的工作负载中实现了最高达 3.1 倍的加速，同时保持轻量级依赖。我们的开源实现可在以下地址获得：https://github.com/kitsuyaazuma/blazefl。

## 速览
**TLDR**：本文提出BlazeFL，一个轻量级的单机联邦学习模拟框架，通过多线程共享内存执行和独立随机数管理实现高效率与确定性，在CIFAR-10实验中相较现有框架速度提升3.1倍并确保完全可重复。 \
**Motivation**：当前单节点联邦学习模拟在效率与可重复性之间存在权衡问题。 \
**Method**：采用多线程共享内存执行和独立随机数流管理以实现确定性与高效性。 \
**Result**：在CIFAR-10实验中，BlazeFL比主流开源基线快至3.1倍，并保持轻量依赖和结果可重复性。 \
**Conclusion**：BlazeFL证明可以在单机高并发环境下实现高效且完全可重复的联邦学习模拟。

---

## Abstract
Federated learning (FL) research increasingly relies on single-node simulations with hundreds or thousands of virtual clients, making both efficiency and reproducibility essential. Yet parallel client training often introduces nondeterminism through shared random state and scheduling variability, forcing researchers to trade throughput for reproducibility or to implement custom control logic within complex frameworks. We present BlazeFL, a lightweight framework for single-node FL simulation that alleviates this trade-off through free-threaded shared-memory execution and deterministic randomness management. BlazeFL uses thread-based parallelism with in-memory parameter exchange between the server and clients, avoiding serialization and inter-process communication overhead. To support deterministic execution, BlazeFL assigns isolated random number generator (RNG) streams to clients. Under a fixed software/hardware stack, and when stochastic operators consume BlazeFL-managed generators, this design yields bitwise-identical results across repeated high-concurrency runs in both thread-based and process-based modes. In CIFAR-10 image-classification experiments, BlazeFL substantially reduces execution time relative to a widely used open-source baseline, achieving up to 3.1$\times$ speedup on communication-dominated workloads while preserving a lightweight dependency footprint. Our open-source implementation is available at: https://github.com/kitsuyaazuma/blazefl.

---

## 论文详细总结（自动生成）

# BlazeFL：快速且确定性的联邦学习模拟 —— 中文论文总结

## 一、研究问题与背景

- **研究动机：**  
  随着联邦学习（Federated Learning, FL）研究的广泛发展，研究者通常需要在单机上模拟数百至上千个虚拟客户端，用于算法原型验证与超参数搜索。然而现有的 FL 模拟框架存在两大难题：  
  1. **效率问题**：多进程或分布式设计虽能绕过 Python GIL（全局解释器锁），却引入进程间通信（IPC）与参数序列化开销，导致模拟速度受限。  
  2. **可复现性问题**：并行执行中共享随机状态及线程调度差异导致非确定性结果，使同一实验的重复运行无法获得逐位一致的结果。  

- **核心问题：**  
  是否能在单节点环境下实现既快速又完全可重复的 FL 模拟？  

- **研究目标：**  
  设计一个轻量、高效、确定性的单节点 FL 模拟框架，以支持高并发条件下的 reproducible 实验运行。

---

## 二、方法论与核心设计

BlazeFL 的核心思想是利用 **“自由线程架构（free-threaded Python）+ 共享内存的数据交换 + 隔离随机数流”**，消除性能与确定性之间的权衡。

### 1. 执行架构：共享内存＋自由线程
- 基于 PEP 703 和 PEP 779，使 Python 在 `free-threaded` 模式下可实现真正的线程级并行。  
- 将客户端以**工作线程（worker threads）**方式运行于同一进程内，共享内存空间：  
  - **参数下行**：服务器通过共享内存直接广播模型参数。  
  - **参数上行**：客户端训练后直接写回共享内存，无需序列化或管道通信。
- 同时提供 **multiprocessing 模式**（进程级）供对比。

### 2. 确定性执行控制
- 每个客户端拥有**独立的随机数生成器 (RNG) 流**：
  - 初始化基于可控的种子规则。  
  - 随机性操作（如数据打乱、数据增强、Dropout）均通过该独立 RNG 生成。  
- 客户端结果按照**采样顺序**（非完成顺序）汇总，避免了由于浮点加法顺序变化引起的非确定性。  
- 若所有随机算子使用 BlazeFL 的 RNG，框架可在同一软硬件环境下实现**逐位一致的结果**。  

### 3. 接口设计
- 基于 Python `typing.Protocol`（结构化子类型接口），允许用户继承任意类，仅要定义标准方法，即可快速接入现有 PyTorch 代码。
- 减少框架锁定（framework lock-in），方便研究者快速试验。

### 4. 轻量依赖与可移植性
- 不依赖外部分布式后端（如 Ray、MPI），核心仅依赖 Python 标准库与 PyTorch。
- 易于移植、归档和复现。

---

## 三、实验设计与对比方案

### 1. 数据集与任务
- 使用 **CIFAR-10** 图像分类任务。
- 客户端划分方式：非独立同分布（non-IID），每个客户端拥有两个类别数据。  
- 每个通信轮次：
  - 每轮选取 100 个客户端。  
  - 每客户端：本地训练 5 个 epoch，每个 epoch 对 500 张图片。  
  - 测试集固定为 10,000 张图片。

### 2. 对比基线
- **Flower (Ray backend)**：主流开源 FL 模拟框架。
- **BlazeFL (process-based)**：基于多进程但仍使用共享内存的实现。  
- **BlazeFL (free-threaded)**：本文提出的单进程多线程实现。

### 3. 实验变量
- 并行客户端数量 \( P \in \{1, 2, 4, 8, 16, 32, 64\} \)。
- 模型架构：轻量 CNN、ResNet-18、ResNet-50、ResNet-101。

---

## 四、软硬件资源与算力配置

- **高性能服务器：**
  - GPU：NVIDIA H100
  - CPU：48 核
  - 内存：192 GB  
- **工作站级服务器：**
  - GPU：NVIDIA Quadro RTX 6000
  - CPU：32 核
  - 内存：256 GB  
- **运行环境：**
  - BlazeFL：Python 3.14.3（支持 free-threading）
  - Flower：Python 3.13.7  
- 文中未明确总训练时长，但每项实验执行 5 轮通信，主要统计墙钟时间（wall-clock time）。
- 算法均使用单 GPU 环境；无多 GPU 或集群训练设置。

---

## 五、实验数量与充分性分析

- 实验包含两套硬件环境、四类网络结构、七种并行度，大约进行了 **56 组性能测试**。  
- 另外开展了多组复现性对比实验：
  - 不同随机种子控制（有/无全局种子、独立 RNG）。  
  - 不同并行度 \( P \) 下的确定性验证。  
- 提供了可量化指标：
  - **最终精度标准差**与**模型权重哈希（SHA-256）一致性**。  
- 实验证据充分、量化明确，且涵盖从轻量到深层模型、从线程到进程的多维分析，结论具有较高可信度。

---

## 六、主要结论与发现

1. **性能提升显著：**
   - 在以通信为主的轻量 CNN 工作负载下，BlazeFL（free-threaded）相较 Flower 实现了 **最高 3.1× 加速**。  
   - 对计算密集模型（ResNet-18/50）仍保持 1.1–1.4× 加速。
2. **完全可重复性：**
   - 在固定软硬件栈下，BlazeFL 的线程模式与进程模式均可实现在 10 次重复运行中的 **精度标准差 0.00、模型哈希完全一致**。  
   - Flower 即使在固定全局种子下仍产生累积性数值漂移。  
3. **扩展性与稳定性：**
   - BlazeFL 的结果在不同并行度（1–64）下完全一致，证明执行顺序与线程调度不影响输出。  
4. **解释性发现：**
   - Flower 中非确定性主要源于**聚合阶段浮点加法顺序变化**。小幅舍入误差随通信轮次累积导致明显结果偏移。

---

## 七、优点与创新亮点

- **高效性：**  
  消除了 Python GIL 限制下的性能瓶颈，在单机中实现真正的多线程并行。  
- **确定性：**  
  首个在高并发下实现逐位可重复（bitwise identical）结果的 FL 框架。  
- **简洁实现：**  
  依赖极少，部署与复现简单，利于科研可追溯性。  
- **接口灵活：**  
  使用 Protocol 接口，兼容原始 PyTorch 代码结构，无需复杂的继承体系。  
- **系统分析全面：**  
  对比了线程、进程、不同模型和硬件，验证结果多维一致。

---

## 八、不足与局限

1. **适用范围有限：**  
   - 明确仅针对**单节点模拟**，未涵盖多节点、跨网络或真实分布式部署情境。  
2. **复现性条件限定：**  
   - 结果确定性需固定软硬件栈；跨平台（GPU 型号、驱动、库版本）可能出现差异。  
3. **视觉管道限制：**  
   - 需要确保数据增强等算子显式使用 BlazeFL 的 RNG，否则可能破坏确定性。  
4. **生态成熟度：**  
   - Free-threaded Python 尚处早期阶段，部分第三方库尚未完全兼容，限制了生态整合性。  
5. **工作负载类别较单一：**  
   - 实验集中于图像分类（CIFAR-10），未覆盖 NLP 或其他 FL 应用场景。

---

**（完）**
