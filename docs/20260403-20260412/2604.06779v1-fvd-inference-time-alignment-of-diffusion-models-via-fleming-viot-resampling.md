---
title: "FVD: Inference-Time Alignment of Diffusion Models via Fleming-Viot Resampling"
title_zh: FVD：通过弗莱明-维奥重采样实现扩散模型的推理阶段对齐
authors: "Shivanshu Shekhar, Sagnik Mukherjee, Jia Yi Zhang, Tong Zhang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06779v1"
tags: ["query:profile-3"]
score: 9.0
evidence: 通过重采样理论进行扩散模型的推理阶段对齐
tldr: 本文提出了FVD，一种用于扩散模型推理阶段的Fleming-Viot重采样方法，通过引入生死机制与随机重生噪声解决SMC采样中的多样性崩塌问题。该方法无需价值函数训练且易并行，实验显示其在图像生成任务上显著提升性能与效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06779v1/fig-001.webp\", \"caption\": \"Figure 8: Qualitative comparison on aesthetic optimization. Samples from each method for a 50K NFE budget. As emphasized earlier, FKD achieves a high aesthetic score but produces samples that have overfit to the reward model and do not make sense [39], a symptom of over-optimization. FVD generates visually appealing images that remain faithful to the prompt.\", \"page\": 15, \"index\": 1, \"width\": 829, \"height\": 797}, {\"url\": \"assets/figures/arxiv/2604.06779v1/fig-002.webp\", \"caption\": \"Figure 6: Effect of target absorption rate α∗ on reward–diversity trade-off (CIFAR-10, 106 NFEs). Left: Mean reward increases and variance decreases with α∗ as stronger selection pressure favors high-reward samples. Right: MMD is minimized at intermediate α∗, indicating optimal distributional coverage; large α∗ leads to increased MMD due to mode collapse. These results show that α∗ provides a direct and interpretable tradeoff over reward maximization and sample diversity.\", \"page\": 10, \"index\": 2, \"width\": 806, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2604.06779v1/fig-003.webp\", \"caption\": \"Figure 2: Scaling behavior with increasing compute (NFEs). We plot FID as a function of the number of function evaluations (NFEs) on CIFAR-10 and MNIST. FVD consistently outperforms FKD and DTS across all compute budgets and settings, demonstrating favorable scaling while maintaining full parallelism.\", \"page\": 8, \"index\": 3, \"width\": 826, \"height\": 197}, {\"url\": \"assets/figures/arxiv/2604.06779v1/fig-004.webp\", \"caption\": \"Figure 10: Reward-rank distribution of removed particles per resampling step (K=1000, λ=1.0, CIFAR-10 class 1). FKD removes particles across the entire reward spectrum, including many high-reward samples, indicating weak selection specificity. In contrast, FVD concentrates removals among low-reward particles, preserving high-reward trajectories and maintaining a more faithful approximation to the target distribution.\", \"page\": 17, \"index\": 4, \"width\": 806, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2604.06779v1/fig-005.webp\", \"caption\": \"Figure 4: Particle collapse dynamics under FKD vs. FVD (K=1000, λ=1.0, CIFAR-10). Left: FVD maintains a consistently lower death rate throughout denoising, whereas FKD exhibits aggressive particle removal. Right: FVD preserves significantly more distinct ancestral lineages, avoiding the rapid collapse observed in FKD. Overall, FVD retains ∼ 10× more lineages, demonstrating improved diversity and stability.\", \"page\": 9, \"index\": 5, \"width\": 806, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2604.06779v1/fig-006.webp\", \"caption\": \"Figure 9: Qualitative comparison on DrawBench (ImageReward). Best samples generated by each method for each prompt at a 50K NFE budget. FVD produces samples with higher prompt alignment and perceptual quality compared to DTS and FKD, consistent with the quantitative results in Figure 3b.\", \"page\": 16, \"index\": 6, \"width\": 826, \"height\": 755}]"
motivation: 现有基于SMC的扩散采样在强选择压力下易导致样本多样性丧失和谱系崩塌。
method: 论文提出了基于Fleming-Viot种群动力学的推理时重采样机制，用独立生存决策与随机重生噪声替代传统多项式重采样。
result: "FVD在DrawBench和类条件任务中比基线方法分别提升7%的ImageReward和14-20%的FID，并且速度提升最高达66倍。"
conclusion: FVD成功解决了扩散模型在推理时多样性崩塌的问题，提供了高效可扩展的推理时间对齐方案。
---

## 摘要
我们提出了弗莱明-维奥扩散（FVD），这是一种在推理阶段进行对齐的方法，用以解决常见于基于序贯蒙特卡罗（SMC）的扩散采样器中的多样性坍缩问题。现有的基于 SMC 的扩散采样器通常依赖于多项式重采样或与其密切相关的重采样方案，但这些方法在强选择压力下仍可能削弱多样性并导致谱系坍缩。受弗莱明-维奥种群动力学的启发，FVD 用一种专为扩散对齐设计的特殊生灭机制取代了多项式重采样。为应对奖励值仅能近似获得且朴素的再生机制会导致确定性轨迹坍缩的情形，FVD 将基于奖励的独立生存判定与随机再生噪声相结合。这产生了灵活的种群动力学，在无需价值函数近似或昂贵的回滚计算的情况下，既能有效探索奖励偏置的分布，又能保持更广泛的轨迹支持。FVD 完全可并行化，并能高效扩展以适应推理计算。实验证明其在多种场景下均取得显著提升：在 DrawBench 上，其 ImageReward 得分比现有方法高出 7%；在类别条件任务中，相较于强基线方法，FID 提升约 14–20%，并且相比基于价值的方法快达 66 倍。

## Abstract
We introduce Fleming-Viot Diffusion (FVD), an inference-time alignment method that resolves the diversity collapse commonly observed in Sequential Monte Carlo (SMC) based diffusion samplers. Existing SMC-based diffusion samplers often rely on multinomial resampling or closely related resampling schemes, which can still reduce diversity and lead to lineage collapse under strong selection pressure. Inspired by Fleming-Viot population dynamics, FVD replaces multinomial resampling with a specialized birth-death mechanism designed for diffusion alignment. To handle cases where rewards are only approximately available and naive rebirth would collapse deterministic trajectories, FVD integrates independent reward-based survival decisions with stochastic rebirth noise. This yields flexible population dynamics that preserve broader trajectory support while effectively exploring reward-tilted distributions, all without requiring value function approximation or costly rollouts. FVD is fully parallelizable and scales efficiently with inference compute. Empirically, it achieves substantial gains across settings: on DrawBench it outperforms prior methods by 7% in ImageReward, while on class-conditional tasks it improves FID by roughly 14-20% over strong baselines and is up to 66 times faster than value-based approaches.

---

## 论文详细总结（自动生成）

# FVD：通过弗莱明–维奥重采样实现扩散模型的推理阶段对齐  
——论文结构化深入总结

---

## 一、研究问题与整体背景

- **核心问题**：  
  现有基于 **Sequential Monte Carlo (SMC)** 的扩散模型推理方法在进行奖励对齐（reward alignment）时，容易出现 **多样性坍塌（diversity collapse）** 与 **谱系崩塌（lineage collapse）**。即，在强选择压力下，采样轨迹集中至少量高奖励样本，导致生成结果缺乏多样性，并偏离训练分布。

- **研究动机**：  
  随着扩散模型应用于图像、视频、语言生成任务，其在推理阶段对齐奖励函数（如美学得分、文本匹配度）成为关键需求。  
  然而传统做法存在两类瓶颈：  
  1. **RL微调方法**（如RLHF、DPO）需重新训练模型，成本高、难适配不同奖励。  
  2. **推理时对齐方法**（如梯度引导、SMC采样、价值搜索）虽无需训练，但梯度法局限于可微奖励，SMC法易坍塌，多项式重采样带来高方差。  

- **论文目标**：  
  提出一种无需再训练、可并行、能保持样本多样性、同时对齐奖励分布的推理阶段采样方法。

---

## 二、核心方法：Fleming–Viot Diffusion (FVD)

### 1. 基本思想
- 借鉴 **Fleming–Viot 种群动力学** 的“生死-再生”机制，替代标准SMC重采样步骤。  
- 使用 **独立生存决策 + 随机重生噪声**，在每次采样过程中维持群体规模不变，同时防止轨迹坍塌。

### 2. 技术机制与公式说明

**核心步骤（算法框架）**：
1. 初始化：生成 K 个DDIM噪声轨迹 `x_T^(i) ∼ N(0, I)`。
2. 在每个扩散步 `t`，所有粒子执行标准 DDIM 更新。
3. 若为重采样步 `t ∈ T`：
   - 计算奖励代理 `r(ˆx₀(x_t, t))` 并映射为潜在函数  
     `G_t(x_t:T) = exp(λ / |T| · r(ˆx₀(x_t, t)))`。
   - 每个粒子 `i` 独立决定是否“死亡”：  
     生存概率 `s_i^(t) = G_t / max_j G_t(x_j:T)`。  
     若随机数超过 `s_i^(t)`，则判定死亡。
   - 对死亡粒子随机选取一位幸存者 `j` 作为 **donor**，再通过非确定性 DDIM 更新（η_rebirth>0）形成新的粒子。
4. 在线调整选择压力 λ：  
   使用 **Robbins–Monro 自适应更新**  
   `λ ← λ − η_k(α_t − α*)`，其中 α_t 为死亡比例、α*为目标吸收率。

**关键特性**：
- **死亡事件独立 Bernoulli 控制 → 方差 O(1)**，相比多项式重采样的 O(K) 明显降低。
- **随机重生噪声** 避免确定性DDIM复制导致的路径重叠。
- **自适应 λ 控制** 根据粒子吸收率自动调整优化强度，从而平衡奖励与多样性。

---

## 三、实验设计

### 1. 数据集与场景
- **类条件任务**：
  - MNIST 与 CIFAR-10，用分类器后验对齐任务 `r(x)=log p(c|x)`。
  - 还测试了 MNIST 偶数/奇数多模态版本。
- **文本到图像生成**：
  - Stable Diffusion v1.5 作为基模型。
  - **DrawBench**（ImageReward 评价，100个提示语）。  
  - **Aesthetic Optimization**（LAION Aesthetic Predictor评分，45类动物）。

### 2. 对比方法
- **SMC类**：FKD（最强SMC基线）、TDS（Tree Diffusion Sampling）、DAS。  
- **梯度类**：DPS。  
- **价值函数类**：DTS（基于MCTS搜索）。

### 3. Benchmark与评估指标
- 图像质量：FID、MMD。  
- 奖励对齐效果：平均 log r(x)、ImageReward、美学分数。  
- 多样性指标：Div、多样性坍塌分析。  
- 算力效率：NFE（Number of Function Evaluations）、wall-clock time。  
- 额外分析：谱系保留数量、死亡率曲线、吸收率控制、λ自适应。

---

## 四、资源与算力

- 文中 **未明确说明具体算力参数**（如GPU型号、数量或运行时间）。  
- 仅提到在“固定 NFE 的匹配计算预算”下进行比较。可推测所有实验在相同推理步数、并行粒子数量 K 下执行。  
- FVD 具有 **完全并行** 结构，相比 DTS **速度提升约 66 倍**。

---

## 五、实验充分性与公平性

- **实验数量**：  
  - 基线对比覆盖 MNIST、CIFAR-10、两组文图生成任务。  
  - 进行了 **谱系分析、死亡率分析、吸收率和 λ 自适应消融、噪声参数 rebirth_eta 消融**。  
  - 提供理论证明、统计表格及定性图像对比，共计涵盖 4 种场景、至少 5 种 baseline、数十组结果。

- **充分性评价**：  
  - 实验覆盖从低维（MNIST）到高维（Stable Diffusion）的大范围任务。  
  - 指标全面且结果跨种子统计，因此可认为 **较为充分且公平**。  
  - 未报告跨模型类型（如视频或语言扩散）的泛化实验。

---

## 六、主要结论与发现

- FVD 通过 **Fleming–Viot 重采样机制** 成功避免了 SMC 推理阶段的多样性崩塌：  
  - 维持约 **10倍更多谱系**（vs FKD）。  
  - 降低死亡率并集中移除低奖励样本。  
- **性能提升**：
  - 类条件任务：FID 提升 14–20%，MMD 降低显著。  
  - DrawBench：ImageReward 提升约 7%。  
  - 速度：比 DTS 快 **约 66×**。
- 自适应 λ 控制与吸收率 α* 能实现直观的奖励–多样性折中，简化超参数调节。
- 在美学优化任务中，FVD 生成样本更稳健、避免过拟合奖励模型。

---

## 七、主要优点

- **创新性强**：首次将 Fleming–Viot 种群动态引入扩散采样。
- **无训练需**：完全推理时对齐，无需回滚或价值函数学习。
- **并行效率高**：粒子独立更新，极易并行，实现大规模推理。
- **稳定性与可解释性**：
  - 独立死亡判定降低方差。  
  - α* 提供直观的控制信号。  
  - λ 自适应使算法自调节、鲁棒性高。
- **理论与实证结合紧密**：在附录中给出随机过程收敛的非严格理论动机与多项式方差界。

---

## 八、不足与局限

- **适用范围有限**：目前仅验证于图像扩散模型，未涉及视频或语言扩散框架。
- **估计噪声问题**：依赖 Tweedie 近似，在早期噪声较大阶段近似误差可能影响性能。
- **缺少算力具体信息**：未提供硬件或时间配置，难以复现资源消耗。
- **缺少多目标场景**：未探讨同时优化多种奖励（如语义+美学）的情况。
- **理论证明不完备**：仅提供非正式收敛论证（mean-field近似），未给出完整严格证明。

---

**（完）**
