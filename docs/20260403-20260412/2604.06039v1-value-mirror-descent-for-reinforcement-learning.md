---
title: Value Mirror Descent for Reinforcement Learning
title_zh: 用于强化学习的价值镜像下降
authors: "Zhichao Jia, Guanghui Lan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06039v1"
tags: ["query:flrlvldm"]
score: 10.0
evidence: 强化学习的新型价值优化方法
tldr: 本文提出了将镜像下降引入强化学习值迭代框架的新方法VMD和其随机变体SVMD，在已知或生成模型下实现线性或近最优收敛；同时保证策略与最优策略间的Bregman距离有界，提升了理论样本效率与实际持续学习能力。
source: arxiv
selection_source: fresh_fetch
motivation: 现有值迭代方法在样本复杂度和策略收敛性方面仍有改进空间，尤其是用于离线训练和持续学习时。
method: 提出了结合镜像下降和传统值迭代的VMD算法及其随机变体SVMD，并利用方差减少技术提升性能。
result: SVMD在一般凸正则化下达到近最优样本复杂度，并在强凸正则化下实现更高精度和策略收敛保证。
conclusion: 该研究为强化学习中的值优化提供了理论与算法上的新贡献，证明了收敛性和样本复杂度的改进。
---

## 摘要
价值迭代类方法在强化学习（RL）中已被广泛研究，用于计算近似最优的价值函数。在生成采样模型下，这些方法在样本复杂度方面可以比策略优化方法更优，特别是在其对折扣因子的依赖上。在实际应用中，它们常用于离线训练或模拟环境中。本文研究具有状态空间 S、动作空间 A、折扣因子 $γ∈(0,1)$ 以及成本在 $[0,1]$ 范围内的折扣马尔可夫决策过程。我们提出了一种新颖的价值优化方法——价值镜像下降（Value Mirror Descent, VMD），它将凸优化中的镜像下降方法融入传统的价值迭代框架。在已知转移核的确定性设置中，我们证明了 VMD 线性收敛。在具有生成模型的随机设置下，我们开发了随机变体 SVMD，它结合了常用于随机价值迭代类方法的方差减少技术。对于具有一般凸正则项的强化学习问题，SVMD 达到了近最优的样本复杂度 $\tilde{O}(|S||A|(1-γ)^{-3}ε^{-2})$。此外，我们证明了所生成策略与最优策略之间的 Bregman 散度在迭代过程中始终保持有界。该性质在现有随机价值迭代类方法中尚不存在，但对于在离线训练后实现有效的在线（持续）学习非常重要。在强凸正则项的情况下，SVMD 达到样本复杂度 $\tilde{O}(|S||A|(1-γ)^{-5}ε^{-1})$，在高精度阶段提高了性能。此外，我们还证明了所生成的策略收敛于最优策略。总体而言，所提出的方法、其分析及相应的理论保证构成了对强化学习与优化领域的新贡献。

## Abstract
Value iteration-type methods have been extensively studied for computing a nearly optimal value function in reinforcement learning (RL). Under a generative sampling model, these methods can achieve sharper sample complexity than policy optimization approaches, particularly in their dependence on the discount factor. In practice, they are often employed for offline training or in simulated environments. In this paper, we consider discounted Markov decision processes with state space S, action space A, discount factor $γ\in(0,1)$ and costs in $[0,1]$. We introduce a novel value optimization method, termed value mirror descent (VMD), which integrates mirror descent from convex optimization into the classical value iteration framework. In the deterministic setting with known transition kernels, we show that VMD converges linearly. For the stochastic setting with a generative model, we develop a stochastic variant, SVMD, which incorporates variance reduction commonly used in stochastic value iteration-type methods. For RL problems with general convex regularizers, SVMD attains a near-optimal sample complexity of $\tilde{O}(|S||A|(1-γ)^{-3}ε^{-2})$. Moreover, we establish that the Bregman divergence between the generated and optimal policies remains bounded throughout the iterations. This property is absent in existing stochastic value iteration-type methods but is important for enabling effective online (continual) learning following offline training. Under a strongly convex regularizer, SVMD achieves sample complexity of $\tilde{O}(|S||A|(1-γ)^{-5}ε^{-1})$, improving performance in the high-accuracy regime. Furthermore, we prove convergence of the generated policy to the optimal policy. Overall, the proposed method, its analysis, and the resulting guarantees, constitute new contributions to the RL and optimization literature.

---

## 论文详细总结（自动生成）

## 一、核心问题与研究背景

- **研究主题**：本文关注强化学习中**值迭代类算法（Value Iteration-type methods）**的收敛效率与样本复杂度优化问题。  
- **背景动机**：  
  - 传统值迭代与Q学习在确定性环境中表现优秀，但在**随机生成模型（generative model）**下如何在理论上同时保证**线性收敛与样本最优性**仍是挑战。  
  - 相比之下，策略优化类方法（如PPO、TRPO等）更易于扩展至在线学习，但常在样本复杂度上不及值迭代方法。  
  - 此外，现有值迭代类方法缺乏对策略稳定性的理论控制，生成的近最优策略可能与最优策略相差较大，不利于持续学习或迁移学习。  

于是，作者试图**融合凸优化中的镜像下降（Mirror Descent）思想**改造经典值迭代算法，从理论上实现高效收敛与策略稳定性。

---

## 二、提出的方法论

### 1. 核心思想

- 提出 **价值镜像下降（Value Mirror Descent, VMD）** 和其随机变体 **SVMD（Stochastic Value Mirror Descent）**。
- **核心思路**：在值迭代的每次更新中引入镜像下降的**近端映射（prox mapping）与Bregman散度**控制，从而在优化过程中同时约束策略更新的距离与方向。

### 2. VMD 算法框架（确定性环境）

每次迭代包括两个关键步骤：

1. **策略更新（镜像下降步）**：  
   \[
   \pi_{k+1}(·|s) = \arg\min_{\pi(·|s)\in Δ_A}\{ η_k[\langle c(s,·)+γP_s V_k, π(·|s)\rangle + h(π(·|s))]+D(π‖π_k)(s)\}
   \]
   用步长 $η_k$ 和 Bregman 散度平衡贪婪性与稳健性。
2. **值更新**：  
   \[
   V_{k+1} = Γ_{π_{k+1}} V_k
   \]
   代表根据新策略计算下一步值函数。

定理表明：在恰当步长和迭代轮数下，VMD 线性收敛至 ε-最优值函数，迭代复杂度为 $\tilde{O}((1-γ)^{-1})$。

### 3. SVMD 算法框架（随机环境）

- 在未知转移概率的情况下，仅能通过生成模型采样。  
- 引入**方差减少技术**：  
  - 第一次采样时估计 $PV_0$；后续仅估计增量 $P(V_t-V_0)$；
  - 建立无偏近似 $\tilde{P}_t V_t$。
- 每个 epoch 包含多次迭代，每次使用近似随机梯度进行镜像更新，并在值更新阶段采用**分量最小操作**维持单调性与稳定性。
- 提供严格样本复杂度分析：  
  - 一般凸正则化下：$\tilde{O}(|S||A|(1-γ)^{-3}ε^{-2})$；
  - 强凸正则化下：$\tilde{O}(|S||A|(1-γ)^{-5}ε^{-1})$。

### 4. 特殊结果：策略稳定性

引入 Bregman 散度界证明：
- 算法生成的策略与最优策略之间的距离有界：$\|D_{π^*}^{π_K}\|_\infty = O((1-γ)^{-2})$。
- 在强凸情形下，策略与最优策略**收敛且稳定**，有利于持续学习阶段的在线微调。

---

## 三、实验设计与验证

- **理论论文性质**：本文全部分析基于**数学推导和理论证明**，未涉及实际数值实验或具体数据集。  
- **验证方式**：通过对算法收敛性、复杂度、Bregman散度控制的严格证明完成算法验证。  
- **对比**：
  - 与 **Variance-Reduced Q-value Iteration**（Sidford et al., 2018）比较——样本复杂度保持相同阶并提供额外稳定性保证。
  - 与 **Policy Mirror Descent**（Lan, 2023）方法比较——在依赖项 $(1-γ)$ 和收敛性质上更优。

---

## 四、资源与算力

- **论文未进行实验**，故**未出现计算资源或GPU规格说明**。  
- 所有结果为理论推导，无训练过程或算力消耗信息。

---

## 五、实验数量与充分性

- 本文未进行数值实验，仅含理论分析与完整证明。  
- 因此不存在实验样本或benchmark测试，但理论推导包含多层递归、概率收敛、高概率事件分析等，对算法收敛性与样本效率的验证非常完整。

---

## 六、主要结论与发现

1. **VMD（确定性环境）**
   - 线性收敛至最优值函数；
   - 迭代复杂度与经典值迭代相同，但更具优化视角。
2. **SVMD（一般凸正则 / 随机环境）**
   - 样本复杂度为 $\tilde{O}(|S||A|(1-γ)^{-3}ε^{-2})$；
   - 保证策略与最优策略间 Bregman 散度有界；
   - 支持离线到在线的“暖启动”。
3. **SVMD（强凸正则情况）**
   - 样本复杂度改善至 $\tilde{O}(|S||A|(1-γ)^{-5}ε^{-1})$；
   - 策略收敛性保证，Bregman散度线性收敛。
4. **综合贡献**
   - 统一了值迭代与镜像下降框架；
   - 提供了第一种在强凸环境中达到 O(ε⁻¹) 依赖的值迭代类方法。

---

## 七、优点与亮点

- **理论创新**：将镜像下降与值迭代结合，首次提出“价值镜像下降”框架。
- **样本效率最优**：在凸正则化设定下与理论下界匹配。
- **策略稳定性**：引入Bregman散度控制，使生成策略更贴近最优策略。
- **可迁移性**：支持离线训练与在线持续学习衔接。
- **强凸情形突破**：实现O(ε⁻¹)样本复杂度，是价值类方法的首次理论突破。

---

## 八、不足与局限

- **缺乏实证实验**：论文完全为理论研究，未在真实强化学习任务中验证算法效果。
- **应用可行性尚未测试**：未探讨在大型连续状态空间（如Deep RL）中的适用性。
- **复杂度常数与实现性**：理论样本复杂度近似最优，但常数项较大，可能在实践中影响性能。
- **泛化问题**：对生成模型假设较强，无法直接适用于无模型或部分可观测环境。
- **策略正则**：依赖凸/强凸正则假设，不一定适合非凸策略分布（如神经网络参数化）。

---

（完）
