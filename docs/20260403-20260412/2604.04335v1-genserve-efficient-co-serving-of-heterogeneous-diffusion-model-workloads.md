---
title: "GENSERVE: Efficient Co-Serving of Heterogeneous Diffusion Model Workloads"
title_zh: GENSERVE：高效协同服务异构扩散模型工作负载
authors: "Fanjiang Ye, Zhangke Li, Xinrui Zhong, Ethan Ma, Russell Chen, Kaijian Wang, Jingwei Zuo, Desen Sun, Ye Cao, Triston Cao, Myungjin Lee, Arvind Krishnamurthy, Yuke Wang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04335v1"
tags: ["query:profile-3"]
score: 9.0
evidence: 异构扩散模型工作负载的高效协同服务
tldr: 本文提出GENSERVE系统，通过在扩散推理的离散步骤间进行资源弹性调整和任务抢占，实现文本到图像与文本到视频任务的高效协同服务，显著提升延迟SLO达标率和资源利用效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04335v1/fig-001.webp\", \"caption\": \"Figure 7: System overview of GENSERVE. Incoming requests are admitted by the Scheduler, which maintains cluster states and queries the Profiler for offline latency estimates to invoke the Solver for joint optimization. The resulting decisions are passed to the Allocator and the Controller, which dispatches tasks and preemption signals to Workers. Workers execute inference on assigned GPUs and report step-level progress back through the Result Queue.\", \"page\": 6, \"index\": 1, \"width\": 494, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.04335v1/fig-002.webp\", \"caption\": \"Figure 1: Serving 4 videos (V1–V4) and 3 images (I1–I3) on 4 GPUs. (a) FIFO: videos occupy all GPUs until completion; images must wait, causing HOL blocking and 5 deadline misses (SLO 2/7). (b) GENSERVE: when I1, I2 arrive, the scheduler preempts V3 and V4 at step boundaries, batches I1+I2 on GPU 3, and serves I3 on GPU 2. After images complete, V3 and V4 resume with SP degree switching (each scaling to 2 GPUs) to recover lost slack. All 7 requests meet their deadlines (SLO 7/7).\", \"page\": 2, \"index\": 2, \"width\": 492, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2604.04335v1/fig-003.webp\", \"caption\": \"Table 4: Key notation in the §4.4.\", \"page\": 8, \"index\": 3, \"width\": 482, \"height\": 324}]"
motivation: 在共享GPU集群上同时服务文本生成图像和视频的扩散模型时，由于不同任务的计算特征和延迟需求差异大，现有系统容易出现延迟SLO违约。
method: GENSERVE利用扩散过程的可预测性，结合智能视频抢占、动态批处理的弹性并行以及SLO感知调度，共同优化资源分配。
result: "实验表明，GENSERVE相比最强基线能在不同配置下提升最高44%的SLO达标率。"
conclusion: GENSERVE有效解决了异构扩散模型共服务中的资源调度问题，实现了高性能与高SLO保障。
---

## 摘要
扩散模型已成为文本生成图像（T2I）和文本生成视频（T2V）的主流方法，但生产平台必须在共享的 GPU 集群上同时提供这两种模式的服务，并满足严格的延迟 SLO 要求。协同服务这类异构工作负载具有挑战性：T2I 和 T2V 请求在计算需求、并行特性以及延迟要求上差异巨大，导致现有服务系统中出现严重的 SLO 违约。我们提出了 GENSERVE，这是一种利用扩散过程固有可预测性以优化服务效率的协同服务系统。核心见解是，扩散推理过程以离散且可预测的步骤进行，并且在步骤边界处天然可抢占，从而为感知异构性的资源管理开启了新的设计空间。GENSERVE 通过三个协调机制在步骤级别实现资源自适应：智能视频抢占、具备动态批处理的弹性序列并行，以及面向 SLO 的调度器，后者联合优化所有并发请求的资源分配。实验结果表明，GENSERVE 在多种配置下将 SLO 达成率相较于最强基线提升至多 44%。

## Abstract
Diffusion models have emerged as the prevailing approach for text-to-image (T2I) and text-to-video (T2V) generation, yet production platforms must increasingly serve both modalities on shared GPU clusters while meeting stringent latency SLOs. Co-serving such heterogeneous workloads is challenging: T2I and T2V requests exhibit vastly different compute demands, parallelism characteristics, and latency requirements, leading to significant SLO violations in existing serving systems. We present GENSERVE, a co-serving system that leverages the inherent predictability of the diffusion process to optimize serving efficiency. A central insight is that diffusion inference proceeds in discrete, predictable steps and is naturally preemptible at step boundaries, opening a new design space for heterogeneity-aware resource management. GENSERVE introduces step-level resource adaptation through three coordinated mechanisms: intelligent video preemption, elastic sequence parallelism with dynamic batching, and an SLO-aware scheduler that jointly optimizes resource allocation across all concurrent requests. Experimental results show that GENSERVE improves the SLO attainment rate by up to 44% over the strongest baseline across diverse configurations.

---

## 论文详细总结（自动生成）

# GENSERVE：高效协同服务异构扩散模型工作负载 — 综合总结

---

## 一、核心问题与研究背景

### 研究动机
- 文本到图像（T2I）和文本到视频（T2V）的扩散模型在生成任务中表现卓越，但在**生产部署**中面临挑战。
- 用户在同一平台提交图像和视频生成请求，这两类任务的**计算负载、并行度和延迟目标**差异极大。
- 在共享 GPU 集群上共服务这类异构负载，现有系统常出现：
  - 延迟违约（SLO violation）严重；
  - 图像任务被长视频阻塞（Head-of-Line Blocking）；
  - 静态配置无法适应任务混合度变化。

### 背景意义
- 现有的扩散模型服务系统通常针对**单一模态（单一任务类型）**，无法实现跨模态资源协调。
- 作者提出“通过利用扩散过程的可预测性与步骤间可抢占性”来实现**异构任务协同优化**，这是当前扩散模型服务研究的重要突破方向。

---

## 二、方法论与核心思想

### 方法概述
**GENSERVE** 是一套面向异构扩散模型（图像与视频）的协同服务系统，核心思想是：
> 将扩散推理视为由可预测且离散的步骤组成的过程，在每个“步骤边界”上启用细粒度、可暂停与可恢复的资源管理。

### 三大机制
1. **智能视频抢占（Intelligent Video Preemption）**
   - 在无空闲 GPU 时，系统计算每个运行中视频的“剩余时长与截止宽裕度（slack）”。
   - 根据公式：  
     \( slack_v = D_v - t_{now} - S_{rem,v} \times T_{step}(v) \)  
     判断哪一个视频可安全暂停，以释放资源给紧急图像请求。
   - 暂停与恢复的代价极小（毫秒级），因为状态保持在 GPU 内存中。

2. **弹性资源分配（Elastic Resource Allocation）**
   - 对视频任务使用**可变序列并行度（Sequence Parallelism, SP）**：
     - 高分辨率视频 → 高 SP 度数；
     - GPU紧张时 → 下调 SP 度释放部分 GPU；
     - GPU空闲时 → 上调 SP 度加速视频完成。
   - 对图像任务启用**SLO 感知动态批处理（Dynamic Batching）**：
     - 合并相同分辨率图像；
     - 保证所有批内请求仍能满足各自截止时间。

3. **SLO 感知动态规划调度器（SLO-aware DP Scheduler）**
   - 在每个推理步骤边界重新求解资源分配问题。
   - 通过动态规划（DP）算法协调视频与图像的 GPU 资源分配：
     - 决策内容：GPU分配数、抢占目标、批大小、SP度数。
     - 优化目标：最大化满足 SLO 的请求数。
   - 时间复杂度 O(G·N·K)，其中 G 为视频组数，N 为 GPU 数量，K 为每组候选动作数。

### 系统架构
GENSERVE 由五个核心组件组成：
- **Scheduler**：维护集群状态、管理队列；
- **Profiler**：存储离线性能配置；
- **Solver（DP调度器）**：实时计算最优分配；
- **Allocator & Controller**：执行GPU分配与调度指令；
- **Workers**：在各GPU上执行推理、上报进度。

---

## 三、实验设计

### 实验平台
- 单节点 **8 × NVIDIA RTX PRO 6000 Blackwell GPU**（每卡 96GB GDDR7）。
- 系统环境：Ubuntu 24.04 LTS、CUDA 12.9、PyTorch 2.8、NCCL通信。

### 模型与数据集
- **模型**
  - 图像：Stable Diffusion 3.5 Medium (2.5B参数)；
  - 视频：Wan2.2-T2V-5B (5B参数)。
- **数据集**
  - 图像：DiffusionDB；
  - 视频：VBench。
  
### 工作负载设置
- 请求类型混合：轻视频(20:80)、平衡(50:50)、重视频(80:20)；
- 不同分辨率与帧数（256p–720p，41或81帧）；
- 到达模式：Poisson（稳定）与 Bursty（突发）。
- 每次实验含 **100 个请求**。

### 对照基线
1. **FCFS** - 先到先服务；
2. **SJF** - 最短任务优先；
3. **SRTF** - 最短剩余时间优先（带抢占）；
4. **RASP** - 静态分辨率感知并行配置。

### 指标体系
- **SLO Attainment Rate（SAR）**：截止时间前完成请求比例；
- 可变参数：SLO系数σ（0.8–1.3）、任务比例与到达率；
- 附加指标：请求延迟 CDF、P90/P99、GPU利用率。

---

## 四、资源与算力说明

- 算力规格明确：**8 × RTX PRO 6000 Blackwell GPUs**，每卡 96GB。
- 所有推理与调度操作均在单机完成，无多节点分布式部署。
- 文中**未涉及训练阶段**，所有模型为推理版部署。
- 动态规划求解器开销：
  - 平均 0.3–0.4 ms/轮；
  - 占单步的 <0.25% 执行时间；
  - 最大观测值 1.9 ms（G ≤ 12）。

---

## 五、实验数量与充分性

- **场景覆盖广泛**：不同任务比例（3种），不同到达率（12–36 req/min），不同SLO松紧度（σ值5档）。
- **消融实验**：系统组件逐项启用（Preemption → DP → SP Switching）验证贡献。
- **灵敏度分析**：
  - 分辨率分布变化（均匀 vs. 偏斜）；
  - 调度开销与预emption开销；
  - paused状态的显存占用。
- **结果全面性**：
  - 涵盖图像与视频 SLO、整体吞吐、P99延迟；
  - 同时包含资源利用与内存分析。

→ 评价：实验较为充分、覆盖面广，且多组对比基线公平选择，具备良好可重复性。

---

## 六、主要结论与发现

- GENServe 相比最强基线（SRTF或RASP）：
  - **提升最高 44% 的 SLO达标率**；
  - 在平衡负载下达成 **87% 总体完成率**；
  - 图像 SLO 达成 100%，视频达成率提升近 2–3倍。
- 系统能在毫秒级决策周期内完成资源重分配，不增加显著开销。
- 动态SP与抢占结合使 GPU利用率显著提高，避免长期视频阻塞。
- 相比静态GPU分区（Dedicated）方案，**动态复用（Replicated）架构**性能更稳定、利用率更高。

---

## 七、优点与创新

- **技术创新**
  - 利用扩散过程离散可预测性，实现步骤级抢占与调度；
  - 将传统不可中断推理转化为可暂停的高响应推理框架；
  - 首次将动态规划算法应用于扩散模型服务的实时资源分配。
- **实现层面**
  - 抢占延迟 < 0.2% 步时；
  - 暂停状态仅占 27 MB 显存；
  - SP 度切换开销微乎其微（O(0.4%)）。
- **应用意义**
  - 实现混合扩散模型（T2I+T2V）的统一服务体系；
  - 为未来多模态生成服务提供可扩展通用框架。

---

## 八、不足与局限

- **实验环境局限**
  - 所有实验在单机 8 GPU 上进行，缺乏跨节点分布式验证；
  - 未测试在低端或多异构硬件上的性能稳定性。
- **任务类型范围有限**
  - 仅涉及文本到图像与文本到视频两模态；
  - 未包含如文本到3D或音频生成等其他形式。
- **模型规模与推理场景**
  - 主要使用两款中大型模型，未验证对更大模型或多模型并行的适用性。
- **算法可扩展性**
  - 动态规划在超大任务规模下或多租户集群中，复杂度可能上升。

---

（完）
